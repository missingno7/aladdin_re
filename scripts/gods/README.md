# Gods tooling

Game-specific scripts for the Gods (USA) project go here (witnesses, classifiers,
native tools).  Shared tooling in `scripts/` takes `--game gods`.  Nothing yet:
see `docs/gods/STATUS.md`.
