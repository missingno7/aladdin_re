Start an active, sustained recovery goal in D:/Prog/aladdin_re. Use the configured Terra model at medium effort and work alone: no subagents, supervisor task, recurring automation or hourly idle gaps. If active goals are unavailable here, tell me before claiming continuous execution.

Goal: grow the connected recovered object/contact/collection subsystem and its adjacent callers into increasingly reusable game source. Work through successive meaningful recoveries for hours, continuing directly from one verified milestone to the next. Do not mark the goal complete after one leaf, one report or one milestone. No arbitrary token budget. Respect an explicit pause or stop.

First read docs/solo-recovery.md, the current section of docs/STATUS.md, docs/recovery-workflow.md and the recent recovery-cost log. Inspect actual main, working changes, implementation and current evidence; 067b957 is only the handoff baseline. Work directly in this checkout as its sole recovery writer. Do not overwrite concurrent work.

Follow the handoff's discovery, qualification and publication loop. Prioritize observed connected behavior, reuse existing semantic functions/AtomicPlan/SoundSeam, and compose upward. Preserve live RAM as the only state authority and keep machine bookkeeping out of semantic game source. Do not build speculative infrastructure.

For each change, run focused original-machine witnesses and executed negative controls; require strict outer equality, native future continuation, safe fresh-process restore and proof that the parent actually owns the path. A recorded comparison flag or caught unexplained exception is not a PASS. Review your own diff before integration. Run the full suite and cold history comparison at cohesive milestones, with source frozen and matching receipts. Commit and push verified milestones to main; never force-push or weaken verification.

Keep a compact progress/cost log, distinguishing semantic work, reused machinery and new manual machine concepts. Investigate failures, choose another useful adjacent frontier when a particular path is blocked, and continue without routine questions or coordination. Report substantive findings and milestones concisely. Do not claim background work is running unless an actual goal or live process is active.

Treat a live verifier without a final receipt as a wait, not a blocker. Use the visible scripts/dev.py launcher, retain one process/session handle, and inspect comparison.json after completion. Do not launch duplicates or mark blocked because repeated observation waits timed out. Recheck completed artifacts when resuming a blocked goal.
