# Gods recovery ledger

Append-only, one line per recovered region or escalation, newest last.  The
grinder's only per-region record; `STATUS.md` is updated once per milestone.

Line format (pipe-separated, one line):

    date | entry / arms | shape | semantics | tests | cold run (node, frames, artifacts dir, status) | hits, fallbacks | commit or blocker

`hits` and `fallbacks` are the candidate worker's `candidate_stats` from the
named cold run's `comparison.json`.  A region whose hits are zero on the
recorded histories is `NOT_EXERCISED` and does not belong here.

## Rows

2026-09-15 | baseline (no region) | - | - | tests/games/gods/test_boot.py, test_recording.py | f0ac19738f19 (main at the time), 15,148 frames, artifacts/gods/verify-main-2026-09-15, PASS (original vs original); tree of two recordings 29,731 frames, artifacts/gods/verify-tree-2026-09-15, PASS | - | b117d68
2026-09-15 | 002806 camera follow step / hold, right, left x arms; y clamp; the x-negative, x-limit, y-negative clamps declined (unwitnessed) | RAM-only leaf returning to its caller | gods_sega.game.camera.camera_follow; plan gods_sega.boundary.camera_follow_plan; candidate 'camera' | tests/games/gods/test_camera.py (17) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-camera-main, PASS; tree 29,731 frames, artifacts/gods/verify-camera-tree, PASS; mutant camera-mutant-result DIVERGENCE at frame 329 (artifacts/gods/verify-camera-mutant) | 6,678 hits, 0 fallbacks (tree: 11,448 hits) | 708404e
2026-09-16 | 002806 camera follow step / the x-limit clamp arm (00282C), witnessed by f40d7bcc9dda (three path classes, 1,130 hits on the tree) | RAM-only leaf, one more arm | camera.camera_follow unchanged; boundary._LIMIT_ARMS / WITNESSED_CLAMPS | tests/games/gods/test_camera.py over every census directory (61 fixtures MATCH) | f40d7bcc9dda, 17,620 frames, artifacts/gods/verify-camera-f40d7bcc, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-tree-2026-09-16b, PASS; mutant DIVERGENCE at frame 1,540 (artifacts/gods/verify-camera-mutant-f40d7bcc) | tree: 41,109 hits, 0 fallbacks (was 39,979 hits, 1,130 fallbacks) | this commit
2026-09-16 | 0018C8 sprite emitter / off-screen x, off-screen y, cache hit after 1–8 slots, cache miss into an empty slot or the ninth (all witnessed on three recordings); the flipped-sprite arm declined (unwitnessed) | a leaf with one platform operation inside: RAM-only prefix, the inline VDP tile upload 001974–001988 ceded to the machine, the register-restore suffix — the first Gods seam, Aladdin's proven shape reproduced; the pause/resume runner extracted to genesis_re.seam | gods_sega.game.sprites.emit_sprite; plan gods_sega.boundary.sprite_emit_plan (+ sprite_emit_suffix); candidates 'sprites', 'camera-sprites' | tests/games/gods/test_sprites.py (148: 134 fixtures MATCH across census-0018C8-{fb408bc7,7251bbd0ecf7,f40d7bcc9dda}), tests/common/test_seam.py | f0ac19738f19, 15,148 frames, artifacts/gods/verify-sprites-f0ac1973b, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16b, PASS (camera-sprites); mutants sprites-mutant-result and sprites-mutant-register DIVERGENCE at frame 429 (artifacts/gods/verify-sprites-mutant-*) | 40,935 hits, 306 fallbacks (all scheduler refusals; 13,218 seams entered, 13,108 completed, 0 foreign returns); tree: 306,295 hits, 2,077 fallbacks (1,733 scheduler refusals, 344 seam deadlines), 82,757 seams entered, 81,822 completed | this commit
2026-09-16 | 001164 sprite emitter sibling / offscreen-x, offscreen-y, placed (no flip), placed (flip), all witnessed over four recordings; the list-full guard (`0011B8`, `LIST_FULL` at `FFEE30`) declined (unwitnessed) | RAM-only leaf, no platform operation; the same descriptor layout as `0018C8` but its own id-to-descriptor table (`0011E6`) and a fixed tile field instead of a cache | gods_sega.game.sprites.emit_static_sprite; plan gods_sega.boundary.static_emit_plan; candidates 'sprites-static', 'camera-sprites' (extended to three gates) | tests/games/gods/test_static_sprites.py (23 fixtures MATCH free of an interposed VBlank, of 82 retained; the VBlank-interposed ones are the scheduler's own admission refusal, not a plan gap) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-sprites-static-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16c, PASS (camera-sprites, three gates); mutant sprites-static-mutant-result DIVERGENCE at frame 457 (artifacts/gods/verify-sprites-static-mutant) | 15,551 hits, 134 fallbacks (all scheduler admission: an interrupt due mid-activation); tree: 454,447 hits, 3,571 fallbacks (3,227 scheduler admission incl. 1,494 at 001164, 344 seam deadline) | this commit
2026-09-16 | 004150 work-table reset / zero-fill (common, all four recordings) and poison-fill (0xFE, witnessed once on fb408bc75597) | RAM-only leaf, fully unrolled (no data-dependent loop count, so cost is constant per arm) | gods_sega.game.tables.reset_table; plan gods_sega.boundary.table_reset_plan; candidates 'table-reset', 'camera-sprites' (extended to four gates) | tests/games/gods/test_tables.py (9 fixtures MATCH over four recordings) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-table-reset-f0ac1973, PASS; tree of eight recordings, artifacts/gods/verify-camera-sprites-tree-2026-09-16d, PASS (camera-sprites, four gates); mutant table-reset-mutant-result DIVERGENCE at frame 429 (artifacts/gods/verify-table-reset-mutant) | 6,669 hits, 0 fallbacks; tree: 495,476 hits, 3,571 fallbacks (3,227 scheduler admission, 344 seam deadline, unchanged from before this region) | this commit
2026-09-16 | 0049DA spawn queue / 0, 1 or 2 active slots per tick, continuing or retiring, all witnessed over four recordings | RAM-only leaf that calls the already-recovered 001164 once per active slot (0-2 calls); no platform operation of its own | gods_sega.game.spawn_queue.scan_spawn_queue; plan gods_sega.boundary.spawn_queue_plan (reuses boundary._static_emit_cost, factored out of static_emit_plan); candidates 'spawn-queue', 'camera-sprites' (extended to five gates) | tests/games/gods/test_spawn_queue.py (18 fixtures MATCH over four recordings) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-spawn-queue-f0ac1973, PASS; tree of eight recordings, artifacts/gods/verify-camera-sprites-tree-2026-09-16e, PASS (camera-sprites, five gates); mutant spawn-queue-mutant-result DIVERGENCE at frame 1,021 (artifacts/gods/verify-spawn-queue-mutant) | 6,597 hits, 72 fallbacks (all scheduler admission); tree: 536,020 hits, 3,939 fallbacks (3,595 scheduler admission incl. 368 at 0049DA, 344 seam deadline) | this commit
2026-09-16 | 0063FA grid cell lookup / a single path class witnessed on every recording (no branch, no store) | RAM-only leaf, straight-line, no calls | gods_sega.game.grid.grid_cell; plan gods_sega.boundary.grid_cell_plan; candidates 'grid-cell', 'camera-sprites' (extended to six gates) | tests/games/gods/test_grid.py (6 fixtures MATCH over four recordings) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-grid-cell-f0ac1973, PASS; tree of eight recordings, artifacts/gods/verify-camera-sprites-tree-2026-09-16f, PASS (camera-sprites, six gates); mutant grid-cell-mutant-result (a register, since the routine stores nothing) DIVERGENCE at frame 6,185 (artifacts/gods/verify-grid-cell-mutant) | 6,709 hits, 86 fallbacks (all scheduler admission); tree: 577,899 hits, 4,366 fallbacks (4,022 scheduler admission incl. 427 at 0063FA, 344 seam deadline) | this commit
2026-09-16 | 00FDB8 footprint stamp / rows × cells from the definition's height and width bytes (1×1, 3×1, 1×3, 1×2 witnessed over four recordings); the no-footprint arm (width bit 7) declined (unwitnessed); negative heights and footprints over 64 cells declined | RAM-only leaf over a caller-supplied record (A2 the definition, A5 the undo cursor, D0/D1 the position); loops bounded by record bytes, cost a formula in (rows, cells) | gods_sega.game.grid.stamp_footprint; plan gods_sega.boundary.footprint_stamp_plan; candidates 'footprint', 'camera-sprites' (seven gates) | tests/games/gods/test_footprint.py (32: 27 fixtures MATCH across census-00FDB8{,-7251bbd0ecf7,-f40d7bcc9dda,-fb408bc75597}, the VBlank-pre-empted ones through pathfacts.region_only) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-footprint-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16g, PASS (camera-sprites); mutant footprint-mutant-result DIVERGENCE (artifacts/gods/verify-footprint-mutant) | 11,517 hits, 65 fallbacks (all scheduler refusals); tree: 648,130 hits, 4,882 fallbacks (4,538 scheduler refusals, 344 seam deadlines) | this commit
2026-09-16 | 00FC8E solid drawer / 0-9 visible cells per call over a 1×1 to 3×3 rows×cells grid (the same definition bytes 00FDB8 reads), off-screen cells skipped on the x or the y test, the type-id table scan matching within 1-2 mismatches on every recording; the inline VDP upload arm (a table entry with the tile index's sign bit set) and an unterminated-or-unmatched table scan declined (unwitnessed) | RAM-only leaf over the same caller-supplied definition as 00FDB8, appending into the sprite list 0018C8/001164 already write; the earlier "per-object-type diversity" screening did not hold once censused with the current tracer | gods_sega.game.solids.draw_solid; plan gods_sega.boundary.draw_solid_plan; candidates 'solid-draw', 'camera-sprites' (eight gates) | tests/games/gods/test_solids.py (60: 9 unit + 51 fixtures MATCH across census-00FC8E{,-7251bbd0ecf7,-f40d7bcc9dda,-fb408bc75597}) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-solid-draw-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16h, PASS (camera-sprites, eight gates); mutant solid-draw-mutant-result (a register: the routine's own last write is the unconditional list-head pointer) DIVERGENCE at frame 429 (artifacts/gods/verify-solid-draw-mutant) | 11,472 hits, 110 fallbacks (all scheduler admission); tree: 718,111 hits, 5,648 fallbacks (5,304 scheduler admission incl. 766 at 00FC8E, 344 seam deadline) | this commit
2026-09-16 | 00FE08 animation step / the 'idle' arm (the live record's own moving flag negative) on every recording; the 'moving' arm declined -- common (about 0.6% of calls), not merely unwitnessed, but it calls the unrecovered coroutine and per-type dispatch at 00FFF0 | RAM-only leaf, refreshes the shared frame-budget word on both arms; a3 = a1+6 left changed regardless of arm | gods_sega.game.animation.animation_step; plan gods_sega.boundary.animation_step_plan; candidates 'animation-step', 'camera-sprites' (nine gates) | tests/games/gods/test_animation.py (53: 3 unit + 6 idle-arm fixtures MATCH, 42 moving-arm fixtures DECLINED, across census-00FE08{,-7251bbd0ecf7,-f40d7bcc9dda,-f0ac19738f19}) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-animation-step-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16i, PASS (camera-sprites, nine gates); mutant animation-step-mutant-result DIVERGENCE at frame 430 (artifacts/gods/verify-animation-step-mutant) | 11,542 hits, 40 fallbacks (38 scheduler admission, 2 unsupported domain); tree: 788,232 hits, 6,274 fallbacks (5,609 scheduler admission, 344 seam deadline, 321 unsupported domain at 00FE08) | this commit
2026-09-16 | 010332 countdown check / the 'idle' (control byte zero) and 'waiting' (countdown not yet zero) arms on every recording; the 'trigger' arm (countdown reaches zero) declined -- calls one of two unrecovered routines (01158C/0115D4) | RAM-only leaf, no frame, no calls on the recovered arms; caller-supplied control byte (A3) and countdown word (A5), not fixed globals | gods_sega.game.timers.countdown_check; plan gods_sega.boundary.countdown_check_plan; candidates 'countdown-check', 'camera-sprites' (ten gates) | tests/games/gods/test_timers.py (48: 3 unit + 10 idle/waiting fixtures MATCH, 33 trigger fixtures DECLINED, across census-010332{,-7251bbd0ecf7,-f40d7bcc9dda,-f0ac19738f19}) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-countdown-check-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16j, PASS (camera-sprites, ten gates); mutant countdown-check-mutant-result (a register, since the 'idle' arm stores nothing) DIVERGENCE at frame 469 (artifacts/gods/verify-countdown-check-mutant) | 3,238 hits, 68 fallbacks (33 scheduler admission, 35 unsupported domain); tree: 801,195 hits, 6,610 fallbacks (5,713 scheduler admission, 344 seam deadline, 553 unsupported domain across 00FE08/010332) | this commit
2026-09-16 | 010A14 collision gate / the 'held' (phase<=7, a global gate word set) and 'gated' (the phase advance's +-4 residue nonzero) arms, witnessed on the two of eight recordings that reach this entry; the 'collision' arm (residue zero, calls 010CBC) declined, and the phase>7 arm ('over', real ROM code) declined as unwitnessed on either recording | RAM-only leaf, no frame at all (a7 unchanged besides the return pop); caller-supplied state struct (A5, a phase word and a moving-state word), D0 the only other input | gods_sega.game.movement.collision_gate; plan gods_sega.boundary.collision_gate_plan; candidates 'collision-gate', 'camera-sprites' (eleven gates) | tests/games/gods/test_movement.py (25: 3 unit + 8 held/gated fixtures MATCH, 12 collision/over fixtures DECLINED, across census-010A14{,-f0ac19738f19}) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-collision-gate-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16k, PASS (camera-sprites, eleven gates); mutant collision-gate-mutant-result DIVERGENCE at frame 469 (artifacts/gods/verify-collision-gate-mutant) | 1,347 hits, 93 fallbacks (13 scheduler admission, 80 unsupported domain); tree: 806,765 hits, 6,983 fallbacks (5,748 scheduler admission, 344 seam deadline, 891 unsupported domain across three declined-call regions) | this commit
2026-09-16 | 00BCCE zone check / a box test around the player's own position (0063FA's GRID_X/GRID_Y), widened on two specific levels, against a caller-supplied position and half-extents; fully witnessed over four recordings -- no declines | RAM-only leaf, an 8-register save/restore frame the routine never writes back through (the whole box arithmetic is scratch); only a shared cooldown word and D2's own final value are durable | gods_sega.game.zones.zone_check; plan gods_sega.boundary.zone_check_plan; candidates 'zone-check', 'camera-sprites' (twelve gates) | tests/games/gods/test_zones.py (75: 6 unit + 69 fixtures MATCH across census-00BCCE-fresh{,-f0ac19738f19,-7251bbd0ecf7,-f40d7bcc9dda}; the stale pre-staged census-00BCCE directory was superseded -- its 13 "calls 0F4472" classes were a VBlank-in-interrupt-handler misattribution) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-zone-check-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16l, PASS (camera-sprites, twelve gates); mutant zone-check-mutant-result (a register: the 'held'/'outside' arms, the great majority, store nothing durable) DIVERGENCE at frame 475 (artifacts/gods/verify-zone-check-mutant) | 4,316 hits, 35 fallbacks (all scheduler admission); tree: 881,371 hits, 7,335 fallbacks (6,100 scheduler admission, 344 seam deadline, 891 unsupported domain across three declined-call regions) | this commit
2026-09-16 | 00126A particle drawer's own emitter / off-screen x, off-screen y, uploaded no-flip, uploaded flip, all witnessed over four recordings; no cache at all, so every on-screen call is a seam (no arm declined) | Aladdin recipe 2/3 (0018C8's own shape) reproduced with this routine's own addresses: no id-to-offset table (the caller passes the descriptor byte offset directly), its own fixed attribute bit (0x4000, not 0018C8's 0x2000); the ceded block's own data-loop length (the descriptor's tile byte count) is never modeled, only charged for real | gods_sega.game.sprites.emit_particle_sprite; plan gods_sega.boundary.particle_emit_plan (+ particle_emit_suffix); candidates 'particle-emit', 'camera-sprites' (thirteen gates) | tests/games/gods/test_particles.py (46: 3 unit + 58 fixtures MATCH across census-00126A-fresh{,-fb408bc75597,-7251bbd0ecf7,-f40d7bcc9dda}) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-particle-emit-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16m, PASS (camera-sprites, thirteen gates); mutant particle-emit-mutant-result DIVERGENCE at frame 469 (artifacts/gods/verify-particle-emit-mutant) | 7,330 hits (3,314 seams entered, 3,026 completed), 335 fallbacks (53 scheduler admission, 282 seam deadline); tree: 955,464 hits, 105,964 seams entered, 101,583 completed, 11,118 fallbacks (6,498 scheduler admission, 3,728 seam deadline, 891 unsupported domain, 1 gate-without-planner foreign-return edge, tree still bit-exact) | this commit
2026-09-16 | 014084 hazard tick / 'paint' (inactive, or an active object whose grid cell isn't 1: a tile-array write clamped to FFBBAA-FFC156) and 'spawn' (grid cell 1: a sound request, a bounded 20-entry FFF90E pool scan and fill, whether or not a slot was free), all witnessed over four recordings; the 'trigger' arm (rare, gated by a parallel-table byte and a counter, calling unrecovered 00F828) declined | RAM-only leaf, no save/restore frame at all (D4/D5/A0/A1/A2 are live scratch); two disjoint bodies behind one early branch, sharing the same definition-style position offset (OBJECT_X/OBJECT_Y) and the level grid 0063FA/00FDB8 already read; each adda.w in the tile-array address sign-extends its own word operand separately (not one combined offset) | gods_sega.game.hazard.hazard_tick; plan gods_sega.boundary.hazard_tick_plan; candidates 'hazard-tick', 'camera-sprites' (fourteen gates) | tests/games/gods/test_hazard.py (127: 7 unit + 113 fixtures MATCH/DECLINED across census-014084-fresh{,-f0ac19738f19,-7251bbd0ecf7,-f40d7bcc9dda}) | fb408bc75597, 34,904 frames, artifacts/gods/verify-hazard-tick-fb408bc75597, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16n, PASS (camera-sprites, fourteen gates); mutant hazard-tick-mutant-result DIVERGENCE at frame 2,628 (artifacts/gods/verify-hazard-tick-mutant) | 26,075 hits, 218 fallbacks (185 scheduler admission, 33 unsupported domain); tree: 1,018,584 hits, 11,755 fallbacks (7,038 scheduler admission, 3,728 seam deadline, 988 unsupported domain, 1 gate-without-planner foreign-return edge, tree still bit-exact) | this commit
2026-09-16 | 010A14 collision gate / the 'collision' arm extended: 010CBC (grid.grid_cell_at, 0063FA's own arithmetic parameterised on D0/D1 -- no direct caller, so no separate gate; used only as this arm's own callee) plus a near/mid/far grid test in the direction the moving-state word selects, witnessed over the two recordings that reach this entry (six census path classes); a further gate byte in a second caller-supplied record (A3) is zero on every occurrence ('collision-held', the tail runs untouched) -- nonzero would enter an unbounded grid search this module does not model ('collision-deep', declined as unwitnessed); the phase>7 arm ('over') stays declined as unwitnessed | RAM-only leaf that calls the already-recovered arithmetic inline (010CBC), the 0049DA-calls-001164 shape without a separate gate since 010CBC has no direct caller of its own | gods_sega.game.grid.grid_cell_at; gods_sega.game.movement.collision_gate extended ('collision-clear'/'collision-held'/'collision-deep' replacing the old declined 'collision' arm); plan gods_sega.boundary.collision_gate_plan extended; candidates 'collision-gate', 'camera-sprites' (unchanged gate count, fourteen gates) | tests/games/gods/test_movement.py (extended unit coverage plus all 20 retained fixtures across census-010A14{,-f0ac19738f19} MATCH, none declined) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-collision-gate2-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16p, PASS (camera-sprites, fourteen gates); mutant collision-gate-mutant-result DIVERGENCE at frame 469 (artifacts/gods/verify-collision-gate2-mutant) | 1,426 hits, 14 fallbacks (all scheduler admission, down from 93 fallbacks incl. 80 unsupported domain); tree: 1,019,144 hits, 11,195 fallbacks (7,040 scheduler admission, 3,728 seam deadline, 426 unsupported domain across two declined-call regions, 1 gate-without-planner foreign-return edge, tree still bit-exact) | this commit
2026-09-16 | 010332 countdown check / the 'trigger' arm extended: the countdown reload and frequency word (both unconditional once triggered), then either the unrecovered 0091BC pool ('trigger-deep', declined) or a direction-mirrored screen window test (01158C/0115D4's own shape, BACK and FORWARD testing the X bound in opposite order) and, inside it, a bounded 20-entry pool scan+fill shaped like hazard.py's own ('trigger-reject': outside the window, no pool touch; 'trigger-spawn': inside it, a free slot filled), witnessed over four recordings (43 fixtures); the pool exhausted ('trigger-pool-full') stays declined as unwitnessed | RAM-only leaf with a transient two-register frame plus a nested call's own stack residue (the bsr's return address and the callee's own move.l a0,-(a7), both durable until the matching pops); the callee never returns control anywhere but back into this activation | gods_sega.game.timers.countdown_check extended ('trigger-deep'/'trigger-reject'/'trigger-spawn'/'trigger-pool-full' replacing the old declined 'trigger' arm); plan gods_sega.boundary.countdown_check_plan extended | tests/games/gods/test_timers.py (extended unit coverage plus 38 of 43 retained fixtures across census-010332{,-7251bbd0ecf7,-f0ac19738f19,-f40d7bcc9dda} MATCH, 5 DECLINED (0091BC calls)) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-countdown-check2-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16p, PASS (camera-sprites, fourteen gates); mutant countdown-check-mutant-result DIVERGENCE at frame 469 (artifacts/gods/verify-countdown-check2-mutant) | 3,273 hits, 33 fallbacks (all scheduler admission, down from 68 fallbacks incl. 35 unsupported domain); tree: 1,019,144 hits, 11,195 fallbacks (7,040 scheduler admission, 3,728 seam deadline, 426 unsupported domain across two declined-call regions, 1 gate-without-planner foreign-return edge, tree still bit-exact) | this commit
2026-09-16 | 00470C trigger conditions / kinds 0-3, 5-12, 15, 16 with every witnessed (kind, arm) pair (membership kinds per compare position); kind 4 (unwitnessed), kinds 13/14 (the score through 00364C), divs by zero or overflow, and unwitnessed compare positions declined | the first Gods dispatcher: one gate, a ROM table indexed by D5 into seventeen small RAM-only predicates, the kinds as arms (Aladdin's family-dispatch shape with the selector in a register) | gods_sega.game.conditions.evaluate; plan gods_sega.boundary.condition_plan; candidates 'conditions', 'camera-sprites' (fifteen gates); census --classifier reg:d5.w | tests/games/gods/test_conditions.py (129 fixtures MATCH across census-00470C{,-fb408bc75597,-f40d7bcc9dda,-7251bbd0ecf7,-f0ac19738f19}) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-conditions-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16p, PASS (camera-sprites); mutant conditions-mutant-outcome (a failing condition reported as passing) DIVERGENCE (artifacts/gods/verify-conditions-mutant); the register mutant was blind (dead residue) and was dropped | 5,247 hits, 36 fallbacks (all scheduler refusals); tree: 1,055,949 hits, 11,521 fallbacks (7,366 scheduler refusals, 3,728 seam deadlines, 426 declined arms of other regions, none at 00470C) | this commit
2026-09-16 | 00364C score conversion / 1, 2 and 3 division-digit values (0-999) witnessed over four recordings, both from the score-update call sites 0035B2/003604; a fourth division (value >= 1000) and a negative value are declined as unwitnessed; kinds 13/14 of 00470C still cannot be admitted -- no recording ever calls the trigger conditions with kind 13 or 14 (checked against all five condition census directories: no 000D/000E fixture exists), so the (kind, arm) pair stays unwitnessed independent of this routine's own recovery | RAM-only leaf: packs a value into four caller-supplied words as BCD digit pairs on top of whatever they already hold (each digit a 68000 abcd extended-add, threading the caller's own X flag in as the first digit's carry-in and BCD-carrying out across words); an internal bsr/rts chain (four entry points into one shared tail) stays inside the region -- its own return-address push is a real, transient RAM write the plan must model, and only the last digit's push survives in final RAM | gods_sega.game.score.convert_score; plan gods_sega.boundary.score_convert_plan; candidate 'score-convert', 'camera-sprites' (sixteen gates) | tests/games/gods/test_score.py (6 unit + 12 fixtures MATCH across census-00364C{,-f0ac19738f19,-7251bbd0ecf7,-f40d7bcc9dda}) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-score-convert-f0ac1973, PASS; segment_verify at boundary-6000/12000 PASS, 0 fallbacks; mutant score-convert-mutant-result DIVERGENCE at frame 675 (artifacts/gods/verify-score-convert-mutant) | 56 hits, 0 fallbacks | this commit
2026-09-16 | 00BA8E / 010CD2 (re-screened) | escalation: NEW_GODS_SUBSYSTEM | - | - | - | - | blocker docs/gods/blockers/2026-09-16-00BA8E.md; confirmed by fresh disassembly and a census of 00BA8E (32 real path classes, fb408bc75597): 25/32 classes are a clean, bounded, RAM-only "no neighbour found" arm (admissible in principle, not carried through this session); 7/32 call 013264, which jumps through a ROM table at 12D04 indexed by an object-type value -- a genuine per-type dispatch; 010CD2 calls the unrecovered 0091BC on one path and branches on a literal state value at 010D7C on the others.  Confirms the 15 Sep note with hard evidence; not attempted further | escalate commit
2026-09-16 | 00F828/00F86A proximity table search-and-add / the 'not-found' search (all 40 entries examined, three per-position shapes: miss, close (key's first word matches, second does not), stale (both match, timer not negative)) followed by the free-slot add, witnessed over two recordings (fb408bc75597, 7251bbd0ecf7; not exercised on f0ac19738f19 or f40d7bcc9dda); the 'trigger' arm (a matching, still-negative-timer entry, falling into 00F8A2's own caller-record dispatch and a double stack return past both frames) and 'pool-full' (all 40 occupied) stay declined -- the first real ROM code, unwitnessed | 00F828 owns its own internal call into 00F86A (the shape 0049DA's calls into 001164 proved); RAM-only, no separate gate for 00F86A; a redundant key recompute in the add step matches the ROM's own | gods_sega.game.hazard.proximity_search/proximity_add; plan gods_sega.boundary.proximity_plan; candidate 'proximity', 'camera-sprites' (seventeen gates) | tests/games/gods/test_proximity.py (45: 4 unit, 39 fixtures MATCH/DECLINED across census-00F828{,-7251bbd0ecf7}, candidate names, reference) | fb408bc75597, 34,904 frames, artifacts/gods/verify-proximity-fb408bc75597, PASS; mutant proximity-mutant-result DIVERGENCE at frame 12,890 (artifacts/gods/verify-proximity-mutant) | 8 hits, 25 fallbacks (all unsupported domain: trigger arm) | this commit
2026-09-16 | 013264 pickup award / item codes 1-27 (three groups, with and without the time bonus, the cue and the consumed slot), specials -1, -2 (sound off and on), -3; codes -4 and below (the continuation into 013316) and code 0 declined | RAM-only leaf over the pickup grid byte before A0 and the item record selected through the ROM table 012D04 (records, not handlers: the blocker of the same day corrected) | gods_sega.game.pickups.collect; plan gods_sega.boundary.pickup_award_plan; candidates 'pickups', 'camera-sprites' (nineteen gates) | tests/games/gods/test_pickups.py (36 fixtures across census-013264-{fb408bc75597,f40d7bcc9dda,7251bbd0ecf7,f0ac19738f19}: 25 MATCH, 11 declined -4) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-pickups-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16t, PASS (camera-sprites); mutant pickups-mutant-result DIVERGENCE (artifacts/gods/verify-pickups-mutant) | 154 hits, 2 fallbacks (1 scheduler refusal, 1 declined -4); tree: 1,033,633 hits, 11,631 fallbacks (7,199 scheduler refusals, 3,728 seam deadlines, 12 declined -4 pickups, the rest other regions declined arms) | this commit
2026-09-16 | 014A3C next-random draw / a single path class (no branch), 3,971 calls over 34,904 frames from many sites, only two of them 00BA8E's own found-effect composition | RAM-only leaf, fixed cost (no branch) | gods_sega.game.effects.next_random; plan gods_sega.boundary.next_random_plan; candidates 'next-random', 'camera-sprites' (twenty gates) | tests/games/gods/test_pickup_check.py (2 fixtures MATCH) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-next-random-f0ac1973, PASS; mutant next-random-mutant-result (a register: the drawn word, not the stored cursor -- the generic "flip the last write" mutation corrupts the cursor itself, which can wrap onto an odd address and fault the 68000 on its own next read, a crash rather than a clean divergence) DIVERGENCE at frame 1,341 (artifacts/gods/verify-next-random-mutant2) | 727 hits, 5 fallbacks (all scheduler admission) | this commit
2026-09-16 | 00932C effect pool add / the first free slot of a 20-entry pool (0-19 skips before the found test), or the pool-full arm (all 20 occupied, itself witnessed, 46/452 occurrences on its own census) | RAM-only leaf, cost bounded by the pool's own 20 slots (the "unrolled test count" shape 0018C8's cache scan and 00F828's table search share); the general form hazard.py's own '_spawn' inlines with d2=d3=0 | gods_sega.game.hazard.effect_pool_add; plan gods_sega.boundary.effect_pool_add_plan; candidates 'effect-pool-add', 'camera-sprites' (twenty-one gates) | tests/games/gods/test_pickup_check.py (21 fixtures MATCH) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-effect-pool-add-f0ac1973, PASS; mutant effect-pool-add-mutant-result DIVERGENCE at frame 1,341 (artifacts/gods/verify-effect-pool-add-mutant) | 89 hits, 0 fallbacks | this commit
2026-09-16 | 00BA8E pickup check / the clean arm (a two-axis box clamp bounding the pickup grid scan -- near/far/none sub-arms per axis, up to four ways to bail, the array-append side effect, common on some recordings); the found arm's own two compositions, found-sound (the box's own residue at or below the award: an immediate sound cue) and found-effect (residue above a nonzero award: two random jitters through the already-recovered next_random, one add into the already-recovered effect_pool_add); found-special (the -1 pickup's own further call), found-message (the digit-split tail), the award-zero bare exit, either jitter draw's own default-mask branch, the second draw's own negative branch and the pool-full arm within this composition declined as unwitnessed | a composition over the already-recovered zone check (00BCCE), pickup award (013264), next-random (014A3C) and effect-pool-add (00932C), each owned as a call (the shape 00462C's evaluator and 0049DA's spawn queue proved) | gods_sega.game.pickups.pickup_check; plan gods_sega.boundary.pickup_check_plan; candidates 'pickup-check', 'camera-sprites' (all twenty-two gates) | tests/games/gods/test_pickup_check.py (160: 5 unit + 155 fixtures MATCH/DECLINED across census-00BA8E-fresh{,-f0ac19738f19,-7251bbd0ecf7,-f40d7bcc9dda}, four recordings) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-pickup-check-f0ac1973b, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16v, PASS (camera-sprites, all twenty-two gates); mutant pickup-check-mutant-result DIVERGENCE at frame 469 (artifacts/gods/verify-pickup-check-mutant) | 4,260 hits, 91 fallbacks (34 scheduler admission, 12 declined -4 continuations across both gates, 15 award-zero, 41 second-jitter-negative); tree: 1,041,739 hits, 13,151 fallbacks (7,939 scheduler admission, 3,728 seam deadline, 1,483 declined arms, 1 gate-without-planner foreign-return edge, tree bit-exact) | this commit; a real defect caught before landing: composing 013264's own call re-applied its raw stores after the composition's own already-correctly-ordered write set, letting the award's own sound cue silently outlive this routine's own overwrite on the found-sound arm -- invisible to every 68000-side fact (cycles, registers, RAM) and to every per-region history-verify, since it only desyncs the Z80 sound co-processor by a few instructions; caught by the eight-recording tree (fb408bc75597 frame 6,840), fixed by not re-applying it, confirmed by re-running the tree
2026-09-16 | 013316 grid inverse and debris burst / the sound-off arm (the only one witnessed): a bounded 80-slot pool scan (the same shared-counter-across-particles shape, up to 8 particles per call, each a random impact cue through the already-recovered next-random and a ROM table offset word); a real precondition fixed the collect() semantics too -- codes -4 and below award BIG_VALUE (10000) unconditionally before falling into this routine, not a silent decline as first modelled; the sound-on arm (a fixed award, no burst), the rate-limit's own abort and arm-itself sub-arms and a pool-exhausted arm decline as unwitnessed | 013264's own code -4-and-below cascade falls straight through with no separate call, so 013316 has no gate of its own -- the composition owns it inside `pickup_award_plan` itself (PICKUP_AWARD_ENTRY), the shape 00BA8E owns its own callees | gods_sega.game.pickups.grid_inverse_award (+ grid_inverse_position); plan gods_sega.boundary._grid_inverse_award_plan, called from pickup_award_plan; candidates 'pickups', 'camera-sprites' | tests/games/gods/test_pickup_check.py (4 unit) + tests/games/gods/test_pickups.py (all eleven code -4 fixtures across census-013264-{7251bbd0ecf7,f0ac19738f19,f40d7bcc9dda,fb408bc75597} now admitted, not declined) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-pickups-f0ac1973b, PASS (155 hits, up from 154 -- the one code -4 occurrence on this history is now admitted); tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16w, PASS (camera-sprites, tree bit-exact); mutant pickups-mutant-result DIVERGENCE at frame 601 (artifacts/gods/verify-pickups-mutant2) | 155 hits, 1 fallback (scheduler admission); tree: 1,041,676 hits, 13,142 fallbacks (declined arms incl. 12 pickup-check-side -4 declines that still fall to the original's own 013264 gate, now admitted there) | this commit
2026-09-16 | 010CD2 pickup probe / a single path (no branch on the composition's own instructions): push the caller-supplied record's (d0,d1,d2), add the camera to (d0,d1), call the already-recovered pickup check 00BA8E with the record's own +8 word as d2, write the result back to the record, and on a negative result store a -1 sentinel to the record's +4 word; fully witnessed on all 412 calls across the four fixture recordings, no arm declined | a caller-supplied record's own camera-relative call into the already-recovered pickup check, composed by calling pickup_check_plan itself with a synthetic register file for the point 00BA8E is entered (one level deeper than the 0049DA-calls-001164 shape); the original 15-Sept blocker's reading that 010CD2 also called the unrecovered 0091BC and branched on 010D7C was a misattribution -- a fresh census found those belong to a separate adjacent routine (010CF8 onward) that 010CD2 never reaches | gods_sega.game.pickups.pickup_probe; plan gods_sega.boundary.pickup_probe_plan; candidates 'pickup-probe', 'camera-sprites' (twenty-three gates) | tests/games/gods/test_pickup_check.py (fixture-parametrized across all 32 retained fixtures, MATCH; unit test) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-pickup-probe-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16x, PASS (camera-sprites, all twenty-three gates, tree bit-exact); mutant pickup-probe-mutant-result DIVERGENCE at frame 469 (artifacts/gods/verify-pickup-probe-mutant3 -- the generic result-byte and a register mutant both proved unusable first: the byte mutant crashes the 68000 by corrupting a creature record an unrecovered coroutine reads later, the register mutant is blind since d0 is scratch on return, so this mutant drops the composition's own writes entirely instead) | 3,248 hits, 58 fallbacks (23 scheduler admission, 35 unsupported domain: pickup check found-jitter-y-negative arm not witnessed); tree: 1,041,708 hits, 13,293 fallbacks, tree bit-exact | this commit
2026-09-16 | 00FFF0 line walker resume (object copy) / all four bodies (toward ±x, shallow/steep), the yield on the budget and the completion on the counter, with and without the minor-axis wrap; a record whose continuation is not a body, and a zero budget, declined | the first stateful subsystem: a persistent 18-byte walker record (phase, position, sign, spans, error, counter) stepped by a budget and re-armed; game state, not a recovery continuation | gods_sega.game.walker (start/run/load/stores, two copies); plan gods_sega.boundary.walker_resume_plan; candidates 'walker', 'camera-sprites' (twenty-four gates) | tests/games/gods/test_walker.py (7, the semantics), test_walker_resume.py (35 fixtures MATCH across census-00FFF0{,-f40d7bcc9dda,-7251bbd0ecf7,-f0ac19738f19}; consecutive invocations over 300 frames) | fb408bc75597, 34,904 frames, artifacts/gods/verify-walker-fb408bc7-b, PASS; tree of eight recordings, artifacts/gods/verify-camera-sprites-tree-walker-b, PASS (camera-sprites); mutant walker-mutant-result (the re-armed x one off) DIVERGENCE at frame 2,260 (artifacts/gods/verify-walker-mutant); a counter-off mutant drove the original into a cartridge write, a fault not a divergence, and was replaced | 169 hits, 1 fallback (a z80 bank guard refusal) of 170 calls; tree: 1,047,974 hits, 6,906 fallbacks (the 321 `moving` declines of 00FE08 remain until its arm owns the call) | this commit
2026-09-17 | 00FE08 animation step / the 'moving' arm's own call into the already-recovered walker resume 00FFF0 (game/walker.py), owned the way 0049DA owns 001164: 'moving-continue' (the walk has not finished as of this call) and 'moving-complete' (it has, more than one waypoint left -- the next waypoint loaded from the definition's own table, the slot byte reset to -1), both witnessed on every recording that reaches the arm; 'moving-coldstart' (the walk finishes with at most one waypoint left, falling into a per-object-type waypoint dispatch 00FEC0/00FF54) censused fresh over all eight recordings and unwitnessed on every one: declined, along with a zero budget and a record whose continuation is not one of the walker's own bodies | a leaf composing a call into an already-recovered routine (00FFF0's own cost fragments, _WR_HEAD/_WR_STEP/_WR_TAIL, reused directly -- 00FFF0 *is* this arm's bsr target, not a twin), plus its own head/tail cost fragments and register residue (the frame restore undoes d0-d2/a1/a2, so only d3/d4/d5/d6/d7/a0/a3/a5 differ from entry; d3's own X residue on 'moving-complete' comes from the waypoint-index SUBQ/ADD chain, not the walker's) | gods_sega.game.animation.animation_step extended; plan gods_sega.boundary.animation_step_plan extended (_animation_idle_plan/_animation_moving_plan); candidates 'animation-step', 'camera-sprites' (twenty-four gates, unchanged count) | tests/games/gods/test_animation.py (57: unit coverage for all four 'moving-*' arms plus idle, and all 43 retained fixtures across census-00FE08{,-7251bbd0ecf7,-f40d7bcc9dda,-f0ac19738f19} MATCH, none declined) | fb408bc75597, 34,904 frames, artifacts/gods/verify-animation-step2-fb408bc75597, PASS (30,043 hits, up from 11,542 -- every occurrence on this history now admits: 39 fallbacks, all exact adapter refusals, 0 unsupported domain); segment_verify at boundary-6000/12000 PASS, 300 hits each, 0 fallbacks; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-17a, PASS (camera-sprites, tree bit-exact); mutant animation-step-mutant-result DIVERGENCE at frame 2,260 (artifacts/gods/verify-animation-step2-mutant -- the first frame the tree ever entered the 'moving' arm) | fb408bc7: 30,043 hits, 39 fallbacks (2 machine admission, 5 observation deadline, 6 vblank in span, 26 z80 bank guard); tree: 00FE08's own fallbacks 321 -> 118, all now exact adapter refusals (0 unsupported domain at this gate); 0091BC still fires 8 times on the tree through 010332's own declined 'trigger-deep' arm (unrecovered) | this commit
2026-09-17 | 00FEC0/00FF54 (the animation step's per-object-type waypoint dispatch) | census only, no recovery | - | - | census-00FEC0[-<node>] and census-00FF54[-<node>] over all eight recordings: `no entry fired on this history` on every one -- the 'moving-coldstart' arm (a walk completing with at most one waypoint left) is real ROM code no recording enters; stays declined per grinder-protocol | - | this commit
2026-09-17 | 0091BC projectile launch / the 20-entry pool scan (0-3 skips witnessed) then the cold start into the walker's own projectile copy (game/walker.py: start/run), all four quadrants and both y-sign cases witnessed across four recordings; the pool exhausted (0091DE) declined, unwitnessed | no save/restore frame at all (D0-D7/A3 live scratch, the hazard tick's own shape); the projectile cold start's own quadrant-dependent setup cost reused with WALKER_RESUME_ENTRY's own per-step and re-arm-tail cost fragments (_WR_STEP/_WR_TAIL) -- the loop bodies are cost-identical between the resume and the cold start, confirmed instruction-for-instruction | gods_sega.game.projectiles.launch (calls game.walker.start/run/stores); plan gods_sega.boundary.launch_plan; candidates 'projectile-launch', 'camera-sprites' (twenty-five gates) | tests/games/gods/test_projectiles.py (44: 2 unit + 40 fixtures MATCH across census-0091BC{,-4492103be245,-7251bbd0ecf7,-f40d7bcc9dda} on four recordings, one segment-tier PASS) | fb408bc75597, 34,904 frames, artifacts/gods/verify-projectile-launch-fb408bc75597, PASS, 50 hits (all 50 witnessed occurrences), 0 fallbacks; mutant projectile-launch-mutant-result DIVERGENCE at frame 29,278 (artifacts/gods/verify-projectile-launch-mutant), the tree's first entry; tree of eight recordings, artifacts/gods/verify-camera-sprites-tree-2026-09-17b, PASS (camera-sprites, twenty-five gates, tree bit-exact) | 50 hits, 0 fallbacks | this commit; the caller(s) are now identified from the retained fixtures' own return addresses and a fresh disassembly: on `f40d7bcc9dda…` (0103C0/010D38) it *is* `010332`'s own `'trigger-deep'` tail, one instruction past the current `COUNTDOWN_CHECK_LAST_PC` (`0103AA`-`0103C8`: the record's own position +0x10 in x, a budget from the rate byte `(rate>>1)+2`, a full 15-register frame, `d6=1` fixed, `jsr 0091BC`, full restore) -- not yet composed into `countdown_check_plan` (a real next bite, not a blocker: every register is restored afterward, so the composition is simpler than `animation-step`'s); on `fb408bc75597…`/`4492103be245…`/`7251bbd0ecf7…` (`009D16`) it is a second, still undisassembled call site -- `009D12`-ish is not `010332`'s own code and remains open
2026-09-17 | 0093D2 line walker resume, projectile copy / a byte-for-byte duplicate of the already-recovered 00FFF0's own body at a second ROM address, over the projectile pool's own records; all four bodies, the yield on the budget (the projectile copy never completes via a counter -- only the object copy's dbra does), with and without the minor-axis wrap; a record whose continuation is not a body, and a zero budget, declined | the same cost fragments WALKER_RESUME_ENTRY's own plan uses (_WR_HEAD/_WR_STEP/_WR_TAIL), confirmed instruction-for-instruction identical between the two ROM copies; only the re-arm table and the RTS addresses differ | gods_sega.game.walker (unchanged, already generic over both copies); plan gods_sega.boundary.walker_resume_projectile_plan (a close twin of walker_resume_plan); candidates 'projectile-resume', 'camera-sprites' (twenty-six gates) | tests/games/gods/test_projectile_resume.py (147 fixtures MATCH across census-0093D2-{fb408bc75597,4492103be245,7251bbd0ecf7,f40d7bcc9dda}, four recordings; one segment-tier PASS) | fb408bc75597, 34,904 frames, artifacts/gods/verify-projectile-resume-fb408bc75597, PASS, 628 hits of 630 calls (2 exact adapter refusals: 1 observation deadline, 1 vblank in span); mutant projectile-resume-mutant-result DIVERGENCE at frame 12,971 (artifacts/gods/verify-projectile-resume-mutant), the frame after the tree's first entry (12,970); tree of eight recordings, artifacts/gods/verify-camera-sprites-tree-2026-09-17c, PASS (camera-sprites, twenty-six gates, tree bit-exact) | 628 hits, 2 fallbacks | this commit; the driver 009210 (calling this resume once per pooled projectile per tick) is censused fresh over the four projectile-firing recordings (artifacts/gods/evidence/census-009210-<node>, 32 real path classes on three of them) but still read for context only, per grinder-protocol ("do not recover the whole driver unless a parent composition requires it")
2026-09-17 | 00FFF0 line walker resume (object copy) / review-gate VBlank-slide check (`scripts/gods/vblank_slide.py`, --ticks 20) on a retained gameplay-tick fixture (census-00FFF0/00FFF0-entry-p0.state) | verdict: DIFFERS at every one of three placements tried (burns of 57,244/57,284/57,324 cycles; two further burns refused by the engine as reaching the VBlank itself) -- but the only differences are the global VBlank counter (130 vs 131 at the compared tick boundaries) and the 60 Hz elapsed/countdown/pad tuple's own elapsed field (96 vs 97, the same counter surfaced a second way); every live work-RAM byte, every register and the sound block are equal at the region's own exit, and both future tick observations agree once the counter offset is accounted for. This is the tool's own documented artifact of a large burn (order of a frame) shifting *which* tick boundary the interrupt lands inside, not a byte the handler and the region share: no live game byte is at stake, so the finding does not reopen `00FFF0`'s own admission (proven through the full ladder, tree bit-exact) -- it is written up, not treated as a blocker, per grinder-protocol | - | - | - | - | this commit; a smaller-burn or different-fixture re-run to isolate whether any *non-counter* byte ever differs is the next question if the walker's own admission is ever challenged again
2026-09-17 | 010332 countdown check / the 'trigger' arm's own deep gate composed at last: the record's own position (biased +0x10 in X only, unlike BACK/FORWARD's -8/+0x20) and a rate-derived budget ((rate>>1)+2, an arithmetic shift) go straight into the already-recovered projectile launch (game/projectiles.py) inside two nested save/restore frames (the shared outer d0-d1 push every trigger arm makes, then a full d0-d7/a0-a5 frame of its own) that leave every register but D3 (the reload) and D4 (the budget) at its entry value; 'trigger-deep-launch' replaces the old declined 'trigger-deep', witnessed at last -- a fresh full-history census of 010332 on f40d7bcc9dda (the original census's window had missed it) retained 5 fixtures (8 real occurrences); 'trigger-deep-pool-full' (the projectile pool exhausted) stays declined, unwitnessed | a leaf composing a call into an already-recovered routine (0091BC's own cost fragments -- and, through it, WALKER_RESUME_ENTRY's own -- reused directly, the shape 00FE08 owns its own call into 00FFF0 proved one level deeper) | gods_sega.game.timers.countdown_check extended; plan gods_sega.boundary.countdown_check_plan extended (_countdown_check_deep_plan); candidates 'countdown-check', 'camera-sprites' (unchanged gate count, twenty-six gates -- no new gate, an existing one's own domain widened) | tests/games/gods/test_timers.py (76: extended unit coverage for 'trigger-deep-launch'/'trigger-deep-pool-full' plus all 64 retained fixtures across census-010332{,-7251bbd0ecf7,-f0ac19738f19,-f40d7bcc9dda,-fresh-f40d7bcc9dda} MATCH/DECLINED as appropriate) | f40d7bcc9ddae7a9377d43712a008c0355f3289274545f0662a308273d6f70d1, 17,620 frames, artifacts/gods/verify-countdown-check3-f40d7bcc9dda, PASS, 3,277 hits, 33 fallbacks (all z80 bank guard, 0 unsupported domain); segment_verify at boundary-6000/12000 PASS (228 hits/4 fallbacks, 116 hits/1 fallback, all z80 bank guard); mutant countdown-check-mutant-result DIVERGENCE at frame 2,696 (artifacts/gods/verify-countdown-check3-mutant); tree of eight recordings, artifacts/gods/verify-camera-sprites-tree-2026-09-17d, PASS (camera-sprites, twenty-six gates, tree bit-exact, the `countdown check trigger arm calls unrecovered 0091BC` fallback reason gone from the tree) | 3,277 hits, 33 fallbacks | this commit; the second 0091BC caller (009D16 on fb408bc75597/4492103be245/7251bbd0ecf7, not 010332's own code) is still unidentified -- the one remaining open question in docs/gods/blockers/2026-09-16-0091BC.md
2026-09-17 | 00BA8E pickup check / every found-arm the eight-recording tree actually witnesses is now admitted: 'found-bare' (box residue positive, award zero -- a bare restore, RESULT_WORD still stored); the special-1 pickup's own extra timer decrement (SPECIAL_TIMER -= the box's own D2 residue) folded into the SAME found-sound/bare/effect tail every other collect() result reaches, declining only when the decrement goes negative ('found-special-timer', jsr 011540, unrecovered); found-effect's own two jitter draws corrected on both axes -- the "default mask" branch (the halved box size's own AND collapsing to zero) reloads a fixed 0xF instead of declining, and the second (Y) draw's own negative branch mirrors the first (X) draw's already-modelled subtract-instead-of-add, neither a decline; the effect pool's own 'full' arm (00932C, already proven through its own direct gate) composed into this activation too, X threaded from the second draw's own add/sub since the pool's own addq never runs when full; and a grid code of -4 or below (013264's own cascade to BIG_VALUE) composed straight into 013316's already-recovered debris burst (game.pickups.grid_inverse_award) the way the direct 013264 gate already owns it, one activation of the same jsr -- the one prior semantic bug this pass also fixed in passing: box residue of exactly zero had been folded into 'found-bare' by a same-day misreading of the branch fall-through, when the ROM's own `bmi`-then-`bne` sequence actually falls to the SAME sound-cue code as a negative residue (`<=0` was already correct; only the arm's own true boundary needed re-deriving to admit 'found-bare' at all). The only three sub-arms that stay declined are genuinely unwitnessed by all eight recordings even at `--max-classes 400` (up from the default 32, which was silently discarding real classes past the cap): the special-1 timer's own negative branch, the digit-split message tail, and 013316's own sound-on/pool-exhausted arms | a composition, not a new shape: every admitted piece reuses an already-recovered routine (013264, 013316, 014A3C, 00932C) or a bounded default already proven elsewhere in this same routine (the mask default mirrors the mask itself, the Y draw mirrors the X draw) | gods_sega.game.pickups.pickup_check extended (SPECIAL_TIMER, the -4-and-below inline call into grid_inverse_award, both jitter axes' default mask and negative branch, py_base/jitter_y exposed for the boundary's own X derivation); plan gods_sega.boundary.pickup_check_plan extended (_grid_inverse_debris_cost, _pickup_award_cost's own 'unrecovered' cost, the debris frame's own transient stack writes at 013264's own entry sp); candidates 'pickup-check', 'pickup-probe', 'camera-sprites' (unchanged gate count) | tests/games/gods/test_pickup_check.py (all 665 retained fixtures across census-00BA8E-fresh{,-4492103be245,-7251bbd0ecf7,-ca2b703b6fd5,-cfd02803e937,-da660c605b4a,-f0ac19738f19,-f40d7bcc9dda,-fb408bc75597-wide} MATCH, none declined -- the last of the eight recordings censused fresh this session, plus a `--max-classes 400` re-census of fb408bc75597 that surfaced the -4 and pool-full fixtures the default cap had discarded); test_pickups.py unchanged (013264's own gate already proved 013316) | f0ac19738f190b5a1efa7ac5cd2677f6e0ce582df16483525fad1f95f9bc7068, 15,148 frames, artifacts/gods/verify-pickup-check2-f0ac1973, PASS, 4,320 hits, 31 fallbacks (all z80 bank guard, 0 unsupported domain, up from 4,260 hits/91 fallbacks); segment_verify at boundary-6000/12000 PASS (231 hits/1 fallback, 132 hits/3 fallbacks, all z80 bank guard); mutant pickup-check-mutant-result DIVERGENCE at frame 469 (artifacts/gods/verify-pickup-check2-mutant); pickup-probe (010CD2, which composes this same plan one level deeper) re-verified too: PASS, 3,283 hits, 23 fallbacks (up from 3,248/58), artifacts/gods/verify-pickup-probe2-f0ac1973; tree of eight recordings, artifacts/gods/verify-camera-sprites-tree-2026-09-17e, PASS (camera-sprites, twenty-three gates, tree bit-exact, 1,046,640 hits, 5,845 fallbacks, down from 13,151 -- the pickup-check declines are now only 145 found-message + 1 found-special-timer) | 4,320 hits, 31 fallbacks | this commit
2026-09-17 | 00F828/00F86A proximity table search-and-add / the found-entry continuation at 00F8A2 recovered at last (game.hazard.proximity_trigger): a caller-record decrement of the SAME timer word the search tested for negativity, gated by which of conditions.py's own TRACKED ids TRIGGER_SELECTOR (FFFFF1FC) currently selects -- all three cmpi/bne checks execute regardless of which one matches (three independent redundant tests, not a mutually-exclusive dispatch), so the cost is the same whichever of 0/1/2 is selected; a decrement past TRIGGER_FLOOR (-200) clears the timer to 0 instead of wrapping negative; a selector outside 0/1/2 (leaves the ROM's own d5 uninitialised) stays declined, unwitnessed. Re-censused 00F828 over all eight recordings (four never censused before) with --max-classes 400 (the default 32-class cap was silently discarding real classes, as found the same session for 00BA8E) and reached 25 real 'trigger' fixtures | proximity_plan refactored: its own body extracted into _proximity_resolve(read, sp, a1, a2) (everything 00F828 does relative to its own entry sp, reused by hazard_tick_plan's own composition below); the double return (docs/gods/STATUS.md) nets the SAME +4 stack effect as any nested call once traced through both the internal bsr and the addq -- no special-casing needed at either caller | gods_sega.game.hazard.proximity_trigger; plan gods_sega.boundary._proximity_resolve extended into proximity_plan | tests/games/gods/test_proximity.py (39 retained fixtures across census-00F828{,-7251bbd0ecf7} MATCH, none declined; two new unit tests for proximity_trigger's own bonus/floor-clamp/selector-decline) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-proximity2-fb408bc75597, PASS, 33 hits, 0 fallbacks (up from 8 hits/25 fallbacks); mutant proximity-mutant-result DIVERGENCE at frame 12,890 (artifacts/gods/verify-proximity2-mutant) | 33 hits, 0 fallbacks | this commit
2026-09-17 | 014084 hazard tick / the 'trigger' gate composed over the now-fully-recovered proximity table: 0140BC-0140CA's own type-match-and-counter-check gates a jsr into 00F828 (game.hazard.proximity_search/proximity_add/proximity_trigger) that lands back at 0140D2 either way -- the SAME shared pool fill 'spawn' already owns runs next regardless of which of 00F828's own outcomes ('added' or 'trigger') the call reaches, so the composition folds into the existing 'spawn' arm (a new 'proximity' sub-result) rather than a separate arm; only a type match with the counter still under 2 (real ROM code, unwitnessed by any of the eight recordings) and 00F828's own remaining declines (a selector outside 0/1/2, the table itself full) still decline as 'trigger' | a leaf composing a call into an already-recovered routine (00F828's own _proximity_resolve, reused directly via the same shared helper proximity_plan now calls) whose own exit registers (A0, D5's own upper half, D6) survive into this activation's own exit since the shared pool-fill tail never touches them again except where it already resets D4/D5 fresh | gods_sega.game.hazard._spawn extended (now takes a1, a proximity sub-result key); plan gods_sega.boundary.hazard_tick_plan extended (the 00F828 call's own cost via _proximity_resolve, its own transient jsr-return-address write, A0/D6/D5-upper survival into the shared exit) | tests/games/gods/test_hazard.py (all 147 retained fixtures across census-014084-fresh{,-7251bbd0ecf7,-f0ac19738f19,-f40d7bcc9dda} MATCH, none declined; the old declined-trigger unit test replaced by a gate-unwitnessed case and a composed-add case) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-hazard-tick2-fb408bc75597, PASS, 26,118 hits, 175 fallbacks (all z80 bank guard, 0 unsupported domain, up from 26,075 hits/218 fallbacks incl. 33 unsupported domain); segment_verify at boundary-6000/12000 PASS (57 hits/0 fallbacks, 195 hits/0 fallbacks); mutant hazard-tick-mutant-result DIVERGENCE at frame 2,628 (artifacts/gods/verify-hazard-tick2-mutant); tree of eight recordings, artifacts/gods/verify-camera-sprites-tree-2026-09-17f, PASS (camera-sprites, twenty-six gates, tree bit-exact, 1,046,716 hits, 5,672 fallbacks, down from 5,845 -- the `hazard tick trigger arm calls unrecovered 00F828` and `proximity trigger arm (00F8A2) is not recovered` fallback reasons both gone from the tree; only the trigger evaluator's firing/disabled arms and pickup check's found-message/found-special-timer remain) | 26,118 hits, 175 fallbacks | this commit
2026-09-17 | 007986 message display gate + 0079DC string copy / part 1 of the trigger firing subsystem blocker (docs/gods/blockers/2026-09-16-00462C-firing.md), recovered as two shared candidates (both have real callers outside the trigger evaluator's own firing arm, 0046AA): 0079DC is the plain byte copy the Split predicted, bounded by the string's own length (the NUL is read but not written -- the caller's own clr.b (a1)+ supplies it), matching all 39 retained fixtures across 19 distinct lengths (the "19 kinds" the 15 September census misread). 007986 is a real priority gate over a shared display buffer, not a pass-through: an occupied buffer with a sufficient stored priority still succeeds, through MESSAGE_BUFFER_ALT -- a second real arm the first two-recording census never saw at all; re-censused over all eight recordings with --max-classes 100 (the default 32-class cap already shown this session, on 00BA8E and 00F828, to silently discard real classes) turned up the one occurrence that exercises it. An occupied buffer with insufficient priority ('blocked') and one with a stored bound of exactly 0 (skips the compare outright, 'unrecovered') stay declined, unwitnessed by any of the eight recordings | two ordinary leaves, neither a new shape: 0079DC is the "unrolled test count" shape bounded by traced data rather than a fixed table (0018C8's cache scan, 00F828's own search); 007986 is a caller-supplied-priority gate with two success paths and one real failure path | gods_sega.game.messages.message_gate/copy_message (new module: not trigger-specific, 007986 has unrelated callers at 0x5BC6/0x86B6); plan gods_sega.boundary.message_gate_plan/string_copy_plan; candidates 'message-gate', 'string-copy', 'camera-sprites' (extended to twenty-eight gates) | tests/games/gods/test_messages.py (new: 6 unit tests plus 20 retained 007986 fixtures across census-007986-{,4492103be245,7251bbd0ecf7,ca2b703b6fd5,cfd02803e937,da660c605b4a,f0ac19738f19,f40d7bcc9dda} and census-triggerfire MATCH/DECLINED, and 39 retained 0079DC fixtures across census-triggerfire MATCH, none declined) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames: message-gate PASS 31 hits/0 fallbacks (artifacts/gods/verify-message-gate-fb408bc75597), string-copy PASS 69 hits/9 fallbacks all z80 bank guard (artifacts/gods/verify-string-copy-fb408bc75597); segment_verify at boundary-6000/12000 PASS for both (0/0, 1/0 and 0/0, 2/0 hits/fallbacks); both mutants DIVERGENCE at frame 2,328 (artifacts/gods/verify-message-gate-mutant, verify-string-copy-mutant); tree of eight recordings, artifacts/gods/verify-camera-sprites-tree-2026-09-17g, PASS (camera-sprites, twenty-eight gates, tree bit-exact, 1,046,975 hits, 5,681 fallbacks) | 31+69 hits, 0+9 fallbacks | this commit; a real defect caught before landing: string-copy's own new cost constants accidentally reused the "_SC_" prefix already bound to score_convert_plan's own module-level constants (_SC_TAIL in particular), silently overwriting them and breaking every 00364C test until renamed to "_STRCPY_" -- a reminder that this file's per-region cost constants share one flat namespace
2026-09-17 | 00BA8E pickup check / the found-message arm recovered at last, and it is not a message at all: fresh disassembly of 00BBEA-00BC1C (AWARD_SCALE_LEVEL, formerly misnamed MESSAGE_FLAG) shows straight-line RAM/register arithmetic -- no bsr/jsr anywhere in it -- that derives an ALTERNATE (box_result, d3) pair (an award right-shift by `3-level`, or a halving correction when the shift removes no bits) and rejoins the SAME found-sound/found-bare/found-effect tail every other collect() result already reaches, at its own entry points (00BC12/00BC18 instead of 00BC1E). A gate scan at the tst.w instruction itself (not the 32/400-class census, whose path-signature budget the box/scan arithmetic's own diversity always exhausted first) found the level is 1 on every one of 104 real occurrences across all eight recordings, all on 7251bbd0ecf7 -- 'found-sound' (8), 'found-bare' (4) and 'found-effect' (84), including the halving-correction sub-branch (4); any other level stays declined (`WITNESSED_SCALE_LEVELS`, unwitnessed). A real defect caught before landing: the scaled path's own two transient stack pushes (00BBF0/00BBFE) share their two stack slots with 013264's OWN call frame (its `move.l d2,-(a7)` before the jsr), so the boundary's write-ordering had to place the scaled pushes AFTER that frame's own writes in `order`, not before, for "last write in execution order wins" to land the right final byte | a composition, not a new shape: the scaled path is a second front end for the existing tail, so `game.pickups._scaled_box_result` returns the same `box_result`/`d3` (plus an `x_op` tuple naming the real last flag-setting instruction, `add.w d3,d2` here vs the unscaled path's `sub.w d3,d2`) that `pickup_check`'s own downstream found-sound/found-bare/found-effect code already consumes | gods_sega.game.pickups extended (`AWARD_SCALE_LEVEL`, `_scaled_box_result`, `_lsr_word`/`_asr_word`); plan gods_sega.boundary.pickup_check_plan extended (`WITNESSED_SCALE_LEVELS`, `_pk_scale_cost`, the transient-write fix); candidates 'pickup-check', 'pickup-probe', 'camera-sprites' (unchanged gate count) | tests/games/gods/test_pickup_check.py (5 new unit tests plus all fixtures across every census-00BA8E-fresh* directory, now including a fresh `census-00BA8E-fresh-7251bbd0ecf7-wide` at `--max-classes 600`, 407 real path classes vs the old 32: 1107 of 1108 total 00BA8E/010CD2 fixtures MATCH, 1 correctly DECLINED (found-special-timer, still unwitnessed)) | f0ac19738f190b5a1efa7ac5cd2677f6e0ce582df16483525fad1f95f9bc7068, 15,148 frames, artifacts/gods/verify-pickup-check3-f0ac1973, PASS, 4,320 hits, 31 fallbacks (all z80 bank guard, unchanged); 7251bbd0ecf717086df9f8029c56dd0914620e59ab459610eea0e849d625fc42, 25,264 frames, artifacts/gods/verify-pickup-check3-7251bbd0ecf7, PASS, 13,606 hits, 30 fallbacks (13 observation deadline, 4 vblank in span, 12 z80 bank guard, 1 found-special-timer -- the found-message fallback reason is gone); segment_verify at boundary-6000/12000 PASS (231 hits/1 fallback, 132 hits/3 fallbacks, all z80 bank guard, unchanged); mutant pickup-check-mutant-result DIVERGENCE at frame 2,034 on 7251bbd0ecf7 (artifacts/gods/verify-pickup-check3-mutant) | 13,606 hits, 30 fallbacks | this commit
2026-09-17 | 0047DA achievement slot reset / trigger firing subsystem blocker part 2, first bite: the collectible/achievement-slot tracker's own reset call, owned through its caller as a platform-tail seam (Aladdin's "one native call inside the branch" shape, recipe 6b, reproduced with Gods' own device convention -- 0018C8's own seam runner and Seam type, unchanged). 0047DA marks one of four tracked-id slots (FFFFF22E array) empty and always calls the collected-item icon upload (001648, disassembled fresh: a VDP control command from the table at 0016C2 indexed by D0, then either a tile-descriptor stream through the remap table 0B5D04/0B5E04 or -- the only arm reachable from THIS caller, since 0047DA sets D2=-1 unconditionally before every call -- a fixed 8-longword fill, FFFFFFFF or DDDDDDDD depending on D1) as a seam: the prefix is 0047DA's own RAM-only head (its d1/d2/a0 frame, the slot store, deriving D1 from whether D0 matches a caller-supplied "highlighted" id at FFFFF236) ending at the bsr itself; the ceded block is all of 001648; the resume is 0047FA; the suffix is 0047DA's own frame restore + rts, RAM-only. D0 (0-3) selects which of 001648's own four VRAM icon slots the table at 0016C2 addresses; 0, 1 and 3 are witnessed across the eight recordings, 2 is real ROM code but no recording ever calls 0047DA with it: declined (`WITNESSED_ICON_SLOTS`). Two defects caught before landing: MOVEQ sign-extends to the FULL 32-bit register (D1<-0/1, D2<--1), not just the low word like a `.w` move elsewhere in this file, and MOVEQ *does* set N/Z/V/C (only X is unaffected) -- both wrongly modelled at first, caught by `factcheck check`'s own register/CCR comparison; and the mutant's own generic "flip the last write" needed the return-address write moved earlier in the composition's own `order` so the semantic slot-store byte, not part of the bsr's return address, is the one that ends up last (a flipped return address is an odd-address instruction fetch -- a 68000 address error, not a clean divergence) | a platform tail via bsr, not 0018C8's own inline-upload shape (the device work lives entirely inside a separately-called subroutine): the whole of 001648 is the ceded block regardless of which of its own two device sub-arms the ROM takes, so the composition need not know which one ran | gods_sega.game.achievements (new module: achievement_slot_reset); plan gods_sega.boundary.achievement_slot_reset_plan (+ achievement_slot_reset_suffix); candidates 'achievement-slot-reset', 'camera-sprites' (extended to twenty-nine gates) | tests/games/gods/test_achievements.py (new: 4 unit tests plus all 9 retained fixtures across census-0047DA-fresh-{fb408bc75597,f0ac19738f19,7251bbd0ecf7,f40d7bcc9dda,4492103be245,ca2b703b6fd5,cfd02803e937,da660c605b4a} MATCH, none declined at the fixture level -- the one unwitnessed icon slot (2) is confirmed separately by a synthetic D0 poke) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-achievement-slot-reset-fb408bc75597, PASS, 12 hits (6 seam entries/completions), 1 fallback (unsupported domain: icon slot 2, exactly the one real occurrence of it on this history); segment_verify at boundary-6000/12000 PASS, 0/0 hits/fallbacks both (the region is too rare for a 300-frame window); mutant achievement-slot-reset-mutant-result DIVERGENCE at frame 20,282 (artifacts/gods/verify-achievement-slot-reset-mutant2) | 12 hits, 1 fallback | this commit
2026-09-17 | 004790 achievement slot dispatch / trigger firing subsystem blocker part 2, second bite: a caller-supplied record pointer's own tracked id, gated by a shared 10-byte-stride record table's own status word at FFFFF8C2 (only status 2 is witnessed across all 12 retained fixtures; a real "blocked" arm for any other status stays declined) then checked in turn against the already-recovered 0047DA's own four ids (FFFFF22E/F230/F232/F234) -- all four comparisons always run, so at most one can differ (the ids are distinct) and 0047DA is called at most once, D0 the matched slot. A miss is a plain RAM-only leaf (4 redundant compares, no calls, witnessed on 6 of 12 fixtures). A match is a platform tail ONE LEVEL UP from achievement_slot_reset_plan: genesis_re.seam.run_seam narrows the gate array to just its own resume_pc while a seam's ceded block runs, so 0047DA's own separate gate never fires inside this seam even though it is armed in the SAME composite candidate -- the whole of 0047DA (itself a seam over 001648) is one opaque, unmodelled ceded block, and this composition needs nothing about 0047DA's own internals beyond what its own candidate already proved. The resume is 004790's own continuation after the ONE bsr that ran (0047BA/0047C4/0047CE/0047D8, one per matched slot, dispatched by the suffix on the live PC alone); the suffix then runs whichever later comparisons the ROM still makes (always misses) and 004790's own final rts. A second defect of the same MOVEQ-flags class as 0047DA's own: the true last flag-setter before the bsr is `moveq #n,d0`, not the `cmp.w` before it (nothing between them touches CCR) -- caught the same way, by factcheck check's own register/CCR comparison | a seam composed over another seam, not a new mechanism: 0047DA's own admission already proved 001648's ceded block; this bite only had to prove that a SECOND gate's own seam can cede straight through a THIRD gate's own entry without the dispatcher ever seeing it fire, which run_seam's own gate-narrowing already guarantees | gods_sega.game.achievements extended (achievement_slot_dispatch, RECORD_TABLE/RECORD_STRIDE/WITNESSED_STATUS/TRACKED_IDS, the exact ADDA.W sign-extending record-address arithmetic); plan gods_sega.boundary.achievement_slot_dispatch_plan (+ achievement_slot_dispatch_suffix); candidates 'achievement-slot-dispatch', 'camera-sprites' (extended to thirty gates) | tests/games/gods/test_achievements.py extended (7 new unit tests plus all 12 retained fixtures across census-004790-fresh-{fb408bc75597,f0ac19738f19,7251bbd0ecf7,f40d7bcc9dda,4492103be245,ca2b703b6fd5,cfd02803e937,da660c605b4a} MATCH, none declined -- the "blocked" status arm stays unwitnessed by every one) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-achievement-slot-dispatch-fb408bc75597, PASS, 8 hits (2 seam entries/completions), 0 fallbacks; segment_verify at boundary-6000/12000 PASS, 0/0 hits both (too rare for a 300-frame window); mutant achievement-slot-dispatch-mutant-result (a register: D0, real either way -- 0047DA marks slot D0 empty and 001648 indexes its own VDP table by it, and the no-match arm does not touch D0 at all so wrongly claiming it changed is itself observable) DIVERGENCE at frame 24,370 (artifacts/gods/verify-achievement-slot-dispatch-mutant) | 8 hits, 0 fallbacks | this commit
2026-09-17 | 00475E slot scan / the trigger firing subsystem blocker's own part 3 preamble, `00475E`: gates on bit 7 of the caller's own record's $10 byte; when set, checks three independent flag words in program order ((a1), $4(a1), $8(a1), each against 1) and, for whichever is 1, calls the already-recovered achievement_slot_dispatch (004790) with a pointer two bytes past the flag -- the 0049DA-calls-001164 shape, up to three times in one activation. Censused fresh over all eight recordings (`--max-classes 400`): every occurrence has at most one flag true (gate-false, all-three-false, or exactly one call, never two or three); the third flag (0x08(a1)) is real ROM code no recording ever sets, and only the first position's call is ever witnessed to match a tracked id (the second, when true, is always a plain miss). A match composes one level up from achievement_slot_dispatch_plan exactly as that planner composes one level up from achievement_slot_reset_plan (the whole of 0047DA/001648 stays one opaque ceded block; only the resume PC, already one of 004790's own `_AD_RESUME_ADDRESSES`, is gated) -- `genesis_re.seam` is not extended; a second independent match in the same activation (never witnessed) would decline inside the suffix rather than chain a second seam | RAM-only leaf composing up to one call into the already-recovered 004790, a seam two levels up from `achievement_slot_reset_plan` when it matches | gods_sega.game.achievements.slot_scan_gate/slot_scan_flag; plan gods_sega.boundary.slot_scan_plan; candidates 'slot-scan', 'camera-sprites' (extended to thirty-one gates) | tests/games/gods/test_achievements.py extended (6 new unit/fixture tests plus all 30 retained fixtures across census-00475E-{fb408bc75597,f0ac19738f19,7251bbd0ecf7,f40d7bcc9dda,4492103be245,ca2b703b6fd5,cfd02803e937,da660c605b4a} MATCH, none declined at the fixture level) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-slot-scan-fb408bc75597, PASS, 68 hits (2 seam entries/completions), 0 fallbacks; segment_verify at boundary-6000/12000 PASS (0/0, 1/0 hits/fallbacks); mutant slot-scan-mutant-result (a register, D0: real on the seam arm and untouched on every no-call arm, achievement-slot-dispatch's own reasoning) DIVERGENCE at frame 24,370 (artifacts/gods/verify-slot-scan-mutant) | 68 hits, 0 fallbacks | this commit
2026-09-17 | 004800 record id scan / the trigger firing subsystem blocker's own part 2, last caller (`docs/gods/blockers/2026-09-16-00462C-firing.md`'s Split): gates on d5 = ($10(a1)) & 0x7fff being one of {2,3,4,7,8} (real ROM code for {5,6} or anything above 8 skips the whole routine); when it is, checks the SAME three (flag, id) field pairs `00475E`'s own slot scan does, but the id word must fall in one of two ranges (0x12-0x17 or 0x7f-0x81, the achievement item ids the collectible tracker actually uses) rather than equal 1. A witnessed pair calls a freshly-disassembled `0048B4`: the SAME four-tracked-id compare achievement_slot_dispatch's own tail runs, with no status gate at all, reaching 0047DA through a tail JUMP (`bra.w`, not `bsr`) -- so 0047DA's own rts returns directly to whichever of 004800's own three call sites made the jump, needing no 004790-style resume-and-continue layer of its own; only 0048B4's own id-compare body (charged explicitly, since the composition jumps straight from 004800's entry to 0047DA's) had to be modelled fresh. Every witnessed call (first or second position) is a match; the third position, a miss from either witnessed one, and a second independent match in the same activation are all real ROM code no recording enters -- declined, not guessed. `achievement_slot_dispatch`'s own id-compare tail was factored out into a shared `match_tracked_id` reused here | RAM-only leaf composing up to one call into 0048B4, a seam over `achievement_slot_reset_plan` reached without any intermediate resume layer at all (0048B4's tail jump leaves 0047DA's own rts returning straight to 004800's own call site) | gods_sega.game.achievements extended (`record_id_scan_gate`, `id_in_range`, `record_id_scan_check`, `match_tracked_id`); plan gods_sega.boundary.record_id_scan_plan; candidates 'record-id-scan', 'camera-sprites' (extended to thirty-two gates) | tests/games/gods/test_achievements.py extended (5 new unit tests plus all 58 retained fixtures across the same eight census-004800-* directories: 54 MATCH, 4 correctly DECLINED (icon slot 2, unwitnessed)) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-record-id-scan-fb408bc75597, PASS, 68 hits (3 seam entries/completions), 1 fallback (unsupported domain: icon slot 2, the one real occurrence); segment_verify at boundary-6000/12000 PASS (0/0, 1/0 hits/fallbacks); mutant record-id-scan-mutant-result (NOT a register this time -- a witnessed match includes icon slot 3, and D0+1 there reaches table index 4, outside 001648's own four VRAM slots, which faults the platform rather than diverging; "flip the last write" instead, which always lands on achievement_slot_reset's own real semantic store, the last write `achievement_slot_reset_plan` itself inserts) DIVERGENCE at frame 20,282 (artifacts/gods/verify-record-id-scan-mutant) | 68 hits, 1 fallback | this commit
2026-09-17 | 0048E4 action reset-elapsed + 004ACA action clear-group / part 3 of the trigger firing subsystem blocker's Split, its first two admissible action-table entries: the evaluator's own firing tail dispatches its action through the ROM table at 0046D0 via a tail JUMP (`jmp (a5)`, 0046CE, disassembled fresh -- `move.w $10(a1),d0` doubled twice into a 15-entry table), so a handler's own `rts` returns straight past the whole evaluator activation to whoever called it: no seam, no frame, the same opaque-tail shape `0048B4`'s own jump into `0047DA` already proved. `0048E4` is `clr.l ELAPSED; rts` unconditionally (the SAME elapsed-seconds counter `conditions.py`'s own kinds 9/10 already read), fully witnessed, no declines, one real occurrence per recording. `004ACA` compares a caller-record's own word at `+0x12` against `pickups.py`'s own three group active-id words (`GROUP_TABLES`) in turn and clears whichever matches -- each of the three has its OWN `rts` (004ADA/004AE8/004AF6, not one shared exit), and only the second group is ever witnessed to match (a miss against all three, and a match against the first or third, are real ROM code no recording enters). The other action-table entries (`004A0A`, `004D04`, `004E1C`, `004E74`, `0048EA`, `005024`, `00772E`; `004A74`/`005074` unwitnessed) call still-unrecovered helper routines (`004AAA`, `004926` -- itself touching the already-recovered spawn queue's own `FFF39A` table, a real lead for later -- `004ECE`/`004D7E`, `0050A4`, `0077A8`, `004F16`) or write the level grid across several blocks: an ordinary decline (a kind that calls unrecovered code), not a new mechanism, so no blocker package | two RAM-only leaves with no frame of their own, reached by a tail jump with no seam needed | gods_sega.game.actions (new module: reset_elapsed, clear_matched_group); plan gods_sega.boundary.action_reset_elapsed_plan, action_clear_group_plan; candidates 'action-reset-elapsed', 'action-clear-group', 'camera-sprites' (extended to thirty-four gates) | tests/games/gods/test_actions.py (new: 6 unit tests plus all 8 retained 0048E4 fixtures and all 5 retained 004ACA fixtures MATCH, across census-{0048E4,004ACA}-{fb408bc75597,f0ac19738f19,7251bbd0ecf7,f40d7bcc9dda,4492103be245,ca2b703b6fd5,cfd02803e937,da660c605b4a} where each fired) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames: action-reset-elapsed PASS 1 hit/0 fallbacks (artifacts/gods/verify-action-reset-elapsed-fb408bc75597), action-clear-group PASS 1 hit/0 fallbacks (artifacts/gods/verify-action-clear-group-fb408bc75597); segment_verify at boundary-6000/12000 PASS for both (0/0 hits both, too rare for a 300-frame window); both mutants (the last write; both arms always store) DIVERGENCE, action-reset-elapsed at frame 15,102 (artifacts/gods/verify-action-reset-elapsed-mutant), action-clear-group at frame 17,112 (artifacts/gods/verify-action-clear-group-mutant) | 1+1 hits, 0 fallbacks | this commit
2026-09-17 | 0075D6 player state machine shared tail / the supervisor's Decision on `docs/gods/blockers/2026-09-17-005700.md`: a platform-tail seam, not a new subsystem.  Gate `0075D6` (reached by a plain `bra.w` from every witnessed state handler and the dispatcher's own 'inactive' arm, so there is no frame of its own on the stack).  Prefix, in ROM order: the tile trigger scan's clean arm (`00773A`, already semantics; the 'trigger' arm -- an event through 007850 -- declines by name); the follow-point step (new, `game.camera.follow_point_step`: FOLLOW_X/FOLLOW_Y, `camera_follow`'s own words, eased toward the player's position -- X by a fixed step of four inside a 0x50/0xD0 dead band, Y by half the distance beyond its own 0x70/0x20 band with a minimum step of one -- clamped to 0xEC0/0x340; only when FFFFEF4E is zero, true on all 55,326 activations of every one of the eight recordings, checked by a full-history scan since `recovery_census.py`'s own path-signature classifier overflows on 0075D6 exactly as it does on 005700 itself -- 521 fixtures retained anyway before the overflow was found, kept as `census-0075D6`; the sibling arm at 00755A declines by name); the state-table re-index (new, `game.player.state_table_reindex`: STATE_TABLE's own entry plus the live D7, an unbounded ROM table read exactly like `game.sprites`' own descriptor tables, fully witnessed on every branch -- both mask tables, both flip outcomes).  Ceded operation: `0076AA`'s `jmp 001312` through the upload's own `rts` (0013CE) -- 0048B4's own "no intermediate resume layer" shape one level further removed: that `rts` pops the return address ALREADY on the stack when the activation began (witnessed as both 001FD6, the main loop's own per-tick call, and 012BA0, a nested return through the tile scan's own declined cascade), so there is no suffix beyond admitting the `rts` itself (gated at 0013CE, not after it: the native adapter refuses a zero-cost atomic).  Fixtures: since the entry-level census overflowed (521 fixtures, `overflow 4523` at `--max-classes 400`), retained instead by re-parking `00773A`'s own 399-real-class census at the parent gate `--parent 0x0075D6` (406 fb408bc75597 + 168 f40d7bcc9dda fixtures, `census-0075D6`/`census-0075D6-f40d7bcc9dda`), plus 80 fixtures a full-history scan retained directly by (x-branch, y-branch, mask-table, flip) combination (`census-0075D6-tail`, `/tmp/full_scan_validate.py`-style scan kept as a documented method here, not committed) to reach the rarer bands the proxy census under-samples (x-increase, y-increase/increase-clamped).  Two arms declined as genuinely unwitnessed on the full tree: the x follow-point clamp at 0xEC0, and the y follow-point clamp at 0; a third, narrower edge also declines: the y-increase band's own minimum-step override (00763C, dy=0x71 exactly, the only value that rounds the halved step to zero) is unwitnessed even though the y-increase band itself is common -- confirmed by a direct fixture (`tail-fb408bc75597-f6705-...`).  D0's own upper half is zero from the tile scan's own `moveq #$10,d0` onward (not the caller's own entry upper half, unlike every other register this planner touches); X survives MOVE untouched from the re-index's own unconditional `add.w d2,d2` (007694), the SAME instruction on every branch, so X is simply the re-index's own index bit 15 regardless of which follow-point arm ran.  Mutant: a follow-point byte off (FOLLOW_X or FOLLOW_Y, whichever this occurrence's own step wrote last); a 'hold'/'hold' occurrence (about half of them) writes neither and passes through unmutated rather than falling back to STATE_COUNTER, which an earlier version of this control corrupted and which faulted the machine (a real M68000 address error inside 001312's own tile-copy loop on a garbage descriptor pointer, confirmed once) rather than diverging cleanly | `game.camera.follow_point_step` (+`FOLLOW_X_STEP`, `FOLLOW_X_LIMIT`, `FOLLOW_Y_LIMIT` etc.), `game.player.state_table_reindex`, `game.player.SHARED_TAIL_ALT_GATE`; plan `gods_sega.boundary.player_tail_plan`; candidates 'player-tail', 'camera-sprites' (extended to thirty-five gates); mutant `player-tail-mutant-result` (`recovery._mutate_follow_point`) | tests/games/gods/test_player_tail.py (new: 8 unit/fixture tests; every one of the 654 retained fixtures across `census-0075D6`, `census-0075D6-f40d7bcc9dda` and `census-0075D6-tail` either MATCHes as a seam (prefix to 001312, suffix from 0013CE) or DECLINEs for a named reason -- 84 MATCH, 570 correctly DECLINED (the tile scan's own trigger arm, or the y=0x71 edge), 0 unexplained) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-player-tail-fb408bc75597, **PASS, 10,042 hits (5,025 seam entries, 5,017 completions), 6,059 fallbacks (5,939 tile-scan trigger declines, 87 z80 bank guard, 31 y-minimum-step declines, 2 seam deadline)**; segment_verify at boundary-6000/12000 PASS (76/107, 188/31 hits/fallbacks, the one y=0x71 decline appearing naturally in the second window); mutant `player-tail-mutant-result` DIVERGENCE at frame 539 on f0ac19738f19 (artifacts/gods/verify-player-tail-mutant -- fb408bc75597's own full 34,904-frame run hits a REAL, unrelated M68000 address error far downstream of an early clean divergence, a tooling limit of running a mutant to completion rather than stopping at the first difference, not evidence against the control: the short segment_verify mutant already diverges cleanly at frame 6,013); `camera-sprites` (all thirty-five gates) on the tree of all eight recordings: **PASS, 107,519 frames, 1,075,426 hits, 24,106 fallbacks (18,249 tile-scan trigger declines, 4,403 z80 bank guard, 451 seam deadline, 345 observation deadline, 304 vblank in span, 133 y follow-point declines (61 clamp, 72 minimum-step), 189 evaluator firing/disabled, 27 machine admission, 5 achievement/pickup declines), tree bit-exact** | 10,042 hits, 6,059 fallbacks | this commit
2026-09-17 | 008222 / 012DA0 / 012E5A movement states' shared hit-resolution web | escalation: NEW_GODS_SUBSYSTEM | - | - | - | - | blocker docs/gods/blockers/2026-09-17-008222.md; every movement-cluster state handler disassembled after 0075D6 (state 1 `007282`, state 0 `006FFE`, and state 24 `006AD8` surveyed as a simpler alternative once state 5's own `00746A` turned out to be a shared fallthrough of state 1's own body, not an independent handler) reaches `008222` (a 40-entry hit-list search, `FFFFF374`/`F378`/`F37C` its own persistent slots) or its consumers `012DA0`/`012E5A` (a two-level per-object-type dispatch through ROM tables at `012D04`/`012C3E`) on a real, frequently-witnessed fraction of activations -- 800 of 3,181 state-1 activations on `fb408bc75597` reach `008222` (25%), and 7 of 14 retained state-24 path classes reach `012DA0`'s own per-type `jmp`.  `00722C`, a separate bounded 200-entry box scan states 0/1 also call, is confirmed NOT a blocker (its returned CCR is provably unused at either call site) but doesn't unblock either state on its own.  Next question: whether `008222` is recoverable as a leaf on its own first, before `012DA0`/`012E5A`'s own per-type consumers are | escalate commit
2026-09-18 | 0075D6 player state machine shared tail / correction to this same day's event-status characterisation, before push: the status==0 decline (`bmi` NOT taken, then `beq` taken) was folded into the witnessed negative-status decline arm on an UNTRACED cost -- cross-checking against a real positive-status (kind-3 fire) trace found `bmi.w` not-taken costs 12 cycles, not the 8 assumed, and 0 of 654 retained fixtures exercise status==0 at all (only negative, 517 times).  Split into its own named arm (`game.player.event_status` returns `'declined-zero'`, not `'declined'`, for status==0) so `tile_trigger_scan`'s `fires` list -- and so `player_tail_plan`'s decline -- covers it like any other unwitnessed arm, instead of shipping an untraced number.  No observable effect on any of the eight recordings (segment/history-verify/tree fallback counts identical before and after: status==0 apparently never occurs) | `game.player.event_status` (arm split); plan `gods_sega.boundary._tc_position` (drops the now-dead `_ES_DECLINE_ZERO` branch, asserts `'declined'` only) | tests/games/gods/test_player.py (3 tests updated, 1 new: the zero-status case is now asserted to be in `fires`, unwitnessed) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, artifacts/gods/verify-player-tail4-fb408bc75597, PASS, 16,386 hits, 2,891 fallbacks, unchanged; mutant DIVERGENCE at frame 2,261 (artifacts/gods/verify-player-tail4-mutant); camera-sprites tree PASS, 107,519 frames, 14,007 fallbacks, unchanged (artifacts/gods/verify-camera-sprites-tree-2026-09-18e) | 16,386 hits, 2,891 fallbacks | this commit
2026-09-18 | 0075D6 player state machine shared tail / the tile trigger scan's own 'trigger' arm characterised in full: 0077A8 disassembled to its own `rts` (0077A8-007878) -- a cell over TILE_THRESHOLD reads its own byte as a record kind and looks up `EVENT_STATUS_WORDS` (== `game.conditions.STATUS_WORDS`) at `kind - 3`; zero or negative **declines**, and the scan (called by `bsr`, or `bra` at the scan's own absolute last position) tries the next cell exactly as a clean scan would -- composed as a general per-cell cost/write model (`boundary._tile_scan_cost`, `_tc_position`, replacing the two fixed clean-arm constants) reproducing 0077A8's own decline body (head + bmi/beq-taken tail) and its abandoned stack residue (sp-12/sp-16, shared between bsr positions 0-4 and, for sp-12 only, the bra position 5); only a STRICTLY POSITIVE status word raises a real event through `EVENT_TABLE`/`EVENT_HANDLERS` (1-11; kind 3 is the already-recovered trigger evaluator `00462C`) and still declines by kind, named, real code this composition does not reach into.  A pre-existing, previously-invisible defect found and fixed the same session, exposed only once 'trigger-but-declined' scans started being admitted: the y follow-point band's own decrease-side d3 setter is `moveq #$20,d3` (clears d3's own upper half), not the increase band's plain `.w` moves (`boundary.player_tail_plan` had carried the caller's own entry upper half through unconditionally) -- found on the *tree* run (node ca2b703b6fd5, frame 9661, a silent `state_sha256` divergence of two RAM bytes matching or a stale copy of d3's own wrongly-carried upper half; video/audio bit-identical), not by any of the 654 fixtures (all had a zero d3 upper half at entry) | `game.player.event_status` (+`EVENT_STATUS_WORDS`, `EVENT_STATUS_INDEX_BIAS`, `EVENT_TABLE`, `EVENT_HANDLERS`); `game.player.tile_trigger_scan` extended (`checks`, `fires`, `arm`/`trigger_index` unchanged for backward compatibility); plan `gods_sega.boundary.player_tail_plan` extended (`_tile_scan_cost`/`_tc_position`/`_ES_*`/`_TC_*` replacing `_PT_TILE_SCAN_NARROW`/`_WIDE`, the sp-12/sp-16 residual-write fix, the d3-upper-half fix); candidates 'player-tail', 'camera-sprites' (unchanged gate count, thirty-five) | tests/games/gods/test_player.py extended (5 new unit tests: `event_status` declines/fires, the scan continuing past a decline, a fire reported in `fires`); tests/games/gods/test_player_tail.py extended (1 new regression test for the d3 fix; the fixture-parametrized test's decline condition narrowed from `scan['arm']=='trigger'` to `scan['fires']`) -- all 654 retained fixtures across `census-0075D6`, `census-0075D6-f40d7bcc9dda` and `census-0075D6-tail` re-verified: 161 now MATCH (86 clean + 75 triggered-but-declined, up from 84), 493 correctly DECLINE by kind (`scan['fires']` non-empty) or the other named reasons | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-player-tail3-fb408bc75597, **PASS, 16,386 hits (8,201 seam entries, 8,185 completions), 2,891 fallbacks (2,704 tile-scan event-found by kind: 3→2,003, 6→650, 4→27, 2→14, 1→3, 5→3, 8→2, 7→1, 9→1; 133 z80 bank guard, 42 y-minimum-step declines, 10 seam deadline, 2 observation deadline)**, up from 10,042 hits/6,059 fallbacks; segment_verify at boundary-6000/12000 PASS (220/35, 220/15 hits/fallbacks, unchanged in kind); mutant `player-tail-mutant-result` DIVERGENCE at frame 2,261 (artifacts/gods/verify-player-tail3-mutant); `camera-sprites` (thirty-five gates) on the tree of all eight recordings: **PASS, 107,519 frames, 1,095,639 hits (130,432 seam entries, 129,355 completions), 14,007 fallbacks, tree bit-exact** -- up from 1,075,426 hits/24,106 fallbacks (a first tree attempt caught the d3 defect above: DIVERGENCE at frame 9,661 on node ca2b703b6fd5, artifacts/gods/verify-camera-sprites-tree-2026-09-18c, fixed and re-verified before this PASS) | 16,386 hits, 2,891 fallbacks (tree: 1,095,639 hits, 14,007 fallbacks) | this commit
2026-09-18 | 008222 (with its helper 00837E) contact search / `docs/gods/blockers/2026-09-17-008222.md`'s Decision: a reentrancy-guarded search over the three collectible lists `pickups.GROUP_TABLES` already names (`FFEF8C`/`FFF01E`/`FFF0B0` count words, entries at +2) read here, structurally parallel to `hazard.proximity_search`, as up to three consecutive 0x18-byte "hit records" (three status words at +0/+8/+0x10) per list -- three independent sub-passes, each gated by its own list's count word (negative skips it outright) and each looking up an item record's own word at +0xA (`ITEM_CONTACT_WORD`, the neighbour of `ITEM_VALUE`) that doubles as a retry budget (decremented after an occupied entry; hitting exactly zero abandons the sub-pass before its own third unrolled attempt) and a latch selector (sub-pass 1 sets `CONTACT_LATCH` unconditionally when its own ITEM_CONTACT_WORD is 1; sub-passes 2/3 read the latch back and discard a match of their own with ITEM_CONTACT_WORD 1 if it is already set -- neither ever witnessed: sub-pass 1's own item never has ITEM_CONTACT_WORD 1, and the latch is therefore never set for 2/3 to find set).  Census over all eight recordings (`--classifier entry --max-classes 400`, `artifacts/gods/evidence/census-008222-entry[-<node>]`, 271 real path classes, no overflow) found sub-pass 2's own item DOES have ITEM_CONTACT_WORD 1 on 96 of those fixtures -- the latch-test code itself is witnessed and admitted (the latch is simply always still clear when it runs), only the latch actually being set (sub-pass 1's own arm) and a found match being gated by it stay declined, alongside a sub-pass 2/3 whose own ITEM_CONTACT_WORD is negative ('skip-negative', unwitnessed on all 271 fixtures).  Every instruction's own cost is data-independent (confirmed against the tracer on every distinct instruction the routine executes), so the boundary costs it one instruction-block at a time rather than per fixed path, generalizing past the retained fixtures the way `proximity_plan` does.  Two register subtleties only exposed by a fixture whose caller entry register upper halves were nonzero (`008222-entry-p34`, `cfd02803e937`): D0/D3/D5 exit with upper half always zero (HEAD's own unconditional `moveq`s, not the caller's own entry value, the way every OTHER register this planner touches preserves it); D4/D6 do preserve the caller's own upper half (no `moveq` of their own touches them).  The three `bsr` calls into `00837E` per sub-pass push a transient return address at (entry `a7` − 4) that `00837E`'s own `rts` pops straight back before the next one runs -- real stack residue from the LAST call made, not dead scratch, modelled as one more write | a leaf (no save/restore frame at all, hazard_tick's own shape) with three independent sub-passes and a shared bounded-retry helper, costed one instruction-block at a time (`game.pickups._contact_probe`/`_contact_subpass`/`contact_search`; `boundary._cs_*` cost tables, `_cs_attempts_cost`, `_cs_subpass_cost`) | gods_sega.game.pickups extended (contact_search, `_contact_probe`, `_contact_subpass`, `REENTRANCY_GUARD`, `CONTACT_LATCH`, `CONTACT_SLOTS`, `CONTACT_INDEX_WORDS`, `CONTACT_ITEM_RECORDS_BASE`, `CONTACT_WORD_OFFSET`); plan `gods_sega.boundary.contact_search_plan`; candidates 'contact-search', 'camera-sprites' (extended to thirty-six gates); mutant `contact-search-mutant-result` (`recovery._mutate_contact_outcome`: D0/D3 toggled by XOR 1, not the generic +1 -- both are always exactly 0 or 1, and the generic register mutant left the very first activation in the whole game, frame 2283 of fb408bc75597 (a 'not found' occurrence), nonzero either way) | tests/games/gods/test_contact_search.py (new: 9 unit tests covering the guard, every sub-pass arm including the latch set/gate, and a poked-fixture decline test; all 271 retained fixtures across `census-008222-entry[-<node>]` MATCH, none declined) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-contact-search-fb408bc75597, **PASS, 794 hits of 800 gates, 6 fallbacks (2 observation deadline, 1 vblank in span, 3 z80 bank guard, all exact adapter refusals, 0 unsupported domain)**; segment_verify at boundary-6000/12000 PASS (3/0, 10/0 hits/fallbacks); mutant `contact-search-mutant-result` DIVERGENCE at frame 2284 (artifacts/gods/verify-contact-search-mutant), the first frame the region is exercised; `camera-sprites` (all thirty-six gates) on the tree of all eight recordings: **PASS, 107,519 frames, 1,098,065 hits, 14,018 fallbacks (11 at the new gate, all exact adapter refusals), tree bit-exact** -- up from 1,095,639 hits/14,007 fallbacks (artifacts/gods/verify-camera-sprites-tree-2026-09-18f) | 794 hits, 6 fallbacks (tree: 1,098,065 hits, 14,018 fallbacks) | this commit
2026-09-18 | 012DA0 (with its helper 012E32) contact-consume-primary + 012E5A (with 012EE6) contact-consume-secondary / the family over the item type `pickups.ITEM_RECORDS` names, per the task's own next step after `008222`: once `contact_search` has populated up to three `CONTACT_SLOTS`, these two near-identical siblings consume them one call per movement-cluster state activation -- re-reading the SAME list's own count word (it may have changed since the search ran, an ordering fact not modelled further), looking the item record up again through `ITEM_RECORDS` and reading its own type field (`ITEM_TYPE_PRIMARY` +0x14 for 012DA0, `ITEM_TYPE_SECONDARY` +0x10 for 012E5A) into d0 (a LONGWORD read -- the one register in the whole routine whose own upper half is cleared, not preserved), then dispatching through a second ROM table (012C3E) into a per-type handler.  Slots 1/2 (FFFFF374/F378) are reached by `bsr` and return normally; slot 3 (FFFFF37C) is a tail JUMP with no bsr, so the handler's own `rts` ends the whole activation directly (0048B4's shape, one level further removed).  Census over `fb408bc75597` (`--classifier entry`, no overflow: 10/8 real path classes) found every witnessed type reduces to a small header (a position delta on d3/d5, data-driven per type -- `pickups.TYPE_HEADERS`) landing in one of two shared bounded-append bodies (up to three (d4,d3,d5,const) quadruples into the hit record's own three status groups -- the SAME offsets `contact_search`'s own probe tested -- plus the item record's own claim flag; one shape also writes a `CONTACT_POOL_TABLE` sentinel per group); 012DA0's own bodies always use constants (1,0,2) and claim value 1, 012E5A's own always (4,3,5) and -1 -- a fact of which ORIGINAL routine reached the body, not of the type value.  Type 9 (012DA0 only) is a wholly separate standalone single-write handler with its own small state block.  Types 1/7/3/9 (012DA0) and 0/2/6 (012E5A) are witnessed and admitted (type 6 was missed by an early per-fixture read of only the FINAL slot's own dispatch and found on a second pass: `012E5A-entry-p2.state`'s own SLOT 1 uses it, not its slot 3); the active-selector override (`CONTACT_ACTIVE_SELECTOR` naming a slot) never fires on any of the 18 retained fixtures and is declined.  The milestone tree run additionally exercises types 8/10/11/14/15 -- real ROM code this session never censused, correctly declined by name, tree still bit-exact.  Costed one instruction-block at a time, matching every instruction's own cost confirmed data-independent against the tracer.  Mutant: the generic "flip the last write" -- real (the full history run diverges) but DELAYED (frame 11,306/11,764 of 34,904, not the region's own first activation at frame 2,625/similar): every consumer of a hit record's own status groups is itself still-unrecovered ROM (the unwitnessed per-type handlers, and 012DA0/012E5A's own still-unrecovered callers), so a corrupted byte is not guaranteed to become video/PCM-observable within a short window -- recorded here rather than solved, per the grinder's own remit | two dispatchers over a shared bounded-append body (recipe 6a, the selector arriving through a record field rather than a register) | gods_sega.game.pickups extended (contact_consume, `_consume_position`, `_item_lookup`, `_append_groups`, `_type9_handler`, `_consume_slot`, `TYPE_HEADERS`, `ITEM_TYPE_PRIMARY/SECONDARY`, `ITEM_RANGE_LOW`, `ITEM_CLAIM_FLAG`, `CONTACT_ACTIVE_SELECTOR`, `CONTACT_TRACK_X/Y`, `CONTACT_POOL_INDEX_BASE`, `CONTACT_POOL_TABLE`, `MOVEMENT_SOUND_CUE`, `TYPE9_*`); plan `gods_sega.boundary.contact_consume_primary_plan`/`contact_consume_secondary_plan`; candidates 'contact-consume-primary', 'contact-consume-secondary', 'camera-sprites' (extended to thirty-eight gates); mutants `contact-consume-primary-mutant-result`, `contact-consume-secondary-mutant-result` (`recovery._mutate_result`) | tests/games/gods/test_contact_consume.py (new: 7 unit tests covering every witnessed type shape, the unrecovered-type stop and the 012E5A type-6 sharing; all 18 retained fixtures across `census-012DA0-entry`/`census-012E5A-entry` MATCH, none declined) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames: contact-consume-primary **PASS, 401 hits of 402 gates, 1 fallback (z80 bank guard, exact, 0 unsupported domain)** (artifacts/gods/verify-contact-consume-primary-fb408bc75597); contact-consume-secondary **PASS, 243 hits of 246 gates, 3 fallbacks (z80 bank guard, exact, 0 unsupported domain)** (artifacts/gods/verify-contact-consume-secondary-fb408bc75597); segment_verify at boundary-6000/12000 PASS for both (0/4 and 2/1 hits/fallbacks); mutants DIVERGENCE at frame 11,306 (primary, artifacts/gods/verify-contact-consume-primary-mutant) and frame 11,764 (secondary, artifacts/gods/verify-contact-consume-secondary-mutant) -- real but delayed, see above; `camera-sprites` (all thirty-eight gates) on the tree of all eight recordings: **PASS, 107,519 frames, 1,099,857 hits, 14,156 fallbacks (138 at the two new gates: 72+66 exact adapter refusals, plus declined types 8/10/11/14/15 elsewhere on the tree -- 20+22+38+47+2 -- real ROM code this session's own census did not sample, correctly declined, tree still bit-exact)** (artifacts/gods/verify-camera-sprites-tree-2026-09-18g) | 401+243 hits, 1+3 fallbacks (tree: 1,099,857 hits, 14,156 fallbacks) | this commit
2026-09-18 | 006AD8 (state 24, game.player.movement_hit_state routine 0) + 006B14 (state 25, routine 1) / the first two movement-cluster states composed over the now-recovered contact-consume family, per the task's own next step: a leaf owning its own 3-tick shape (STATE_COUNTER incremented unconditionally on entry) and an internal JSR into the already-recovered `012DA0`/`012E5A` (`boundary._cc_resolve`, extracted from `contact_consume_primary_plan`/`secondary_plan` so both the standalone gate and this composing caller share one cost/register model -- the `0049DA` calling `001164` shape one level up).  Tick 1 (counter becomes 1) calls the consumer once; tick 2 (becomes 2) and the no-call half of tick 1 both fall straight into the shared tail (`0075D6`, already recovered as `player-tail`) via a `blt.w`; tick 3 (reaches 3) transitions to state 14 (`STATE_INDEX = 0xE`) and copies a tracked position (`CONTACT_OVERRIDE_Y`) into `grid.GRID_Y` before falling into the SAME tail via a `bra.w` -- the state's own plan ends with `pc = 0075D6` and leaves the tail's own SEPARATELY-armed gate to continue, the same composition boundary `0075D6` itself already draws with the states above it.  `CONTACT_OVERRIDE_FLAG`'s own bit 0 clear reloads the counter from `CONTACT_OVERRIDE_COUNTER` instead of the routine's own fixed reset value -- 3 for state 24, 1 for state 25 (found via a real cost mismatch: state 25's own `STATE_INDEX` write uses ABS.W addressing, 16 cycles, not state 24's ABS.L, 20 cycles, and its own counter reset is `moveq #$1`, not `#$3` -- the two states are near-identical in shape, not byte-identical in encoding).  Census over `fb408bc75597` (`--classifier entry`, no overflow: 14/15 real path classes) matches every arm structurally (no declines at all: every occurrence's own consumer call, if any, lands on an already-witnessed contact-consume type) | a leaf composing an internal JSR into an already-recovered family, plus its own small 3-tick head/tail, costed one instruction-block at a time | gods_sega.game.player extended (movement_hit_state, CONTACT_DIRECTION, CONTACT_OVERRIDE_FLAG/COUNTER/Y, CONTACT_HIT_TRANSITION_STATE, CONTACT_HIT_DIRECTION, CONTACT_HIT_RESET_COUNTER); plan gods_sega.boundary.movement_hit_primary_plan/movement_hit_secondary_plan (`_cc_resolve` extracted from the contact-consume planners for reuse); candidates 'state-24', 'state-25', 'camera-sprites' (extended to forty gates); mutants `state-24-mutant-result`, `state-25-mutant-result` (`recovery._mutate_result`) | tests/games/gods/test_movement_states.py (new: 4 unit tests for every arm including both routines' own reset values; all 14+15 retained fixtures across `census-006AD8-entry`, `census-006B14-entry` MATCH, none declined) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames: state-24 **PASS, 153 hits of 153 gates, 0 fallbacks**; state-25 **PASS, 52 hits of 52 gates, 0 fallbacks** (artifacts/gods/verify-state-24-fb408bc75597, verify-state-25-fb408bc75597); segment_verify at boundary-6000/12000 PASS for both (0 hits, too rare for a 300-frame window); mutants DIVERGENCE at frame 2,736 (state-24, first occurrence frame 2,733) and frame 3,236 (state-25, first occurrence frame 3,233) -- essentially the first frame each region is exercised (artifacts/gods/verify-state-24-mutant, verify-state-25-mutant); `camera-sprites` (all forty gates) on the tree of all eight recordings: **PASS, 107,519 frames, 1,100,252 hits, 14,169 fallbacks (13 at the two new gates, all exact adapter refusals, 0 unsupported domain), tree bit-exact** (artifacts/gods/verify-camera-sprites-tree-2026-09-18h) | 153+52 hits, 0 fallbacks (tree: 1,100,252 hits, 14,169 fallbacks) | this commit
2026-09-18 | 00722C box-overlap scan (game.movement.box_overlap_scan) / the states' own shared callee named in the original 008222 blocker's own "What was tried" section, recovered on its own merits per the task's own list ("00722C ... as a leaf first if that keeps the states' plans small"): a bounded 200-entry scan (FFFF4342, 8-byte stride) testing a box around each entry's own (x, y) against a fixed box around the player's own tracked position (GRID_X + 0x10, GRID_Y + 0x28), stopping early on the first entry whose box contains it.  Two status fields per entry (+4 negative, +6 zero) each skip it outright; the result is returned through the CCR by a deliberate `move.w #imm,-(a7); rtr` trick (RTR pops the pushed word as the WHOLE exit CCR, X included, not just NZVC).  Confirmed (again) what the blocker already found: the result is discarded at BOTH witnessed call sites (states 0 and 1) -- no conditional branch reads it before the next flag-setting instruction -- so, exactly like `tile_trigger_scan` (00773A) before `0075D6` was recovered, this candidate has no mutant the game can see yet and is not its own gate; it waits to be composed into whichever of states 0/1 lands first.  Census over `fb408bc75597` (`--classifier entry --max-classes 400`, 19 real path classes of 21 occurrences) covers skip-negative, skip-zero, every one of the box test's own four chained comparisons failing, and a full 200-entry exhaustion, with no declines.  One real defect caught by `factcheck check`, not guessed: the box test's own fourth comparison (`ble.b`, the match test) reuses the SAME "taken" cost as the first three comparisons' own failing branch when it does NOT match -- but `ble` not-taken costs 8 cycles, not the 10 every earlier `blt`/`bgt` TAKEN failure costs; caught as a 2-cycle mismatch on exactly the fixtures whose own found entry is preceded only by skip arms | a bounded RAM-only leaf, costed one instruction-block at a time (the fourth comparison's own two outcomes -- match found vs continue scanning -- share nothing with the first three's own pass/fail costs, despite the surface symmetry) | gods_sega.game.movement extended (box_overlap_scan, BOX_SCAN_TABLE/COUNT/STRIDE, BOX_SCAN_STATUS_A/B, BOX_SCAN_PLAYER_X/Y_MARGIN, BOX_SCAN_ENTRY_X/Y_NEAR/FAR); plan gods_sega.boundary.box_overlap_scan_plan (not yet a candidate: no gate armed, no mutant) | tests/games/gods/test_box_overlap_scan.py (new: 3 unit tests for skip/found arms; all 19 retained fixtures across census-00722C-entry MATCH, none declined) | factcheck check only (not a gated candidate); no history-verify, segment_verify, mutant or tree run applies until it is composed into a state | — | this commit
2026-09-18 | 0075D6 player state machine shared tail / the tail's event arm over the evaluator, per the task's own next step: `game.player.event_status` extended (`EVENT_RECORD_INDEX_OFFSET`, `status_address`/`record_index` on the `'event'` arm -- the SAME `EVENT_STATUS_WORDS` entry's own +2 word, read by 00462C's own head as `2(a0)`); `boundary.player_tail_plan` composes the scan's own found-tile arm at last: each triggered cell's own raise (0077BE-007876, ten unconditional flag-word checks -- real code, no recording has ever been seen with any of them set -- then the table dispatch itself, `jsr (a1)`) is modelled, and a kind-3 raise (00462C, the trigger evaluator) is resolved through the ALREADY-RECOVERED `evaluator_plan`'s own machinery: `_evaluator_resolve` extracted from `evaluator_plan` (parked directly at 00462C) so a THIRD caller -- after `evaluator_plan` itself and now the tile scan's own raise -- shares one cost/register model, the shape `004790` reusing `achievement_slot_reset_plan` proved. A kind-3 raise whose own three conditions all hold (the firing arm) still declines by name (`docs/gods/blockers/2026-09-16-00462C-firing.md`'s own NEW_GODS_SUBSYSTEM stands); every other kind declines by name too (real code this module does not model); the scan continues after each ADMITTED raise, up to six per activation, exactly as it already did for a plain decline. Register residue threads correctly across however many raises one activation makes: a4 (the only register a raise may leave untouched, when no condition kind touches it) carries forward from the previous raise, or the activation's own entry a4 if this is the first; d0's own upper half resets to zero on EVERY 0077A8 call (`moveq #0,d0`, declined or admitted alike -- not only on an event) and is threaded pair-to-pair inside a raise exactly as 00470C's own dispatcher reads it, so only the LAST raise's own last-pair predicate (a kind 9/10 divs remainder, the only source of a nonzero upper half) can leave it nonzero at the tail's own final `move.w f18c,d0`. Two real defects caught before landing, both by `factcheck check`'s own byte-exact comparison, neither by guessing: (1) a naming collision -- this session's own first draft of the raise's cost constants reused the `_TE_*` prefix already bound to `evaluator_plan`'s own module-level constants, silently rebinding `_TE_TAIL` to a wrong value and breaking every `test_triggers.py` fixture until the collision was found and the whole composition refactored to share `_evaluator_resolve` instead of reimplementing it (this file's per-region cost constants share one flat namespace, the SAME reminder `string-copy`'s own `_SC_`/`_STRCPY_` collision left on 17 September); (2) the slot-truth test for the firing AND must be "was the slot written", not `conditions.evaluate`'s own `arm == 'true'` -- kind 0 (`'none'`, the placeholder condition) never clears its slot either, so it holds exactly like a genuine `'true'` arm, and `evaluator_plan`'s own existing code already got this right (`call['result']['arm'] != 'false'`) where this session's own first draft did not. Census: `artifacts/gods/evidence/census-0075D6`/`census-0075D6-f40d7bcc9dda`/`census-0075D6-tail` (654 fixtures, unchanged retention from 17-18 September, now re-verified end to end: `factcheck check` MATCH on every fixture whose scan raises nothing but admitted kind-3 non-firing events, 404 of 654, DECLINED correctly by name on the other 250 -- 0 mismatches) | `game.player.event_status` extended; plan `gods_sega.boundary.player_tail_plan` extended (`_tc_position`/`_tile_scan_cost` now return the scan's own accumulated writes and register residue; `_evaluator_resolve` extracted from `evaluator_plan`, `_trigger_evaluate_resolve` a thin wrapper over it for the tile scan's own raise); candidates 'player-tail', 'evaluator', 'camera-sprites' (unchanged gate count, forty) | tests/games/gods/test_player_tail.py extended (the fixture-parametrized test's own success case now asserts every fired check is an admitted kind-3 event, not that none fired at all); tests/games/gods/test_player.py extended (`event_status`'s own new fields); tests/games/gods/test_triggers.py unchanged and re-passing (the collision fixed); all 654 retained fixtures across `census-0075D6`, `census-0075D6-f40d7bcc9dda` and `census-0075D6-tail` re-verified: 404 MATCH (up from 161), 250 correctly DECLINE by kind, the firing arm, the disabled arm or the other named reasons, 0 mismatches | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-player-tail5-fb408bc75597, **PASS, 20,068 hits, 1,067 fallbacks (711 kind-6 event declines, 155 z80 bank guard, 52 firing-arm declines, 47 y-minimum-step declines, 42 seam deadline, other kinds and adapter refusals the rest)**, up from 16,386 hits/2,891 fallbacks; segment_verify at boundary-6000/12000 PASS (288/1, 232/9 hits/fallbacks); mutant `player-tail-mutant-result` DIVERGENCE at frame 2,261 (artifacts/gods/verify-player-tail5-mutant, unchanged from 18 September); `camera-sprites` (forty gates) on the tree of all eight recordings: **PASS, 107,519 frames, 1,102,192 hits (135,958 seam entries, 134,839 completions), 8,575 fallbacks (2,065 kind-6 event declines, 4,470 z80 bank guard, 325 firing-arm declines, 305 vblank in span, 350 observation deadline, 496 seam deadline, 211 other-kind event declines, 133 y-minimum-step declines, 78 y-clamp declines, 146 declined arms of other regions, 27 machine admission, 4 disabled-arm declines), tree bit-exact**, up from 1,100,252 hits/14,169 fallbacks (artifacts/gods/verify-camera-sprites-tree-2026-09-18j) | 20,068 hits, 1,067 fallbacks (tree: 1,102,192 hits, 8,575 fallbacks) | this commit
2026-09-18 | 0044C0 (with its helper 004550) trail check / event kind 6, the tail's found-tile arm's second admitted event, per the task's own next step: a caller-register-free leaf (every input a fixed work-RAM address) saving and restoring all eight data registers around a bounded scan of a six-slot position-history ring (`TRAIL_HISTORY`, `FFFFF272`, 4 bytes each): the current position against each recorded slot's own 0x80-wide box, in program order, stopping at the first match ('found', 891 of 903 occurrences on the main history alone, `--classifier entry`, no overflow at `--max-classes 400`) with no RAM effect beyond an unconditional scratch copy (`TRAIL_LAST_POSITION`, `FFFFF1EC`); exhausting all six (`'exhausted'`, real ROM code -- a ring shift, a counter, a call into `007B4C` -- this module does not model, rare: 13 of 903) declines by name. Composed into `player_tail_plan` the same way kind 3 was: `_tc_position`'s event branch now dispatches on `check['handler']` (`0x00462C` or `0x0044C0`, any other kind still declined by name), and the scan continues after an admitted kind-6 raise exactly as after a kind-3 one. Getting the COMPOSITION right (the standalone `trail_check_plan` gate needed none of this) surfaced three real facts about the raiser's own preamble no prior session's kind-3-only composition had needed, each caught by `factcheck check`'s own byte-exact residue comparison, none guessed: (1) the raiser's own dispatch computes `d0 = (status - 1) * 4` (the EVENT_TABLE offset, not the raw status word) and 0044C0 never touches d0 again before its own `movem.l d0-d7,-(a7)`, so that value -- 0x14 for kind 6 -- is what its own transient frame residue must carry, not the status itself; (2) the raiser's OWN 00773A head sets D1 to the tile row (`(y & TILE_Y_MASK) << 3`, exposed as `game.player.tile_row`) unconditionally, before any position is even tested -- NOT the tail activation's own entry D1 -- so a kind-6 raise's own register-frame push needs that value threaded in, carried the same way A4 already was, across however many raises one activation makes; (3) the raiser's own dispatch (`movea.l (a1,d0.w),a1`) leaves A1 = the handler's own address (`0x0044C0`) before the `jsr`, and since 0044C0 never touches A1 itself (only absolute addressing throughout) that value rides through to the tail's own final exit untouched -- 00462C, by contrast, immediately overwrites A1 with the trigger table base, which is why this never surfaced during the kind-3-only composition. A fourth, structural defect (not a data one) was caught the same way: the composition's own register-residue dict was being REPLACED by each new raise instead of MERGED, silently losing an earlier raise's own D5/D6/A2/A3/A4/A5 residue whenever a LATER raise in the same activation touched a different, smaller subset of registers (exactly kind 6's own case, which touches only A1) -- fixed by merging per-key throughout. `_evaluator_resolve` (17 Sep's own kind-3 extraction) needed a small generalisation, not a rewrite, to expose D1 (previously deleted as unused) now that kind 6's own register-frame push needs it threaded through kind-3 raises too. Every number from the tracer: `artifacts/gods/evidence/census-0044C0{,-<node>}` (eight recordings, 62 real path classes, 0 overflow), plus 654 pre-existing `census-0075D6`/`census-0075D6-f40d7bcc9dda`/`census-0075D6-tail` fixtures re-verified end to end | RAM-only leaf, a caller-register-free box-overlap chain (`game.trail.trail_check`/`_slot_overlap`), composed into the tail's own raise the same way kind 3 was (`_evaluator_resolve` extracted 17 Sep, reused unchanged in shape) | `gods_sega.game.trail` (new module: `trail_check`, `_slot_overlap`, `TRAIL_COUNTER`, `TRAIL_HISTORY`, `TRAIL_LAST_POSITION`, `TRAIL_BOX_MARGIN`); `gods_sega.game.player.tile_row` (new, exposing 00773A's own D1 residue); plan `gods_sega.boundary.trail_check_plan` (+ `_trail_check_resolve`, `_trail_check_frame_writes`); `player_tail_plan` extended (`_tc_position`'s event branch now dispatches by handler, `_tile_scan_cost` threads D1/D5/D6 carry alongside A4, merges register residue per-key instead of replacing); candidates 'trail-check', 'player-tail', 'camera-sprites' (extended to forty-one gates); mutant `trail-check-mutant-result` (`recovery._mutate_result`) | `tests/games/gods/test_trail.py` (new: 4 unit tests plus all 62 retained fixtures across `census-0044C0{,-<node>}` -- 22 MATCH, 40 correctly DECLINE (exhaustion), 0 mismatches); `tests/games/gods/test_player_tail.py` extended (the fixture-parametrized test's own admitted-kind check widened to `{0x00462C, 0x0044C0}`); all 654 retained `census-0075D6*` fixtures re-verified: 483 MATCH (up from 404), 171 correctly decline, 0 mismatches | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames: trail-check (standalone) **PASS, 884 hits, 19 fallbacks (12 unrecovered exhaustion, 6 z80 bank guard, 1 observation deadline)** (artifacts/gods/verify-trail-check-fb408bc75597); mutant `trail-check-mutant-result` DIVERGENCE at frame 3,876 (artifacts/gods/verify-trail-check-mutant, the second witnessed occurrence -- byte-exact state comparison makes even a transient-looking store load-bearing); player-tail (composed) **PASS, 21,443 hits, 383 fallbacks (331 firing-arm declines, 158 z80 bank guard, 61 kind-6 exhaustion declines, 47 y-minimum-step declines, 49 seam deadline, small per-kind counts the rest)**, up from 20,068 hits / 1,067 fallbacks (artifacts/gods/verify-player-tail6-fb408bc75597); mutant `player-tail-mutant-result` DIVERGENCE at frame 2,261 (unchanged, artifacts/gods/verify-player-tail6-mutant); `camera-sprites` (all forty-one gates) on the tree of all eight recordings: **PASS, 107,519 frames, 1,106,185 hits, 6,607 fallbacks (331 firing-arm declines, 4,478 z80 bank guard, 61 kind-6 exhaustion declines, the kind-6 unwitnessed-event fallback reason entirely gone), tree bit-exact**, up from 1,102,192 hits / 8,575 fallbacks (artifacts/gods/verify-camera-sprites-tree-2026-09-18l) | 884 hits (standalone), 21,443 hits (composed player-tail), 383 fallbacks (tree: 1,106,185 hits, 6,607 fallbacks) | this commit
2026-09-18 | states 1 (007282), 0 (006FFE), 14 (006DA6) / next-bites survey after the movement-cluster family | investigation, no escalation code needed (not NEW_GODS_SUBSYSTEM: nothing unnamed, nothing undiscovered; not unbounded: no data-dependent loop) | - | - | - | - | `docs/gods/blockers/2026-09-17-008222.md` addendum, "18 September (continued)": states 1 and 0 censused over the main history (`--classifier entry`, `--max-classes 4000`, 1,171 real path classes for state 1 alone -- confirmed by inspection to be the ALREADY-RECOVERED shared tail's own downstream diversity accumulating through the census's full-activation path signature, not 1,171 independent state-1 behaviours: grouped by depth-zero `bsr` target alone, state 1 has only three real shapes, `(0063FA)` 2,970/3,181, `(0063FA,008222)` 204/3,181, `(0063FA,00722C)` 7/3,181) and fully disassembled (recursive-descent capstone, cross-checked against eleven `factcheck.py facts --path` traces and four `branches --vary` sweeps): ~130 instructions, 14+ conditional branches, all reachable, none of it a small leaf in the recipe-6a sense.  State 0 disassembled in full too (same size, same shape, its own constants and its own copy of a shared sub-body state 1 instead reaches by jumping into ROM immediately after state 0's own body, `007208`-`007228`).  State 14 (`006DA6`) disassembled far enough to confirm the same size and shape.  Two findings recorded: a branch at `007386` (state 1's own `00735C bmi.b`) is PROVABLY UNREACHABLE from state 1's gate (confirmed by `branches --vary FFEA20.w=0xFFFF`, not inferred) since an earlier test on the SAME word already excludes the sign this branch tests; and state 1 and state 5 (`00746A`) are real siblings sharing code both ways (`00746A`'s own `007496 bra.w $7282` falls back into state 1's body after calling the now-recovered `012DA0`), confirming `STATUS.md`'s own "one region, two gates, one planner" framing -- state 1's own gate does not need `00746A` modelled, but recovering the two together is the natural next shape.  No source file changed; the addendum is a map for the next bite (state 1's own full decision tree is transcribed there), not a plan | this commit (docs only, no `src/` change)
2026-09-18 | 007282 state 1 / the player state machine's own dispatch table entry 1, per the task's own next bite after the "18 September (continued)" survey: a ~130-instruction decision tree over the already-recovered grid cell (0063FA), contact search (008222) and the shared tail (0075D6, a separately-armed gate this plan hands off to by ending at `pc = 0x0075D6` -- the same composition boundary `movement_hit_state`'s own states 24/25 already draw).  Every arm the main history witnesses is recovered: two immediate "wall" transitions (state 0xC/0x3), a "jump-start" transition to state 9 (both `FFFFEA20` sub-arms), a contact-search-gated cascade whose 'not found' result (or `EA23` bit 2 clear directly) reaches a shared movement cascade (`FFFFF182`-gated, then either three row-stride grid tests -- the SAME `tile_trigger_scan` pattern -- or a five-instruction shared sub-body only state 1 reaches, `007208`), and a contact-search 'found' result that hands off deterministically into state 5's own dispatch read (`00749A`-`0074B4`, ending at `pc = 0x0075DA`, skipping the tail's own first instruction already done).  A STATE_COUNTER already above 7 reaching the cascade's own reset (`007448`) is witnessed (8 of 3,181, always 0x2D -- the shared sub-body's own reset arm is the only place that stores a counter this large) and modelled too: the +4 already applied to POSITION_X is undone and the counter masks back to 0.  Only the box-overlap scan (`0072D8`, `FFFFEA1E == 1`, 7 of 3,181) declines, by name, unrecovered here (`game.movement.box_overlap_scan` exists but is not yet composed).  Census over `fb408bc75597` (`--classifier entry`, `--max-classes 4000`, 1,171 real path classes) verified via `pathfacts.trace(..., stop_pc=plan.registers['pc'])` per the addendum's own finding that the census over-counts the verification surface (the ALREADY-recovered tail's own downstream diversity): grouped by the plan's own exit, only three real shapes. `contact_search_plan` refactored into `_contact_search_resolve` (mirroring `_cc_resolve`) so a nested BSR composes it the way `_movement_hit_plan` already does. The generic "flip the last write" mutant crashes the machine on two arms (`'shared-unchanged'`/`'shared-reset'`, whose own semantic stores are empty, so the mutated byte is stack scratch from the composed grid_cell call, later read back as an address); `_mutate_state1_counter` (d7 off by one) is real on every arm instead, since the shared tail's own first instruction stores it into `FFFFF190` unconditionally | costed one instruction-block at a time from the tracer (eleven `factcheck.py facts --path` traces plus targeted `branches --vary` sweeps), the shape `008222`/pickup-check already established for a branchy region | `gods_sega.game.player` extended (`state1_step`, `state1_cascade`, `state1_handoff`); plan `gods_sega.boundary.state1_plan` (+ `_s1_gate_cost`, `_state1_cascade_cost`, `_state1_grid_cost`); `boundary.contact_search_plan` refactored (`_contact_search_resolve` extracted, behaviour unchanged: all 271 `test_contact_search.py`/`test_contact_consume.py` fixtures re-verified MATCH); candidates 'state-1', 'camera-sprites' (extended to forty-two gates); mutant `state-1-mutant-result` (`recovery._mutate_state1_counter`) | `tests/games/gods/test_state1.py` (new: 15 unit tests covering every arm plus all 1,171 retained fixtures across `census-007282` -- 1,164 MATCH, 7 correctly DECLINE (box-overlap), 0 mismatches) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-state-1-fb408bc75597, **PASS, 3,152 hits, 29 fallbacks (20 z80 bank guard, 7 box-overlap declines, 2 observation deadline, all exact or declined)**; segment_verify at boundary-6000/12000 PASS (24/1, 33/1 hits/fallbacks, the one fallback each time the box-overlap decline); mutant `state-1-mutant-result` DIVERGENCE at frame 2,284 (artifacts/gods/verify-state-1-mutant), the first frame the region is exercised; `camera-sprites` (all forty-two gates) on the tree of all eight recordings: **PASS, 107,519 frames, 1,105,568 hits, 6,703 fallbacks (4,545 z80 bank guard, 503 seam deadline, 353 observation deadline, 331 firing-arm declines, 305 vblank in span, 99 at the new gate itself, the rest small per-kind counts), tree bit-exact** -- the hit/fallback split shifted from the prior PASS (1,106,185/6,607) because the adapter's own span-refusal conditions (z80 bank guard, deadlines) now apply to state 1's own earlier gate on some occurrences that previously reached `player-tail`'s gate cleanly, an exact effect of gate placement, not a correctness change | 3,152 hits, 29 fallbacks (tree: 1,105,568 hits, 6,703 fallbacks) | this commit
2026-09-18 | 006FFE state 0 / the player state machine's own dispatch table entry 0, per the task's own next bite after state 1: NOT a byte-identical copy of state 1 (the "move left" counterpart of state 1's "move right") -- real differences confirmed by the tracer, not assumed by symmetry, transcribed in `game/player.py`'s own module note above `state0_step`: (1) arm A compares FFFFEA20 to the LITERAL 1, not its sign, so a negative EA20 still reaches every later branch (state 1's own arm A already proves EA20 >= 0); (2) the bit-0 sub-arm's own EA20-positive case reaches a three-way grid-byte dispatch (0x710A-0x007150) state 1 has no analogue of at all, landing on state 14 (two ROM entry points, only one also advancing POSITION_X by 0x20 first) or falling into the shared cascade untransitioned -- EA20 negative and EA20 zero both still converge into the SAME state-8 tail state 1's own bit-0 arm uses; (3) the cascade's own low-bits gate compares POSITION_X's low 5 bits to 0 (not 8), the grid tests read offsets -1/+0x7F/+0xFF (not +1/+0x81/+0x101) and DECREMENT POSITION_X (not increment); (4) the shared sub-body is state 0's own physical inline copy at 0071E6 (not a jump into state 1's own 007208), and its own negative-EA1E arm sign-extends d7 to the FULL 32-bit register (`moveq #$ff,d7` = 0xFFFFFFFF, not state 1's `moveq #$1,d7`) -- caught by `factcheck check`'s own byte-exact register comparison, not guessed, and modelled with a `'d7_full'` field the generic upper-half-preserving merge skips. The contact-search-found hand-off is the SAME deterministic tail-let state 1 uses, entered one instruction earlier (`0074A2`, forcing STATE_INDEX to 6, not 5) -- `_handoff` extracted from `state1_handoff` to share it. Every arm the main history witnesses is recovered; only the box-overlap scan (`007056`, `FFFFEA1E == 1`, 13 of 2,144 occurrences) declines, unrecovered. Two real bugs caught by `factcheck check`, not guessed: the cascade's own EA20 sign test (`00716E tst.w ea20`) is a SEPARATE instruction from the branch that reads it (`007172 bpl.w`), missed on the first draft (12 cycles); and the shared-sub-body's own three arms were never actually reached in the boundary's first draft at all (a placeholder `'shared-sub-gate'` result was left unresolved, silently leaving d7 unchanged on every fixture that should have taken it) | costed one instruction-block at a time from the tracer (a fresh set of `factcheck.py facts --path` traces plus `branches --vary` sweeps on `census-006FFE/006FFE-entry-p0.state`, plus real fixtures for arms a single parked state cannot reach), the same shape state 1 used -- no cost or branch assumed from state 1's own constants | `gods_sega.game.player` extended (`state0_step`, `state0_cascade`, `state0_shared_sub`, `state0_handoff`; `state1_handoff` refactored to share `_handoff`); plan `gods_sega.boundary.state0_plan` (+ `_s0_gate_cost`, `_state0_cascade_cost`, `_state0_grid_cost`); candidates 'state-0', 'camera-sprites' (extended to forty-three gates); mutant `state-0-mutant-result` (`recovery._mutate_state0_counter`, the same d7-off-by-one shape state 1's own mutant uses, for the same reason -- the generic mutant would be blind or crash on the shared-sub-body's own empty-store arms) | `tests/games/gods/test_state0.py` (new: 13 unit tests covering every arm plus all 879 retained fixtures across `census-006FFE` -- 866 MATCH, 13 correctly DECLINE (box-overlap), 0 mismatches) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-state-0-fb408bc75597, **PASS, 2,114 hits, 30 fallbacks (16 z80 bank guard, 14 box-overlap declines, all exact or declined)**; segment_verify at boundary-6000/12000 PASS (42/0, 8/3 hits/fallbacks, the three fallbacks all box-overlap declines); mutant `state-0-mutant-result` DIVERGENCE at frame 2,400 (artifacts/gods/verify-state-0-mutant), the first frame the region is exercised; `camera-sprites` (all forty-three gates) on the tree of all eight recordings: **PASS, 107,519 frames, 1,105,086 hits, 6,812 fallbacks (116 at the new gate, all exact or declined), tree bit-exact** -- the hit/fallback split shifted again from the prior PASS (1,105,568/6,703) for the same exact reason state 1's own milestone recorded: the adapter's own span-refusal conditions now apply to state 0's own earlier gate on some occurrences | 2,114 hits, 30 fallbacks (tree: 1,105,086 hits, 6,812 fallbacks) | this commit
2026-09-18 | 006DA6 state 14 / the player state machine's own dispatch table entry 14, per the task's own next bite after states 1 and 0: a vertical-movement dispatcher (states 0/1 are horizontal) over the SAME recovered pieces (grid cell 0063FA, contact search 008222, shared tail 0075D6) plus its own "settle" tail (STATE_COUNTER wraparound at 0x14, fresh or carried from `FFFFEA1E < 0`).  Composed and verified in two passes because the FIRST pass's own verification ladder caught real gaps a single-history census cannot see.  Pass one (204 retained fixtures across `census-006DA6` on `fb408bc75597`, 183 real path classes): every arm the tail's own gate reaches composed, including two forks the "18 September (continued)" survey had wrongly declined -- the settle tail's own `FFFFF1AE != 0` continuation (`006F84` onward) IS witnessed (an odd prior sound-trigger count carried into settle) and composes as `'probe-f1ae'`/`'loopback'` (a provably-bounded, at-most-two-pass loop, `_state14_settle_cost`'s own capped iteration); and the settle tail's own `FFFFEA20 != 0` fork (`'rejoin-main'`) hands back to the SAME EA20 arm dispatch the top-level 'main' path already owns, composed via a new shared helper (`boundary._state14_main_dispatch`, extracted so both callers use ONE cost/register model) rather than declined or duplicated.  Only arm B's own `+0x17F` nibble sub-test still declined by name (thought unwitnessed).  `factcheck check` MATCH on all 204 fixtures, 0 mismatches -- but the coordinator's own rule ("push only after a tree PASS") caught what the single-history census could not: a full `camera-sprites` tree run across all eight recordings DIVERGED (a cycle-count-only mismatch, same video frame, at frame 16,934 on a DIFFERENT recording, `7251bbd0ecf7…`, never censused for this region).  Pass two: censused `006DA6` fresh over all seven OTHER recordings (622 more fixtures, largest node 187 fixtures) and iterated `factcheck check` to zero mismatches across all 1,050 fixtures, finding and fixing four real bugs none of the single-history evidence could expose: (1) arm B's own `'nibble-declined'` was a MISREAD of the disassembly, not a genuine decline -- `006EB0`'s own `beq.b $6ec4` (matched) mirrors arm A's own detour exactly, and the NOT-taken case falls straight into `'transition-0'`'s own body, exactly like arm A's own `'transition-1'` fallthrough; (2) `FFFFF1A4`'s own unconditional clear (`006DFA`, runs before the retry-budget compare even looks at its own result) was missing from three of arm A's own downstream stores (masked in the single-history census because F1A4 happened to already be 0 there); (3) arm B has NO analogous re-clear past its own retry budget (unlike arm A's own `006E0A` re-clearing F1A6) -- F1A4 only ever increments there, so three of arm B's own downstream stores wrongly hardcoded zero instead of carrying the incremented value forward (caught by a real F1A5 byte holding 6, not 0); (4) `006E2A`/`006EA0`'s own `move.w f18e,d0;andi.w #$f,d0` overwrites D0 with the low nibble before EITHER arm's own nibble-tested exit -- missed on `'contact-gate-nibble'` for both arms (only the `'transition-1'`/`'transition-0'` exits had carried a `'d0'` override); plus two composition-level bugs in `_state14_main_dispatch`/`_state14_settle_cost` itself: the `'rejoin-main'` handoff left D7 unset (relying on it equalling the PLAN's own entry D7, false whenever settle's own reset path had already forced D7 to 0 first) -- fixed by setting D7 as the dispatch's own baseline exit register from its own live entry value; and the settle probe's own `'exhausted'` exit assumed grid_cell's own `asl.w` (deep inside an earlier `bsr`) was the last flag-setter, when `_S14S_EXHAUST_TAIL` is actually three instructions and the SECOND move (`f1b2,f18e`, restoring the original POSITION_Y) runs last.  A fifth, smaller bug (`_S14B_NIBBLE_BEQ`'s own taken/not-taken costs transcribed backwards) surfaced during the same iteration.  All five found by a real trace outside the single-history census, none guessed.  With every arm composed (no decline left at all), added `state-14` to the `camera-sprites` composite and a `_mutate_state14_counter` (d7 off by one, the same shape states 0/1 use, verified not blind/crashing first) | costed one instruction-block at a time from the tracer (the SAME recipe states 0/1 established for a branchy dispatcher), reused via a NEW shared dispatch helper (`_state14_main_dispatch`) rather than a duplicated cost path for the rejoin | `gods_sega.game.player` extended (`state14_step`, `state14_arm_a`, `state14_arm_b`, `state14_contact`, `state14_contact_found`, `state14_arm_d`, `state14_settle`, `state14_settle_probe`); plan `gods_sega.boundary.state14_plan` (+ `_state14_main_dispatch`, `_state14_contact_cost`, `_state14_arm_d_cost`, `_state14_settle_cost`, ~50 `_S14*` cost constants); candidates 'state-14', 'camera-sprites' (extended to forty-four gates); mutant `state-14-mutant-result` (`recovery._mutate_state14_counter`) | `tests/games/gods/test_state14.py` (new: 24 unit tests covering every arm including both mirrored nibble sub-tests, plus all 204 retained fixtures across `census-006DA6` on `fb408bc75597` -- all MATCH, 0 declines, 0 mismatches; a further 846 fixtures across the other seven recordings checked ad hoc via the SAME `factcheck check` machinery during debugging, also 0 mismatches) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-state-14-fb408bc75597, **PASS, 1,146 hits, 9 fallbacks (all z80 bank guard, exact adapter refusals)**; segment_verify at boundary-6000/12000 PASS (0/0 hits, too rare for a 300-frame window); mutant `state-14-mutant-result` DIVERGENCE at frame 2,688 (artifacts/gods/verify-state-14-mutant), the first frame the region is exercised; `camera-sprites` (all forty-four gates) on the tree of all eight recordings: **PASS, 107,519 frames, 1,106,820 hits, 6,827 fallbacks (29 at the new gate, all z80 bank guard, 0 unsupported domain), tree bit-exact** (artifacts/gods/verify-camera-sprites-tree-2026-09-18o) -- the FIRST tree attempt on this region DIVERGED (frame 16,934 on `7251bbd0ecf7…`, cycle-count only), fixed by the census-and-fix cycle above before this PASS | 1,146 hits, 9 fallbacks (tree: 1,106,820 hits, 6,827 fallbacks) | this commit
2026-09-18 | 00746A state 5 / the player state machine's own dispatch table entry 5, the next bite by frequency after states 1/0/14, per the 17 September Decision's "one region, two gates, one planner" -- ALSO reached by a `bra.w` fallthrough from inside state 1's own body (`0073E0`).  A counter under 5 (a counter that becomes exactly 3 also calls the already-recovered contact-consume primary `012DA0`) always takes a deterministic table hand-off into the shared tail (`pc = 0x0075DA`); a counter of 5 or more gates on `FFFFEA23` bit 2 -- clear, or a contact-search (`008222`) 'not found' result, falls back into state 1's own gate (`pc = 0x007282`, hands off rather than inlining); a contact-search 'found' result takes the same table hand-off through its own index-0 entry.  Fixed a real defect in the shared `_cc_resolve` (012DA0/012E5A contact-consume composition): a later found slot whose own type is 9 (never touches d6) unconditionally reset the caller's own d6 to `None`, discarding an earlier found slot's real value -- unwitnessed until state 5's own multi-slot-found activations exercised it | costed one instruction-block at a time from the tracer on real fixtures over `census-00746A-*` | `gods_sega.game.player` (`state5_step`, `state5_handoff`, `state5_fallback_stores`); plan `gods_sega.boundary.state5_plan`; candidates 'state-5', 'camera-sprites' | `tests/games/gods/test_state5.py` (1,101 fixtures across all five recordings, 21 correctly-declined contact-consume types 11/15, 0 mismatches) | ca2b703b6fd595a8d8bd0109ff67f09511d1cda614c44e3c93aca4bfa215ced8, 14,583 frames, artifacts/gods/verify-state-5-ca2b703b6fd5b, **PASS**; mutant `state-5-mutant-result` (`_mutate_outcome` -- the generic "flip the last write" mutant corrupted STATE_COUNTER, whose own live register feeds the shared tail's own state-table re-index one instruction later and faulted the machine outright on a real fixture) DIVERGENCE at frame 2,583; `camera-sprites` tree PASS, 107,519 frames, bit-exact | 454 hits, 6 fallbacks (all z80 bank guard) | 683e22b, 563afe9 (the `_cc_resolve`/mutant/upper-half fixes landed together with state 6)
2026-09-18 | 0074C2 state 6 / a byte-for-byte mirror of state 5's own shape (`game.player.state6_step`), ALSO reached by a `bra.w` fallthrough from inside state 0's own body (`state0_handoff`, `0074A2`).  Calls the secondary contact-consume routine (`012E5A`) instead of the primary, and its own fallback lands on state 0's own gate (`STATE_INDEX` cleared to 0, `pc = 0x006FFE`) instead of state 1's; the handoff and contact-search-gate arms reuse the exact shared code state 5's own entry reaches (`_state56_handoff_stores_and_cost`, renamed from `_state5_...` since it is not state-5-specific).  Picked up the new `--perturb-upper-halves` check (landed on main mid-session) and found two more real defects: the state5/6 handoff's own 'found' sub-arm (`0074A8`'s own `moveq #0,d7`) and contact_search's own 'busy' arm (`moveq #1,d0`) both execute a FULL 32-bit clear; the boundary was merging the caller's own entry upper half onto them instead.  Also fixed the same defect class in state0_plan/state1_plan/state14_plan (their cascades' 'f182-set'/'position-advance'-no-overflow/'shared-unchanged' arms store D7 through a word-sized op, the entry's own upper half survives, while the MOVEQ-based arms need a full clear -- `_cascade_exit_d7`, shared between states 0 and 1) and in state14's own dispatch baseline/settle tail (a `d7_register` the caller now decides: 0 on a fresh settle reset, the outer entry's own full register otherwise) | costed one instruction-block at a time from the tracer, reusing state 5's own `_S5_*` constants (confirmed byte-identical in cost against real traces) | `gods_sega.game.player` (`state6_step`, `state6_fallback_stores`); plan `gods_sega.boundary.state6_plan`; candidates 'state-6', 'camera-sprites' | `tests/games/gods/test_state6.py` (755 fixtures, 16 correctly-declined, 0 mismatches); state0/state1/state14's own fixtures re-checked under `--perturb-upper-halves` after the fix (1,171/879/204 fixtures, 0 mismatches each) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887 (via ca2b703b6fd5 for the leaf seal), 14,583 frames, artifacts/gods/verify-state-6-ca2b703b6fd5, **PASS**; mutant `state-6-mutant-result` (`_mutate_outcome`, the same reasoning as state 5's own) DIVERGENCE at frame 2,441; MILESTONE SEAL: `tree_verify.py --candidate camera-sprites` (thirty-one gates, five leaves cold) PASS, 107,519 frames, bit-exact, artifacts/gods/verify-camera-sprites-leaves-2026-09-18b | 442 hits, 4 fallbacks (all z80 bank guard) | 563afe9 (pushed)
2026-09-18 | 0066A8 state 9 / a falling/jump-arc physics dispatcher, paired with state 8 (not yet recovered) the same way 5/6 and 0/1 are: shares a jump-arc velocity table (`006414`, byte-offset-indexed by the fall timer `FFFFF19C`) and two small grid-probe leaves (`006442`/`006468`, right before state 8's own entry `00648C`) with the SAME shape state1_step's own gate test already uses, parameterised on the row bias (`_row_gate_open` in `game/player.py`, reused not copied).  The head normalizes STATE_COUNTER; an inline grid-block test gates an X-advance by F196; two EA1E-gated landing checks (state 14, witnessed 15 times -- a real bug in the first pass required F196 == 0 as part of the landing gate, but `0066371E beq.b $6726` SKIPS the tracked-X test on F196 == 0 rather than declining; state 13, UNWITNESSED by any of 1,287 retained fixtures, declined by name); a ground-ahead probe before the fall step (state 16); the fall step itself (POSITION_Y -= the table's own entry, doubled if `FFFFF19E` is set -- unwitnessed, declined) followed by a near-row probe (state 12) then a re-try of the ground-ahead probe (state 16 again); failing both, a trigger gate (EA23 bit 2, the already-recovered `contact_search`, F19C short of the cap) transitions to state 20, or the fall timer just advances, unchanged (still state 9) or forcing a landing (state 12) once it caps.  Six real defects the FAST tier's own iteration caught before any of this reached the tree: stack residue missing for every internal grid_cell/row-gate bsr; F19x-family constants needing `& 0xFFFFFF` before use as a `_bytes()` address (unlike STATE_INDEX/POSITION_X/POSITION_Y); the row-gate's own inner `bsr.b $63fa` itself uncounted, and its own direct-match test costing 12 (register-indirect) vs 16 (displacement) depending on the probe; A0/D0/D1 left at a stale grid_cell result instead of the row-gate's own internal recomputation; D1's own `moveq #0/#1` being a full 32-bit clear the boundary was merging instead; arm A's own decline falling through into arm B's own head (re-testing the already-negative EA1E) at a real extra cost; two tails one tuple short of their own final `bra.w $75d6`; and the terminal arm's own last flag-setter being `006880`'s `moveq #0,d7`, not the earlier `move.w #$c,f192.w` | costed one instruction-block at a time from the tracer on real fixtures covering every witnessed arm | `gods_sega.game.player` (`state9_step`, `state9_fall_tail`, `_state9_head`, `_row_gate_open`, `_state9_ground_stores`); plan `gods_sega.boundary.state9_plan` (+ `_row_gate_cost`, `_state9_head_cost`); candidates 'state-9', 'camera-sprites' | `tests/games/gods/test_state9.py` (1,287 fixtures across all five recordings, 0 declines under the plain entry -- landing-13/F19E-doubled are real ROM code never witnessed but never actually raised in any retained fixture either -- 0 mismatches) | ca2b703b6fd595a8d8bd0109ff67f09511d1cda614c44e3c93aca4bfa215ced8, 14,583 frames, artifacts/gods/verify-state-9-ca2b703b6fd5, **PASS**; mutant `state-9-mutant-result` (`_mutate_outcome`, the same STATE_INDEX/D7-feeds-an-unbounded-table hazard states 5/6 already route around) DIVERGENCE at frame 2,327; MILESTONE SEAL: `tree_verify.py --candidate camera-sprites` (thirty-two gates, five leaves cold) PASS, 107,519 frames, bit-exact, artifacts/gods/verify-camera-sprites-leaves-2026-09-18c | 291 hits, 2 fallbacks (both z80 bank guard) | 026f6b0 (pushed)
2026-09-18 | 0069AC state 26 / a near-twin of state 9's own falling shape, reusing the SAME jump-arc table and the SAME two row-gate leaves verbatim (confirmed byte-identical in cost against real traces, not assumed).  Three real differences: the head normalizes STATE_COUNTER from `FFFFF1BA` (a flag), not F19C's own value; the X-advance has no F19C gate at all, and -- unlike state 9 -- the grid cell is NOT re-read after it (`0069EE`'s own `cmpi.w #$16,f19c.w` follows the X-advance's own `add.w` directly, no second `bsr.w $63fa` in between); and the re-check ground-ahead probe uses `STATE26_RECHECK_GATE` (0x12), not `STATE9_TRIGGER_GATE` (0x16, still used by the first probe).  In place of state 9's own trigger gate, state 26's own tail is D7-based: D7 at or past 3 hands off straight into state 9 (D7 forced to 5, STATE_INDEX to 9 -- state 9's own head is a no-op on this hand-off); short of 3, D7 == 1 also calls the already-recovered contact-consume primary (`012DA0`) first; both tails then advance F19C the same way `state9_fall_tail`'s own countdown/terminal arms already do (reused verbatim).  Three real defects the FAST tier caught: an earlier draft copied state 9's own two-grid_cell-call shape here by assumption, adding a whole extra `bsr.w $63fa` the real ROM does not have; the already-recovered contact-consume primary's own `_consume_position` reads GRID_X/GRID_Y live, needing a reader reflecting THIS activation's own X-advance and fall step; and two real 68000 encoding differences from state 9's own shape -- the low-bits grid-block gate's own `bge.b` cost was only charged on the "low >= 8" arm, never "low < 8"; and the landed check's own not-found branch is a `beq.w` (word displacement) here, not state 9's own `beq.b`, so its own not-taken cost is 12, not 8 | costed one instruction-block at a time from the tracer, reusing state 9's own cost constants wherever the shape is genuinely identical | `gods_sega.game.player` (`state26_step`, `state26_tail`, `_state26_head`); plan `gods_sega.boundary.state26_plan`; candidates 'state-26', 'camera-sprites' | `tests/games/gods/test_state26.py` (447 fixtures across all five recordings, 13 correctly-declined contact-consume types 11/15, 0 mismatches) | ca2b703b6fd595a8d8bd0109ff67f09511d1cda614c44e3c93aca4bfa215ced8, 14,583 frames, artifacts/gods/verify-state-26-ca2b703b6fd5, **PASS**; mutant `state-26-mutant-result` (`_mutate_outcome`) DIVERGENCE at frame 5,675; MILESTONE SEAL: `tree_verify.py --candidate camera-sprites` (thirty-three gates, five leaves cold) PASS, 107,519 frames, bit-exact, artifacts/gods/verify-camera-sprites-leaves-2026-09-18d | 56 hits, 2 fallbacks (both z80 bank guard) | 42b78c1 (pushed)
2026-09-18 | 005700 player state machine / semantic-operation card, per the task's own next step after state 14: `005700` is not a new blocker -- the 17 September escalation was the shared tail alone, since resolved as `player-tail`, and the states composed over it since (1, 0, 14, plus the movement-cluster pair 24/25) confirm the supervisor's own 17 September Decision that `005700` becomes "the family over FFFFF192" once the tail is admitted, the SAME per-state composite shape `achievement-slot-dispatch` already is over `0047DA`/`004800`.  Wrote the family's own semantic-operation card in `STATUS.md` per `docs/common/recovery-process.md`'s own card fields (semantic operation; entry/exit boundary; persistent state; external observations; pending effects; permitted interference; proven movable events; required ordering boundaries; timing dependency; evidence and scope; remaining blocker), naming: the dispatch's own read of `STATE_INDEX`/`STATE_COUNTER` and 29-entry table `005618`; the "one gate hands off to a separately-armed gate" boundary at `0075D6` every recovered state already draws (and the one-instruction-in `0075DA` variant states 0/1's own contact-search-found arms use); the per-state persistent fields already named in `game/player.py`; that the family has no platform interaction, no pending effects and no timing dependency of its own (all of that lives inside the already-recovered tail or in still-unrecovered event cascades); the full-history `FFFFF192` frequency tally from the 17 September blocker doc (34,904 activations on `fb408bc75597`, every state's own count, states 7/15 unwitnessed); and the remaining blocker as pure volume -- state 5 (1,492, a real sibling of state 1 sharing code both ways, the next bite) then twenty-three more witnessed states in frequency order, each needing the SAME recipe states 0/1/14 established.  No source file changed; also updated the stale 17 September narrative paragraph immediately above the card (which still read "No state's own composition is admissible until this tail is [recovered]") to point at the card instead of leaving a contradicted blocker standing | documentation only, no escalation code needed (005700 is not NEW_GODS_SUBSYSTEM in its own right; the card records why) | - | - | - | - | STATUS.md's own "player state machine" section and its new semantic-operation card subsection | this commit (docs only, no `src/` change)
2026-09-18 | 00648C state 8 / a falling/jump-arc physics dispatcher, state 9's own sibling sharing the SAME jump-arc velocity table (STATE9_FALL_TABLE) and the SAME two row-gate leaves (006442/006468, `_row_gate_open`) verbatim -- confirmed byte-identical against the ROM for two of its own five arms (landing-14/landing-13 are literal copies of state9_step's own arm A/B, each its own physical duplicate at a different address, differing only in the final branch's own displacement).  Four real differences from state 9's own shape: the head's own resting value is 6 (not 5) and its own addq target is 5 (not 4) -- otherwise the SAME four-arm normalization (`_state8_head`); the block test gates on low5 == 0 (not state 9's low5 < 8) and checks the LEFT neighbour's offsets -1/0x7F/0xFF (not +1/+0x81/+0x101), with one fewer instruction in its own gate (no `cmpi.w #$8,d0`, a direct `bne` on the `andi` result); the ground/landed/trigger targets are states 17/11/21 (not state 9's 16/12/20), and the re-check ground-ahead probe (after the fall step) uses its own gate, 0x12 (not state 9's own STATE9_TRIGGER_GATE, 0x16, which the FIRST probe still uses -- the SAME numeric value STATE26_RECHECK_GATE uses, coincidentally or not, but state 8's own constant); and -- unlike state 9's own declined arm -- 'landing-13' IS witnessed here (one real occurrence, `f40d7bcc9dda...`), recovered rather than declined.  A real defect the FAST tier caught before this reached the tree: the landing-13 arm's own first cell test is a BEQ (taken = found directly, falling to a second test only when not taken), not arm A's own BNE (taken = decline) -- an inverted branch-cost lookup undercounted the taken cost by 2 cycles on the one fixture that witnesses this arm, caught by `factcheck check`'s own byte-exact cycle comparison | costed one instruction-block at a time from the tracer, reusing state 9's own cost constants verbatim wherever the ROM bytes or the instruction/addressing shape are identical (the whole head, both landing tails, both ground/landed/terminal/trigger tails, the row-gate leaves and the contact-search composition) -- only the block test's own two-instruction gate and the landing-13 arm's own second cell test needed new constants | `gods_sega.game.player` extended (`state8_step`, `state8_fall_tail`, `_state8_head`, `_state8_ground_stores`); plan `gods_sega.boundary.state8_plan` (+ `_state8_head_cost`, `_S8_P1_HEAD`, `_S8_P1_BNE_LOW`, `_S8_B_CMPI_CELL80`, `_S8_B_BNE_CELL80`); candidates 'state-8', 'camera-sprites' (extended to forty-nine gates); mutant `state-8-mutant-result` (`recovery._mutate_outcome`, the same STATE_INDEX/F19C/D7-feeds-an-unbounded-table reasoning as states 9/26's own) | `tests/games/gods/test_state8.py` (new: unit tests for the head/tail/ground-stores plus all 1,170 retained fixtures across `census-00648C` on all five recordings, 0 mismatches, 0 declines; `factcheck check --perturb-upper-halves`: 1,152 MATCH, 0 mismatch, 18 SKIPPED -- adapter-refused constructions) | ca2b703b6fd595a8d8bd0109ff67f09511d1cda614c44e3c93aca4bfa215ced8, 14,583 frames, artifacts/gods/verify-state-8-ca2b703b6fd5, **PASS, 300 hits of 302 gates, 2 fallbacks (both z80 bank guard, exact adapter refusals)**; segment_verify at boundary-6000/12000 PASS (23/0, 0/0 hits/fallbacks); mutant `state-8-mutant-result` DIVERGENCE at frame 2,709; MILESTONE SEAL: `tree_verify.py --candidate camera-sprites` (forty-nine gates, five leaves cold) PASS, 107,519 frames, 1,098,503 hits, 6,874 fallbacks (18 at the new gate), bit-exact, `artifacts/gods/verify-camera-sprites-leaves-2026-09-18e` | 300 hits, 2 fallbacks (tree: 1,098,503 hits, 6,874 fallbacks) | this commit
2026-09-18 | 006B4E state 13 / state 14's own counterpart, a vertical-movement dispatcher.  The contact-search gate (006C62-006CC5) is byte-identical to state 14's own (006EC4-006F27, confirmed against the ROM save for relocated branch displacements) and reuses the SAME shared composition (`_state14_contact_cost`/`player.state14_contact`/`player.state14_contact_found`); the two jump-start tails (0x6FB8 into state 8, 0x6FDA into state 9) are the SAME physical ROM addresses state 14's own arm A/B jump into, not copies.  Real differences from state 14's own shape, all confirmed by a fresh disassembly and `factcheck.py facts --path` on real fixtures over `census-006B4E-*` (four recordings; the fifth, `ca2b703b6fd5`, never reaches this entry): no `FFFFF1B0` store and no separate `FFFFEA1E`-sign gate before the `FFFFEA20` dispatch -- the "settle, carrying the counter" hand-off folds into the `FFFFEA20 == 0` arm itself (`FFFFEA1E == 1` there, not state 14's own `< 0` test one level up); arm A/B's own jump-start gate is `FFFFEA1E`'s SIGN (not state 14's own `FFFFEA23` bit 0) -- arm A's own `'transition-9'` is UNWITNESSED by any of the four recordings that reach state 13 at all and declines by name, unlike arm B's own `'transition-8'` which is witnessed; arm D's own toggle gate is `FFFFEA1E >= 0` / `< 0` (a sign test, `tst`+`bpl`, not state 14's own exact `== 1`, `cmpi`+`bne`) and toggles INTO state 14 (the mirror of state 14's own toggle into 13); and the settle tail's own retry probe is NOT state 14's own "always exits unchanged or loops" shape -- it tests `+0x180(a0)` for a BLOCKING byte (value 1, not state 14's own solid-ground value 2) and, on a miss, tests `+0x100(a0)`/`+0x180(a0)` for solid ground -- a genuine three-way result (blocked: state 26; ground found: exit unchanged; neither: state 10).  Four real defects the FAST tier caught before this reached the tree: (1) the landing-13-shaped arm A/B retry-budget-exceeded continuation (grid match, nibble match, or plain transition) leaves `FFFFF1A6` (arm A) at its own INCREMENTED value, not state 14's own re-cleared 0 -- state 13's ROM has no `clr.w f1a6.w` before its own grid_cell call at all, confirmed by a real trace (`FFFFF1A7` held 6, not 0); (2) arm A's own f1a6-budget-not-exceeded arm falls through into arm B's own entry (`006BF0`, its own `tst.w ea20;bpl.w` always taking the positive branch since `FFFFEA20 == 1` in arm A) rather than a dedicated detour -- missed on the FIRST draft's own cost, undercounting by one `tst`/`bpl` pair every one of arm A's own decline paths (budget, grid match, nibble match) takes; (3) the settle probe's own "exit unchanged" arm still commits `POSITION_Y`'s own `+6` advance (`006CFA`'s own `addq.w #6,f18e.w` is an UNCONDITIONAL memory add, unlike state 14's own register-based, saved/restored step) -- missing this write was the first real MISMATCH the FAST tier caught; (4) the settle tail's own `'loopback'` pass re-reads `FFFFF1AE` LIVE from RAM instead of threading the just-toggled value the way `state14_settle`'s own `f1ae_override` parameter does -- without it the plan's own second pass never sees the toggle and loops past the three-pass defensive cap, raising `UnsupportedCandidate` on a real 120-frame segment a census snapshot alone never exercised.  A fifth, smaller bug (the settle probe's own "blocked" arm's last flag-setter, `006D12`'s own `andi.w #$fff0,f18e.w`, not `006D10`'s own `moveq #0,d7`) and a word-branch cost table transcribed backwards (the settle-carrying gate's own `beq.w`) were also caught and fixed, both before the tree | costed one instruction-block at a time from the tracer, reusing state 14's own cost constants verbatim wherever the ROM bytes or the instruction/addressing shape are identical (the whole contact-search gate and its own found-continuation, the arm A/B retry-budget/grid/nibble/transition bodies down to their own per-instruction costs) -- only the head, the arm gates' own jump-start/toggle tests and the settle probe needed new constants | `gods_sega.game.player` extended (`state13_step`, `state13_arm_a`, `state13_arm_b`, `state13_arm_d`, `state13_settle`, `state13_settle_probe`); plan `gods_sega.boundary.state13_plan` (+ `_state13_main_dispatch`, `_state13_arm_d_cost`, `_state13_settle_cost`, `_state13_settle_probe_cost`, the `_S13_*`/`_S13A_*`/`_S13B_*`/`_S13D_*`/`_S13S_*`/`_S13P_*` cost constants); candidates 'state-13', 'camera-sprites' (extended to fifty gates); mutant `state-13-mutant-result` (`recovery._mutate_outcome`, the same STATE_INDEX/F19x/D7-feeds-an-unbounded-table reasoning as states 8/9/26's own) | `tests/games/gods/test_state13.py` (new: unit tests for the head/arm-d/settle-probe plus all 227 retained fixtures across `census-006B4E` on four recordings, 0 mismatches, 0 declines besides the named unwitnessed arms; `factcheck check --perturb-upper-halves`: 221 MATCH, 0 mismatch, 6 SKIPPED -- adapter-refused constructions) | f0ac19738f190b5a1efa7ac5cd2677f6e0ce582df16483525fad1f95f9bc7068, 15,148 frames, artifacts/gods/verify-state-13-f0ac1973, **PASS, 90 hits of 91 gates, 1 fallback (z80 bank guard, exact adapter refusal)** -- the shortest node, `ca2b703b6fd5`, never exercises this entry; segment_verify at boundary-6000/12000 PASS (0/0, 42/1 hits/fallbacks, the one fallback z80 bank guard); mutant `state-13-mutant-result` DIVERGENCE at frame 4,895 | 90 hits, 1 fallback | this commit
2026-09-18 | 006B4E state 13, continued / MILESTONE SEAL: `tree_verify.py --candidate camera-sprites` (fifty gates, five leaves cold) PASS, 107,519 frames, 1,099,228 hits, 6,885 fallbacks (16 at the new gate), bit-exact, `artifacts/gods/verify-camera-sprites-leaves-2026-09-18f`.  A real finding recorded, not acted on: the STATE_TABLE (`005618`, confirmed from `005700`'s own dispatch code -- `asl.w #3,d0; movea.l 4(a0,d0.w),a0; jmp (a0)`, an 8-byte stride, the address at offset +4) shows `FFFFF192 == 20` (not 26) reaches `0069AC` -- confirmed directly against real fixtures (`census-0069AC-ca2b703b6fd5/0069AC-entry-p0.state`'s own live `FFFFF192` reads `0x14` = 20, matching `census-0066A8`'s own `F192 == 9` and `census-006B4E`'s own `F192 == 13` at their respective entries, cross-checking the table for every already-recovered state).  The existing `'state-26'` candidate name (`STATE26_ENTRY = 0x0069AC`, sealed and pushed 18 September) is a MISNOMER: its own gate PC and every fact/test/verification is keyed by that PC, not by the name, so nothing already proven is wrong -- but the true STATE_INDEX for this entry is 20, not 26, and `docs/gods/STATUS.md`'s own frequency tally already used the CORRECT value (20: 194 occurrences) throughout, tallied independently by the 17 September blocker's own live-`FFFFF192` scratch scan, not from this table.  `STATE_TABLE` reconstructed in full from the ROM (index: address): 0:006FFE, 1:007282, 2:0074F0, 3:007516, 4:007538, 5:00746A, 6:0074C2, 7:006FFC, 8:00648C, 9:0066A8, 10:005EA2, 11:005D32, 12:005FF4, 13:006B4E, 14:006DA6, 15:006D68, 16:006686, 17:006666, 18:005834, 19:005886, 20:0069AC, 21:006886, 22:0062B0, 23:006164, 24:006AD8, 25:006B14, 26:005724, 27:00581E, 28:005828 -- the remaining witnessed candidates (12, 16, 19, 11, 2, 3, 4, 17, 21, 18, 28, 22, 27, 23, 10) use these addresses; state 20 needs no further work (already `state-26`).  Not renaming the existing candidate: out of scope for this session, a name-only change to already-sealed, tree-verified code and tests, tracked here as a fact for the next session rather than acted on | - | - | - | - | STATUS.md and this ledger row | ec08b91 (state 13's own commit, this seal pushed with it)
2026-09-18 | 005FF4 state 12 / an oscillating swing/pendulum dispatcher -- a genuinely NEW shape, not a horizontal/vertical/falling twin of anything already recovered.  Shares the already-recovered grid cell and contact search, and -- for the first time -- RAM the zone check's own routine also reads (`game/zones.py`'s `COOLDOWN`/`SUPPRESS_COOLDOWN`, `FFFFEF3E`/`FFFFF1B6`; state 12 shares the fields, not the routine).  The oscillation head (005FF4-006033) runs unconditionally every activation: `FFFFF198` (a tick counter) advances TWICE per tick (once by the OLD `FFFFF194`'s own value shifted right 2, again by a plain +1 after `FFFFF194` itself bumps/caps at `STATE12_OSCILLATION_CAP`, 10); an `FFFFEA20`-gated block test runs two independent, mutually-exclusive-in-practice checks (`== -1`, a LEFT arm provisionally setting state 11 with a block test gated on `FFFFF18C & 0x1f == 0`, the SAME gate shape state 8's own arm A uses; or `== 1`, a RIGHT arm provisionally setting state 12 itself with its own `& 0x1c` gate), each additionally gated by `FFFFF1A0 < STATE12_RETRY_CAP` (6, real ROM, unwitnessed when it closes the gate, declined by name); the tail re-reads the grid cell and either finds ground (state 16, a genuine three-way `COOLDOWN`-adjustment tail: exit unchanged within the tick gate, exit suppressed by `SUPPRESS_COOLDOWN`, or halve the excess into `COOLDOWN`), opens a trigger gate into the already-recovered `contact_search` (state 22), or exits unchanged.  The LEFT arm (`FFFFEA20 == -1`) is witnessed only via a real 120-frame `segment_verify` continuation from a retained fixture, never by any of the 605 real path classes the whole-history census over all five recordings retained as its own entries -- captured directly from that continuation (`FFFFF192` read live at the gate) and retained as its own fixture once found.  Nine real defects the FAST tier caught before the tree, none guessed: (1) the SECOND `FFFFF198` addition (`006022 addq.w #1`) was missing entirely from the first draft, which only modelled the first (`005FFE add.w`); (2) two word-branch cost tables transcribed backwards (`btst`'s own `006110 beq.w`, the contact-search gate's own `00611A bne.w`, and separately `SUPPRESS_COOLDOWN`'s own `006156 bne.w`); (3) `btst.b` only ever touches Z, not the full CCR -- an early draft used the generic logic-op SR helper, losing N/V/C/X from the comparison immediately before it; (4) the right arm's own retry-budget-open-but-block-test-skipped path (`FFFFF18C`'s own low bits `& 0x1c` between 8 and 0x18) needed its own detour cost (arm B's own `tst.w ea20;bpl.w`, always taken here) the first draft never charged; (5) that same skip path never updated `POSITION_X`/`D0` in the exit registers; (6) `POSITION_Y` is already advanced in REAL RAM by the oscillation head's own two immediate memory writes before the grid_cell calls, block tests and ground tests run -- the boundary's own `read` closure needed a POSITION_Y substitution from the very top of the function, the same way `position_x_after` already gets one; (7) the ground tail's own "exit unchanged within the tick gate" sub-arm never wrote `POSITION_Y` at all, though `006CFA`'s own `addq.w #6,f18e.w` (unlike state 14's own register-based, saved/restored step) is an unconditional MEMORY write that sticks regardless of which of the three tail exits fires; (8) the tick/cooldown arithmetic's own final `D0` value (post-`subi`, post-`asr`) was never written to the exit registers on two of the three tail exits; (9) `00614A subi.w` is a real SUBTRACT (X follows C), not a CMP (X untouched) -- an early draft used the compare-only SR helper.  The left arm's own block test (`006048`-`006086`) also had its own nibble-test nesting bug (running unconditionally instead of only when the block test itself ran), caught the same way | costed one instruction-block at a time from the tracer on real fixtures over `census-005FF4-*` (609 real path classes across five recordings collapsing to six real terminal shapes, plus the one captured left-arm fixture) | `gods_sega.game.player` extended (`state12_step`, `state12_oscillate`, `state12_block_test_left`, `state12_block_test_right`, `state12_ground_tail`); plan `gods_sega.boundary.state12_plan` (+ `_state12_head_cost`, the `_S12_*`/`_S12G_*` cost constants); candidates 'state-12', 'camera-sprites' (extended to fifty-one gates); mutant `state-12-mutant-result` (`recovery._mutate_outcome`, the same STATE_INDEX/F19x/D7-feeds-an-unbounded-table reasoning as states 8/9/13/26's own) | `tests/games/gods/test_state12.py` (new: unit tests for the oscillation head, both block tests and the ground tail, plus all 610 retained fixtures across `census-005FF4` on five recordings (including the captured left-arm fixture), 0 mismatches, 3 correctly-declined (retry-budget-exhausted); `factcheck check --perturb-upper-halves`: 605 MATCH, 0 mismatch, 3 DECLINED, 2 SKIPPED -- adapter-refused constructions) | ca2b703b6fd595a8d8bd0109ff67f09511d1cda614c44e3c93aca4bfa215ced8, 14,583 frames, artifacts/gods/verify-state-12-ca2b703b6fd5, **PASS, 101 hits of 101 gates, 0 fallbacks**; segment_verify at boundary-6000/12000 PASS (4/0, 6/0 hits/fallbacks); mutant `state-12-mutant-result` DIVERGENCE at frame 3,017; MILESTONE SEAL: `tree_verify.py --candidate camera-sprites` (fifty-one gates, five leaves cold) PASS, 107,519 frames, 1,098,187 hits, 6,872 fallbacks (6 at the new gate), bit-exact, `artifacts/gods/verify-camera-sprites-leaves-2026-09-18g` | 101 hits, 0 fallbacks (tree: 1,098,187 hits, 6,872 fallbacks) | this commit
2026-09-18 | 006686 state 16 / a tiny two-step "settle then countdown" leaf -- the target both state 9's own "ground-before"/"ground-after" arms and state 26's own mirror transition into (`_state9_ground_stores`).  No `d7` is ever read as an input, only written (moveq #2) on the transition arm.  `FFFFF1B8 == 0` increments it to 1 and exits unchanged (a one-tick "settle" delay after `_state9_ground_stores` itself already cleared it); `FFFFF1B8 != 0` decrements `FFFFF198` (the SAME tick counter `_state9_ground_stores` clears to 0 too) by 4 -- a non-negative result exits unchanged (the countdown continues next tick), a negative result transitions to state 1 with `d7` forced to 2.  391 real path classes across all five recordings collapse to exactly three real terminal shapes.  One real defect the FAST tier caught: the word-branch cost table for `006698`'s own `bpl.w` was transcribed backwards (taken/not-taken swapped), the SAME class of bug states 12's own session kept finding | costed one instruction-block at a time from the tracer on real fixtures over `census-006686-*` | `gods_sega.game.player` extended (`state16_step`); plan `gods_sega.boundary.state16_plan`; candidates 'state-16', 'camera-sprites' (extended to fifty-two gates); mutant `state-16-mutant-result` (`recovery._mutate_outcome`) | `tests/games/gods/test_state16.py` (new: unit tests for all three arms plus all 391 retained fixtures across `census-006686` on five recordings, 0 mismatches, 0 declines; `factcheck check --perturb-upper-halves`: 379 MATCH, 0 mismatch, 12 SKIPPED -- adapter-refused constructions) | ca2b703b6fd595a8d8bd0109ff67f09511d1cda614c44e3c93aca4bfa215ced8, 14,583 frames, artifacts/gods/verify-state-16-ca2b703b6fd5, **PASS, 85 hits of 85 gates, 0 fallbacks**; segment_verify at boundary-6000/12000 PASS (3/0, 3/0 hits/fallbacks); mutant `state-16-mutant-result` DIVERGENCE at frame 2,373 | 85 hits, 0 fallbacks (tree: 1,098,931 hits, 6,879 fallbacks) | ca1ffe6; MILESTONE SEAL: tree_verify.py --candidate camera-sprites (fifty-two gates, five leaves cold) PASS, 107,519 frames, bit-exact, artifacts/gods/verify-camera-sprites-leaves-2026-09-18h (pushed)
2026-09-18 | 005D32 state 11 / BYTE-IDENTICAL to state 12's own oscillation head and `FFFFEA20`-gated block test -- confirmed via a raw ROM diff, `rom[0x005D32:0x005E28] == rom[0x005FF4:0x0060EA]`, differing only in relocated branch-displacement bytes, down to the LEFT/RIGHT arms' own hardcoded provisional `STATE_INDEX` values (0xB/0xC, unchanged, since those ARE this dispatcher's own pair of table indices, not state-specific constants).  The ground tail's own four stores are the same instructions, just reordered in the ROM (`clr f1b8`/`moveq d7`/`move fdf6` BEFORE `andi f18e`, unlike state 12's own `andi`-second order) -- functionally identical, targeting states 17 (ground, `STATE11_GROUND_INDEX`) and 23 (trigger, `STATE11_TRIGGER_INDEX`) instead of state 12's own 16/22.  Every `_S12_*`/`_S12G_*` cost constant reused verbatim, since the ROM bytes (and therefore every cycle/instruction count) are identical; no new defects, since none of state 12's own nine were specific to its own address range.  The LEFT arm (`FFFFEA20 == -1`) was, again, invisible to the whole-history census (693 real path classes across all five recordings never landed on it) and needed the same targeted 120-frame `segment_verify` continuation capture as state 12's own (a temporary env-var-gated `machine.snapshot()` dump on `FFFFEA20 == -1` entry, removed once the fixture was captured and retained under `census-005D32-7251bbd0ecf7/005D32-entry-p999-leftarm.state`); found on the very first node tried, within 4,000 frames, with the candidate already active throughout (a real bit-exact run, not merely a park-point capture) | costed by direct address-shift correspondence from state 12's own already-costed plan (0x005FF4-0x005D32 == a constant -0x2C2 offset applied to every embedded return-site/exit-PC literal, cross-checked against real traces at every one of those addresses) rather than re-deriving from the tracer block by block | `gods_sega.game.player` extended (`state11_step`, `state11_ground_tail`, reusing `state12_oscillate`/`state12_block_test_left`/`state12_block_test_right` directly by reference); plan `gods_sega.boundary.state11_plan` (reuses `_state12_head_cost` and every `_S12_*`/`_S12G_*` cost constant directly, no new constants needed); candidates 'state-11', 'camera-sprites' (extended to fifty-three gates); mutant `state-11-mutant-result` (`recovery._mutate_outcome`, the same STATE_INDEX/F19x/D7-feeds-an-unbounded-table reasoning as states 8/9/12/13/16/26's own) | `tests/games/gods/test_state11.py` (new: unit tests for the ground tail's three real outcomes under state 11's own target constants, plus all 481 retained fixtures across `census-005D32` on five recordings (including the captured left-arm fixture), 0 mismatches, 0 declines under the plain sweep beyond the expected retry-budget-exhausted sub-case; `factcheck check`: 481 fixtures, 420 MATCH, 0 MISMATCH, 61 DECLINED; `--perturb-upper-halves`: 412 MATCH, 0 mismatch, 59 DECLINED, 10 SKIPPED -- adapter-refused constructions) | ca2b703b6fd595a8d8bd0109ff67f09511d1cda614c44e3c93aca4bfa215ced8, 14,583 frames, artifacts/gods/verify-state-11-ca2b703b6fd5, **PASS, bit-exact, 57 candidate hits of 79 gates, 22 fallbacks (20 retry-budget-exhausted, 2 z80 bank guard)**; segment_verify at boundary-6000/12000 PASS (1/0, 0/0 hits/fallbacks); mutant `state-11-mutant-result` DIVERGENCE at frame 3,529 | 97 hits at the new gate across the tree (27+17+25+6+22 by node), tree total 1,098,311 hits, 6,968 fallbacks; MILESTONE SEAL: `tree_verify.py --candidate camera-sprites` (fifty-three gates, five leaves cold) PASS, 107,519 frames, bit-exact, `artifacts/gods/verify-camera-sprites-leaves-2026-09-18i` | this commit
2026-09-18 | 005886 state 19 / escalation: NEW_GODS_SUBSYSTEM (not a same-recipe leaf) | - | - | - | - | blocker docs/gods/blockers/2026-09-18-005886.md; a full census (190 fixtures, five recordings) buckets into TEN terminal shapes, an order of magnitude more than any state closed this session.  Confirmed by hand trace: an `FFFFEA1E`-gated head splits into two halves of one state machine (an `F238`/`F23A` flag dispatch on one side, an unconditional 200-entry inline scan on the other); that inline scan is `game.movement.box_overlap_scan`'s own algorithm (same table, stride, margins and skip tests, confirmed instruction-by-instruction) but NOT byte-identical to `00722C` (a second, separately compiled copy) and, unlike states 0/1's own two witnessed call sites, its result IS consumed here (a found entry's own kind field, checked against a fresh 192-entry ROM table at `0x14B42`, arms or permanently disables the table slot); a new leaf `005CEE` (reached from both head-halves) indexes `game.achievements.ACHIEVEMENT_SLOTS`/`HIGHLIGHT_ID` (`FFFFF22E`/`F236`) directly by register rather than by `achievement_slot_reset`'s own four fixed offsets -- correcting that module's own docstring, which claimed those fields belonged to the achievement tracker alone -- and, on its own real second arm, calls a wholly new, unrecovered leaf `010D7C` (a floating icon/message spawn record over four untouched fields, `FFFFF1F0`/`F1F2`/`F1F4`/`F1F6`, gated by a busy test and a special-cased kind) that a completely different investigation (the 16 September `00BA8E`/`010CD2` escalation) had already flagged as a real branch target from the other direction.  At least five more terminal shapes (past `005C14`'s own `FFFFEA23` bit tests, over half the census) were not traced at all.  Recommended: continue the coordinator's own frequency-order list (2, 3, 4, 17, 21, 18, 28, 22, 27, 23, 10), each a real same-recipe leaf, and return to state 19 as its own dedicated session later, the way states 0/1/14 each got one | escalate commit
2026-09-18 | 0074F0 state 2 / another tiny three-arm leaf, the SAME shape as state 16's own "settle then countdown": `FFFFEA20 < 0` transitions straight to state 3 (`d7` untouched, real ROM leaves it alone entirely); otherwise a counter (`d7`) counts up (`addq.w #1,d7`, a WORD op) and, once it reaches 3, forces `d7` to 6 (`moveq #6,d7`, a real 32-bit LONG move) and transitions to state 1.  No memory is read besides `FFFFEA20`.  269 real path classes across all five recordings collapse to exactly three real terminal shapes.  One real defect the FAST tier's own `--perturb-upper-halves` pass caught (the plain sweep alone missed it, since every witnessed `d7` already carried a clean upper half): the counting arm's own result was written as the FULL 32-bit exit register instead of `(registers['d7'] & 0xFFFF0000) | result['d7']`, clobbering a real upper half the ROM's own word-sized `addq.w` leaves alone; the transition arm's own `moveq` needed no such fix, since it is a genuine 32-bit move that really does clear the upper half | costed one instruction-block at a time from the tracer on real fixtures over `census-0074F0-*` | `gods_sega.game.player` extended (`state2_step`); plan `gods_sega.boundary.state2_plan`; candidates 'state-2', 'camera-sprites' (extended to fifty-four gates); mutant `state-2-mutant-result` (`recovery._mutate_outcome`, the same STATE_INDEX/D7-feeds-an-unbounded-table reasoning as states 8/9/11/12/13/16/26's own) | `tests/games/gods/test_state2.py` (new: unit tests for all three arms plus all 269 retained fixtures across `census-0074F0` on five recordings, 0 mismatches, 0 declines; `factcheck check --perturb-upper-halves`: 261 MATCH, 0 mismatch (after the D7 fix above), 8 SKIPPED -- adapter-refused constructions) | ca2b703b6fd595a8d8bd0109ff67f09511d1cda614c44e3c93aca4bfa215ced8, 14,583 frames, artifacts/gods/verify-state-2-ca2b703b6fd5, **PASS, bit-exact, 66 hits of 66 gates, 0 fallbacks**; segment_verify at boundary-6000/12000 PASS (0/0, 0/0 hits/fallbacks); mutant `state-2-mutant-result` DIVERGENCE at frame 2,283 | 66 hits, 0 fallbacks | this commit
2026-09-18 | 007516 state 3 / a mirror of state 2's own countdown/gate shape, one step further into the player state machine's own `FFFFEA20`-driven cycle: `FFFFEA20 == 1` transitions to state 2 (real ROM code, UNWITNESSED by any of 270 retained fixtures across all five recordings, declined by name); otherwise a counter (`d7`) counts DOWN (`subq.w #1,d7`, state 2's own counts UP) and, once it goes negative, is forced to 6 (`moveq #6,d7`) and transitions to state 0 (state 2's own transitions to state 1).  No memory read besides `FFFFEA20`.  270 real path classes across all five recordings collapse to exactly two real terminal shapes.  No defects this time -- the upper-half lesson from state 2's own `moveq` vs `addq.w`/`subq.w` distinction carried straight over into the first draft | costed one instruction-block at a time from the tracer on real fixtures over `census-007516-*`; the transition-2 arm costed by hand from a direct ROM disassembly of the 10-byte gap the census never reached (`move.w #2,f192.w; bra.w $75d6`), declined rather than guessed at | `gods_sega.game.player` extended (`state3_step`); plan `gods_sega.boundary.state3_plan`; candidates 'state-3', 'camera-sprites' (extended to fifty-five gates); mutant `state-3-mutant-result` (`recovery._mutate_outcome`, the same STATE_INDEX/D7-feeds-an-unbounded-table reasoning as states 2/8/9/11/12/13/16/26's own) | `tests/games/gods/test_state3.py` (new: unit tests for all three arms plus all 270 retained fixtures across `census-007516` on five recordings, 0 mismatches, 0 declines under the plain sweep since no fixture ever hit `FFFFEA20 == 1`; `factcheck check --perturb-upper-halves`: 266 MATCH, 0 mismatch, 4 SKIPPED -- adapter-refused constructions) | ca2b703b6fd595a8d8bd0109ff67f09511d1cda614c44e3c93aca4bfa215ced8, 14,583 frames, artifacts/gods/verify-state-3-ca2b703b6fd5, **PASS, bit-exact, 56 hits of 57 gates, 1 fallback (the unwitnessed transition-2 decline)**; segment_verify at boundary-6000/12000 PASS (3/0, 0/0 hits/fallbacks); mutant `state-3-mutant-result` DIVERGENCE at frame 2,423 | 56 hits, 1 fallback | this commit
2026-09-18 | 007538 state 4 / a genuinely different shape from states 2/3's own counter/gate leaves: `FFFFEA20 == 0` jumps DIRECTLY into state 15's own entry (`006D68`, the STATE_TABLE's own index-15 address, confirmed against the reconstructed table) via a plain `beq.w`, the SAME "one region, two gates" shared-fallthrough shape states 5/6 already use into 1/0 -- the FIRST real evidence of state 15's own semantics, though state 15 remains unwitnessed as an independent dispatch (no recording has ever shown `FFFFF192 == 15` directly, only reached this way).  State 15's own body, as reached from here, re-runs the already-recovered grid cell lookup (`0063FA`) and tests its own ground byte (`+0x180(a0)`): found exits unchanged (no `FFFFF192` write at all, `d7` untouched); NOT found is real ROM this session did not trace further past `006D72` and declines by name.  `FFFFEA20 != 0` transitions to state 3 (`d7` forced to 0, `moveq #0,d7` running AFTER the `FFFFF192` store and so the real last flag-setter, mirroring state 3's own `clr`/`moveq` ordering) when negative, or state 2 (`d7` forced to 2) when positive.  81 real path classes across all five recordings collapse to exactly three real terminal shapes.  No defects: the grid-cell composition pattern (return-site push, `_asl_sr` for the internal `asl.w #3,d1` before the caller's own `cmpi.b` overwrites N/Z/V/C while X survives) and the moveq-is-the-real-last-flag-setter pattern both carried straight over from earlier states | costed one instruction-block at a time from the tracer on real fixtures over `census-007538-*`, reusing the already-costed `GRID_CELL_COST` constant for the composed `0063FA` call | `gods_sega.game.player` extended (`state4_step`, reusing `game.grid.grid_cell`); plan `gods_sega.boundary.state4_plan` (reuses `GRID_CELL_COST`); candidates 'state-4', 'camera-sprites' (extended to fifty-six gates); mutant `state-4-mutant-result` (`recovery._mutate_outcome`, the same STATE_INDEX/D7-feeds-an-unbounded-table reasoning as states 2/3/8/9/11/12/13/16/26's own) | `tests/games/gods/test_state4.py` (new: unit tests for the two transition arms plus all 81 retained fixtures across `census-007538` on five recordings, 0 mismatches, 0 declines under the plain sweep since every fixture's own ground test happened to succeed; `factcheck check --perturb-upper-halves`: 79 MATCH, 0 mismatch, 2 SKIPPED -- adapter-refused constructions) | ca2b703b6fd595a8d8bd0109ff67f09511d1cda614c44e3c93aca4bfa215ced8, 14,583 frames, artifacts/gods/verify-state-4-ca2b703b6fd5, **PASS, bit-exact, 34 hits of 35 gates, 1 fallback (the unwitnessed "ground not found" decline)**; segment_verify at boundary-6000/12000 PASS (0/0, 0/0 hits/fallbacks); mutant `state-4-mutant-result` DIVERGENCE at frame 2,281 | 34 hits, 1 fallback (tree: 1,099,391 hits, 6,982 fallbacks); MILESTONE SEAL: `tree_verify.py --candidate camera-sprites` (fifty-six gates, five leaves cold) PASS, 107,519 frames, bit-exact, `artifacts/gods/verify-camera-sprites-leaves-2026-09-18j` (batch of three: states 2, 3, 4) | 9c1612a; MILESTONE SEAL commit follows
2026-09-18 | 006666 state 17 / the SAME "settle then countdown" shape as state 16's own (`FFFFF1B8`/`FFFFF198`), transitioning to state 0 instead of state 1 once `FFFFF198` goes negative (`d7` forced to 2, the same value state 16's own transition uses).  Not byte-identical to state 16's own code (the final store is `clr.w f192.w`, not a `move.w #imm`, so the tail's own bytes differ even though the algorithm does not), so costed with its own `_S17_*` constants rather than reusing state 16's own.  298 real path classes across all five recordings collapse to exactly three real terminal shapes.  No defects: the settle/countdown pattern, the word-branch cost convention and the "later flag-setter overrides the earlier one" rule (`clr.w` runs after `moveq`, so `clr.w` is the real last flag-setter here, the SAME reasoning states 3/4's own `clr`/`moveq` orderings already established) all carried straight over | costed one instruction-block at a time from the tracer on real fixtures over `census-006666-*` | `gods_sega.game.player` extended (`state17_step`, mirroring `state16_step`'s own shape); plan `gods_sega.boundary.state17_plan`; candidates 'state-17', 'camera-sprites' (extended to fifty-seven gates); mutant `state-17-mutant-result` (`recovery._mutate_outcome`, the same STATE_INDEX/D7-feeds-an-unbounded-table reasoning as states 2/3/4/8/9/11/12/13/16/26's own) | `tests/games/gods/test_state17.py` (new: unit tests for all three arms plus all 298 retained fixtures across `census-006666` on five recordings, 0 mismatches, 0 declines; `factcheck check --perturb-upper-halves`: 296 MATCH, 0 mismatch, 2 SKIPPED -- adapter-refused constructions) | ca2b703b6fd595a8d8bd0109ff67f09511d1cda614c44e3c93aca4bfa215ced8, 14,583 frames, artifacts/gods/verify-state-17-ca2b703b6fd5, **PASS, bit-exact, 59 hits of 59 gates, 0 fallbacks**; segment_verify at boundary-6000/12000 PASS (4/0, 0/0 hits/fallbacks); mutant `state-17-mutant-result` DIVERGENCE at frame 2,755 | 59 hits, 0 fallbacks | this commit
2026-09-18 | 006886 state 21 / a genuinely large, three-way hybrid: state 9/26's own jump-arc fall step (the SAME shared table `006414`, `_row_gate_open`, reused verbatim) combined with state 12's own LEFT block-test gate (`FFFFF18C & 0x1F == 0`, offsets -1/0x7F/0xFF, not state 9's own `< 8` gate with offsets 1/0x81/0x101); the head is `FFFFF1BA`-gated exactly like state 26's own (`_state26_head`'s own shape), but a genuinely separate ROM copy (confirmed not byte-identical via a raw ROM diff, so it gets its own `_state21_head`); TWO ground-ahead probes, gated by state 9's own `STATE9_TRIGGER_GATE` (0x16, the first) and state 26's own recheck value (0x12, the second) -- a hybrid pairing neither existing state uses; and a THIRD real composition, a `jsr` into the already-recovered movement-cluster consumer `012E5A` (`contact_consume`, not state 26's own `012DA0`) when the post-head `d7` counter is exactly 1, transitioning to state 8 (not state 26's own state 9) once `d7` reaches 3.  The X-advance has no `FFFFF19C` gate at all, unlike state 9's own gated version -- it always applies once the block test declines.  The doubled fall step (`FFFFF19E != 0`) is modelled directly here (a second, identical `sub.w` against the SAME table value) rather than declined the way state 9's own version is, since the adjacent ROM code for it is trivial and directly disassembled, not a guess.  448 real path classes across all five recordings collapse to five real terminal shapes; the "D7 < 3, != 1" tail's own cap-override (a SEPARATE copy of the landing-at-state-11 stores, at `006994`) is real ROM, unwitnessed by any recording, and declines by name.  No defects found: every composition this planner reuses (`_row_gate_cost`, `_cc_resolve`, `GRID_CELL_COST`) was already fully tested by states 9/26's own and the movement-cluster's own earlier sessions, and reusing them directly (rather than re-deriving their own costs) caught every fact on the very first attempt -- the ONLY genuinely new cost constants needed were for state 21's own head, block test, advance and D7-based tail, all instruction-for-instruction from a direct disassembly of `006886`-`0069AC` cross-checked against real traces at every branch | costed one instruction-block at a time from the tracer on real fixtures over `census-006886-*`, reusing `_row_gate_cost`/`_cc_resolve`/`GRID_CELL_COST` directly for the shared compositions | `gods_sega.game.player` extended (`_state21_head`, `state21_step`, `state21_tail`); plan `gods_sega.boundary.state21_plan` (reuses `_row_gate_cost`, `_cc_resolve`, `GRID_CELL_COST`, `_S5_JSR_CONSUME`); candidates 'state-21', 'camera-sprites' (extended to fifty-eight gates); mutant `state-21-mutant-result` (`recovery._mutate_outcome`, the same STATE_INDEX/D7-feeds-an-unbounded-table reasoning as states 2/3/4/8/9/11/12/13/16/17/26's own) | `tests/games/gods/test_state21.py` (new: unit tests for the head and the D7-based tail's own three arms plus all 448 retained fixtures across `census-006886` on five recordings, 0 mismatches, 16 correctly-declined (the unwitnessed cap-override); `factcheck check --perturb-upper-halves`: 430 MATCH, 0 mismatch, 16 DECLINED, 2 SKIPPED -- adapter-refused constructions) | ca2b703b6fd595a8d8bd0109ff67f09511d1cda614c44e3c93aca4bfa215ced8, 14,583 frames, artifacts/gods/verify-state-21-ca2b703b6fd5, **PASS, bit-exact, 47 hits of 47 gates, 0 fallbacks**; segment_verify at boundary-6000/12000 PASS (5/0, 0/0 hits/fallbacks); mutant `state-21-mutant-result` DIVERGENCE at frame 3,419 | 47 hits, 0 fallbacks | this commit
2026-09-18 | 005834 state 18 / escalation addendum: additional scope inside the SAME `005886` (state 19) blocker, not a separate one | - | - | - | - | `docs/gods/blockers/2026-09-18-005886.md` (addendum); a census of state 18 (63 occurrences on `fb408bc75597`) shows the SAME `FFFFEA1E`-gated head calling the SAME leaf `005CEE` (byte-for-byte the same three instructions as state 19's own `005886`-`00588E`, just relocated), and several of its own retained fixtures' terminal `last_pc` values are the IDENTICAL numeric addresses state 19's own are (`5842`, `584E`, `592A`, `5882`, `5C32`, `5C54`, `5C78`, `5C80`, `5C94`, `5CEA`) -- not merely similar, the SAME addresses.  A raw ROM diff of the two entries' own head bytes shows this is NOT a duplicated, byte-identical copy the way states 11/12 or 8/9 are (369 of the first 400 bytes differ), so the only explanation for identical terminal PCs is a shared-fallthrough jump into state 19's own body, the same "one region, two gates" shape states 4/15, 5/1 and 6/0 already established.  Recommended: recover states 18 and 19 together in state 19's own dedicated session, not attempted alone here; continuing the coordinator's own list with state 28 next | escalate commit
2026-09-18 | 005828 state 28 / the smallest leaf yet: an UNCONDITIONAL transition to state 1, `d7` forced to 2, no input ever read at all.  All 71 real path classes across all five recordings agree on this single shape | costed directly from the tracer on real fixtures over `census-005828-*` | `gods_sega.game.player` extended (`state28_step`); plan `gods_sega.boundary.state28_plan`; candidates 'state-28', 'camera-sprites' (extended to fifty-nine gates); mutant `state-28-mutant-result` (`recovery._mutate_outcome`, the same STATE_INDEX/D7-feeds-an-unbounded-table reasoning as states 2/3/4/8/9/11/12/13/16/17/21/26's own) | `tests/games/gods/test_state28.py` (new: one unit test plus all 71 retained fixtures across `census-005828` on five recordings, 0 mismatches; `factcheck check --perturb-upper-halves`: 71 MATCH, 0 mismatch) | ca2b703b6fd595a8d8bd0109ff67f09511d1cda614c44e3c93aca4bfa215ced8, 14,583 frames, artifacts/gods/verify-state-28-ca2b703b6fd5, **PASS, bit-exact, 12 hits of 13 gates, 1 fallback (z80 bank guard, an adapter refusal)**; segment_verify at boundary-6000/12000 PASS (0/0, 1/0 hits/fallbacks); mutant `state-28-mutant-result` DIVERGENCE at frame 3,173 | 12 hits, 1 fallback (tree: 1,099,034 hits, 6,999 fallbacks); MILESTONE SEAL: `tree_verify.py --candidate camera-sprites` (fifty-nine gates, five leaves cold) PASS, 107,519 frames, bit-exact, `artifacts/gods/verify-camera-sprites-leaves-2026-09-18k` (batch of three: states 17, 21, 28) | ba73045; MILESTONE SEAL commit follows
2026-09-18 | 0062B0 state 22 / state 21's own `FFFFF1BA`-gated head in front of state 12's own ENTIRE oscillation body and BOTH block tests, reused verbatim (byte-for-byte the same masks, offsets and immediates as state 12's own, confirmed via a direct disassembly of `0062B0`-`006412`); a genuine ground-found jump lands on the SAME PHYSICAL ground-tail code state 12's own uses (`006134`-`006160`, not a relocated copy -- state 12's own `beq.w $612e` and state 22's own land on the IDENTICAL bytes, so `state12_ground_tail` and every `_S12G_*` cost constant are reused exactly, addresses included).  One real, confirmed difference from state 12's own: the ALT ground test's own mask is `FFFFF18C & 0x1F` here (`0063BE`-`0063C2`), NOT state 12's own `0x1C`.  Unlike state 12's own planner, the retry-budget-exhausted sub-case is REAL AND WITNESSED here (a clean skip to the next test, not a decline).  The tail (once neither ground test finds anything) is D7-based: exactly 3 transitions to state 12 (`0xC`, `d7` forced to 0); exactly 1 calls the already-recovered movement-cluster consumer `012DA0` (`contact_consume` PRIMARY, not state 21's own `012E5A` SECONDARY) before exiting unchanged; any other value exits unchanged directly.  91 real path classes across all five recordings collapse to six real terminal shapes.  Two real defects, both the SAME recurring word-branch cost-table inversion class this session kept finding: `0063BA beq.w $612e` and `0063D2 beq.w $612e` (the ground/alt-ground word branches) were transcribed backwards.  One real, useful non-defect finding along the way: this candidate's own mutant (`_mutate_outcome`, dropping `012DA0`'s own writes wholesale) corrupts a linked-list-style record the real game dereferences a few frames later on one retained fixture, causing a genuine M68000 address error rather than a clean value mismatch -- `history-verify`'s own crash-tolerant comparison already reports this cleanly as DIVERGENCE at frame 5,605; `segment_verify`'s own lighter-weight check does not catch native exceptions, so the FAST-tier test catches it directly and documents why | costed one instruction-block at a time from the tracer on real fixtures over `census-0062B0-*`, reusing state 12's own `_state12_head_cost`/`_S12_LEFT_*`/`_S12_RIGHT_*`/`_S12_GRID_*`/`_S12_NIBBLE_*`/`_S12_RETRY_*`/`_S12_LR_HEAD`/`_S12_BSR_GRID`/`_S12_GROUND_TEST`/`_S12G_*` cost constants directly (confirmed byte-identical), plus `_cc_resolve`/`_S5_JSR_CONSUME` for the `012DA0` composition | `gods_sega.game.player` extended (`_state22_head`, `state22_step`, reusing `state12_oscillate`/`state12_block_test_left`/`state12_block_test_right` directly by reference); plan `gods_sega.boundary.state22_plan` (reuses the `_S12_*`/`_S12G_*` constants above plus `_cc_resolve`); candidates 'state-22', 'camera-sprites' (extended to sixty gates); mutant `state-22-mutant-result` (`recovery._mutate_outcome`, the same STATE_INDEX/D7-feeds-an-unbounded-table reasoning as states 2/3/4/8/9/11/12/13/16/17/21/26/28's own) | `tests/games/gods/test_state22.py` (new: unit tests for the head and the D7-based tail's own two gated arms plus all 91 retained fixtures across `census-0062B0` on five recordings, 0 mismatches, 1 correctly-declined (an unwitnessed pickup type inside the reused `_cc_resolve`); `factcheck check --perturb-upper-halves`: 90 MATCH, 0 mismatch, 1 DECLINED) | ca2b703b6fd595a8d8bd0109ff67f09511d1cda614c44e3c93aca4bfa215ced8, 14,583 frames, artifacts/gods/verify-state-22-ca2b703b6fd5, **PASS, bit-exact, 11 hits of 11 gates, 0 fallbacks**; segment_verify at boundary-6000/12000 PASS (0/0, 1/0 hits/fallbacks); mutant `state-22-mutant-result` DIVERGENCE at frame 5,605 (a native address-error crash, not a value mismatch -- confirmed a real consequence of the dropped `012DA0` writes, not a bug) | 11 hits, 0 fallbacks | this commit
2026-09-18 | 00581E state 27 / another unconditional leaf, the SAME shape as state 28's own: always transitions to state 0 (`clr.w f192.w`, not state 28's own `move.w #1,f192.w`), `d7` forced to 2, no input ever read at all.  All 55 real path classes across all five recordings agree on this single shape | costed directly from the tracer on real fixtures over `census-00581E-*` | `gods_sega.game.player` extended (`state27_step`); plan `gods_sega.boundary.state27_plan`; candidates 'state-27', 'camera-sprites' (extended to sixty-one gates); mutant `state-27-mutant-result` (`recovery._mutate_outcome`, the same STATE_INDEX/D7-feeds-an-unbounded-table reasoning as states 2/3/4/8/9/11/12/13/16/17/21/22/26/28's own) | `tests/games/gods/test_state27.py` (new: one unit test plus all 55 retained fixtures across `census-00581E` on five recordings, 0 mismatches; `factcheck check --perturb-upper-halves`: 53 MATCH, 0 mismatch, 2 SKIPPED -- adapter-refused constructions) | ca2b703b6fd595a8d8bd0109ff67f09511d1cda614c44e3c93aca4bfa215ced8, 14,583 frames, artifacts/gods/verify-state-27-ca2b703b6fd5, **PASS, bit-exact, 9 hits of 9 gates, 0 fallbacks**; segment_verify at boundary-6000/12000 PASS (0/0, 0/0 hits/fallbacks); mutant `state-27-mutant-result` DIVERGENCE at frame 2,647 | 9 hits, 0 fallbacks | this commit
2026-09-18 | 006164 state 23 / sits IMMEDIATELY BEFORE state 22's own code in ROM (`006164`-`0062AC`, then `0062B0` is `STATE22_ENTRY` itself, confirmed via a direct disassembly) and is structurally identical to it: the SAME oscillation, the SAME LEFT/RIGHT block tests (byte-for-byte the same masks, offsets and immediates), the SAME ALT ground test with its own `FFFFF18C & 0x1F` mask.  Two real, confirmed differences from state 22's own: the ground-found jump lands on state 11's own PHYSICAL ground tail (`005E6C`, not state 12's own `00612E` state 22's own uses -- `state11_ground_tail` and every `_S12G_*` cost constant reused exactly, addresses included, since it is the SAME shared code state 11 already reuses from state 12); and the D7-based tail's own two targets differ: exactly 3 transitions to state 11 (`0xB`, not state 22's own state 12) with `d7` forced to 0; exactly 1 calls the already-recovered movement-cluster consumer `012E5A` SECONDARY (state 21's own choice) instead of state 22's own `012DA0` PRIMARY.  34 real path classes across all five recordings collapse to five real terminal shapes.  No defects: every `_S22_*`/`_S12_*`/`_S12G_*` cost constant was reused directly (confirmed byte-identical via a direct disassembly of `006164`-`0062AC` against state 22's own `0062B0`-`006412`), and reusing them caught every fact on the first attempt | costed by direct correspondence to state 22's own already-costed plan, cross-checked against real traces at every branch, rather than re-deriving from the tracer block by block | `gods_sega.game.player` extended (`_state23_head`, `state23_step`, reusing `state12_oscillate`/`state12_block_test_left`/`state12_block_test_right` directly by reference); plan `gods_sega.boundary.state23_plan` (reuses every `_S22_*`/`_S12_*`/`_S12G_*` cost constant and `_cc_resolve` directly, no new cost constants needed); candidates 'state-23', 'camera-sprites' (extended to sixty-two gates); mutant `state-23-mutant-result` (`recovery._mutate_outcome`, the same STATE_INDEX/D7-feeds-an-unbounded-table reasoning as states 2/3/4/8/9/11/12/13/16/17/21/22/26/27/28's own) | `tests/games/gods/test_state23.py` (new: unit tests for the head and the D7-based tail's own two gated arms plus all 34 retained fixtures across `census-006164` on five recordings, 0 mismatches, 1 correctly-declined (an unwitnessed pickup type inside the reused `_cc_resolve`); `factcheck check --perturb-upper-halves`: 33 MATCH, 0 mismatch, 1 DECLINED) | ca2b703b6fd595a8d8bd0109ff67f09511d1cda614c44e3c93aca4bfa215ced8, 14,583 frames, artifacts/gods/verify-state-23-ca2b703b6fd5, **PASS, bit-exact, 1 hit of 1 gate, 0 fallbacks**; segment_verify at boundary-6000/12000 PASS (0/0, 0/0 hits/fallbacks); mutant `state-23-mutant-result` DIVERGENCE at frame 6,167 (the SAME native-crash-as-divergence class state 22's own mutant produces, handled the same way in the FAST tier) | 1 hit, 0 fallbacks | this commit
2026-09-18 | 005EA2 state 10 / the LAST witnessed state in the coordinator's own frequency-order list.  State 12's own oscillation head and BOTH block tests, reused verbatim (byte-for-byte the same masks, offsets and immediates), but with NO `FFFFF1BA`-gated head this time -- state 10 goes straight into the oscillation -- and its own, genuinely NEW ground tail (`005FBC`-`005FF0`, not a jump into any already-recovered state's own shared code): `STATE_INDEX` is set to 0x11 unconditionally, then conditionally DECREMENTED to 0x10 when `FFFFF1A8` is non-negative -- a mechanic no other state's own ground handling uses.  The cooldown adjustment's own three sub-cases (exit within the tick gate, suppressed, apply) all converge on the SAME single exit (`d7` forced to 0, the sound cue queued into `pickups.MOVEMENT_SOUND_CUE`, unlike state 12's own three separate exits where the first two skip both the `d7` store and the sound cue).  No D7-based trigger/consume tail either: failing to find ground exits unchanged directly (`blt.w $75d6` on the ALT test's own low bits, or a plain `bra.w $75d6` when the ALT ground byte itself is clear).  22 real path classes across all five recordings collapse to two real terminal shapes -- a small census, but every branch this session's own recipe requires was witnessed.  Two real defects the FAST tier caught: (1) the RIGHT arm's own gate test (`cmpi.w #1,ea20.w`) was missing its own cost entirely, an easy one-line omission from copying state 12's own template without its `_S12_RIGHT_GATE_TEST`/`_S12_RIGHT_GATE_BNE` bracketing, caught immediately by the plain factcheck sweep (a 2-cycle-class error, but this time from a genuinely missing block rather than an inverted branch table); (2) the ground tail's own final sound cue used `hazard.SOUND_COMMAND` (`FFFFFDF4`, the oscillation head's own cue) instead of `pickups.MOVEMENT_SOUND_CUE` (`FFFFFDF6`, the actual target `005FEA move.w #$39,fdf6.w` writes) | costed one instruction-block at a time from the tracer on real fixtures over `census-005EA2-*`, reusing state 12's own `_state12_head_cost`/`_S12_LEFT_*`/`_S12_RIGHT_*`/`_S12_GRID_*`/`_S12_NIBBLE_*`/`_S12_RETRY_*`/`_S12_LR_HEAD`/`_S12_BSR_GRID`/`_S12_GROUND_TEST`/`_S12_ALT_HEAD`/`_S12_ALT_TEST`/`_S12_ALT_GROUND_TEST` cost constants directly for the shared portions, fresh `_S10_*` constants only for the genuinely new ground tail and its own branch targets | `gods_sega.game.player` extended (`state10_step`, `state10_ground_tail`, reusing `state12_oscillate`/`state12_block_test_left`/`state12_block_test_right` directly by reference); plan `gods_sega.boundary.state10_plan`; candidates 'state-10', 'camera-sprites' (extended to sixty-three gates); mutant `state-10-mutant-result` (`recovery._mutate_outcome`, the same STATE_INDEX/D7-feeds-an-unbounded-table reasoning as states 2/3/4/8/9/11/12/13/16/17/21/22/23/26/27/28's own) | `tests/games/gods/test_state10.py` (new: unit tests for both ground-index outcomes and the ground tail's three real outcomes plus all 22 retained fixtures across `census-005EA2` on five recordings, 0 mismatches, 0 declines; `factcheck check --perturb-upper-halves`: 22 MATCH, 0 mismatch) | f40d7bcc9dda (chosen over `ca2b703b6fd5`, which never witnesses state 10 at all), 17,620 frames, artifacts/gods/verify-state-10-f40d7bcc9dda, **PASS, bit-exact, 22 hits of 22 gates, 0 fallbacks**; segment_verify at boundary-6000/12000 PASS (0/0, 0/0 hits/fallbacks -- state 10 is rare enough neither retained boundary snapshot happens to hit it); mutant `state-10-mutant-result` DIVERGENCE at frame 6,592 | 22 hits, 0 fallbacks (tree: 1,098,926 hits, 6,996 fallbacks); MILESTONE SEAL: `tree_verify.py --candidate camera-sprites` (sixty-three gates, five leaves cold) PASS, 107,519 frames, bit-exact, `artifacts/gods/verify-camera-sprites-leaves-2026-09-18l` (batch of four: states 22, 27, 23, 10 -- the last of the coordinator's own frequency-order list) | 62bf6ea; MILESTONE SEAL commit follows
2026-09-18 | 005886 state 19 / 005834 state 18 (`docs/gods/blockers/2026-09-18-005886.md`'s own escalation, resolved) / a shared multi-part region, "one region, two gates" (NOT byte-identical heads -- a raw ROM diff of state 18's own first 400 bytes against state 19's differs in 369 -- but branching into the SAME physical downstream code, confirmed by trace): the box-overlap scan (`0058D2`, a second, separately-compiled copy of `00722C`'s own algorithm, consumed here unlike its own twin's two discard-only call sites at states 0/1 -- a found entry's own kind field disables or, when `FFFFEA23` bits 1/2 are both clear, re-arms the 200-entry table slot at `FFFF4342`; a kind of exactly 2 either way, every one of 63 witnessed 'found' fixtures across both states); the achievement highlight cycle (new leaf `005CEE`, `game.achievements.achievement_highlight_cycle`, correcting this session's own earlier belief that `ACHIEVEMENT_SLOTS`/`HIGHLIGHT_ID` belong to the achievement/collectible-slot tracker alone -- both fields are shared, not owned; calls `001648` with a POSITIVE D2 for the first time in this project, the found entry's own kind, never witnessed by `achievement_slot_reset`'s own always-D2=-1 caller); and the wholly new leaf `010D7C` (`game.spawns.floating_icon_spawn`, a fourth subsystem distinct from `game.messages`'s own fields: a floating icon/message spawn record, witnessed once for state 19 and three times for state 18, all reaching the same 'spawn' arm -- 'special' kind `0x3D` and 'busy' decline, unwitnessed).  The post-scan dispatch's own two-jsr "idle-cycle" arm (`005C98`-`005CEA`, a sound cue then an advance-and-highlight cycle) is Aladdin's own "platform tail: a run of them" shape for the first time in Gods: two real `001648` calls ceded as ONE span (never chaining two seams), confirmed necessary by a real "Seam return/frame mismatch" native error an early draft raised by guarding the first call's own transient return-address slot, which the second call legitimately reuses | costed one instruction-block at a time from the tracer on every retained fixture across states 19 (`census-005886-*`, five recordings, 190 fixtures) and 18 (`census-005834-*`, five recordings, 118 fixtures); the scan's own per-entry costs (`_BS_*`, `_bs_entry_cost`) reused verbatim from `box_overlap_scan_plan`, confirmed instruction-for-instruction identical | `gods_sega.game.player` extended (`state1918_scan_consume`, `state1918_dispatch`, `state19_head`/`state19_budget_tail`, `state18_head`/`state18_budget_tail`); new module `gods_sega.game.spawns` (`floating_icon_spawn`); `gods_sega.game.achievements` extended (`achievement_highlight_cycle`, correcting its own module docstring); plans `gods_sega.boundary.state19_plan`/`state18_plan` (both Seam-or-AtomicPlan depending on arm); candidates 'state-19' (armed inside `camera-sprites`, now sixty-four gates -- the native machine's own capacity limit, PortForge, pinned) and 'state-18' (a standalone candidate, per Aladdin's own precedent for exhausted native capacity in `docs/archive/aladdin/recovery-cost-log.md`: "an attempted nine-gate expansion exceeded native capacity... independent wrapper plans are oracle-only entry points"); mutants `state-19-mutant-result`/`state-18-mutant-result` (`recovery._mutate_register`, NOT the usual `_mutate_outcome` -- dropping every write faults the M68000 here by corrupting a seam's own return address, confirmed empirically; D0 carries the icon slot into every ceded `001648` call and is real on every admitted arm) | `tests/games/gods/test_state19.py` (new: semantics unit tests for the scan/dispatch/highlight-cycle/floating-spawn arms plus all 190 + 118 retained fixtures across both states, 0 mismatches; `factcheck check --perturb-upper-halves`: 184/118 MATCH, 6/0 SKIPPED -- adapter-refused constructions, 0 mismatch) | `ca2b703b6fd595a8d8bd0109ff67f09511d1cda614c44e3c93aca4bfa215ced8`, 14,583 frames: state-19 `artifacts/gods/verify-state-19-ca2b703b6fd5` **PASS, bit-exact, 59 hits of 60 gates, 1 fallback (z80 bank guard, exact)**; state-18 `artifacts/gods/verify-state-18-ca2b703b6fd5` **PASS, bit-exact, 57 hits of 57 gates, 0 fallbacks**; segment_verify at boundary-6000/12000 PASS for `camera-sprites` (extended); mutant `state-19-mutant-result` DIVERGENCE at frame 5,491; mutant `state-18-mutant-result` DIVERGENCE at frame 8,039 | 59 + 57 hits, 1 fallback (tree: 1,099,813 hits, 7,006 fallbacks) | this commit; MILESTONE SEAL: `tree_verify.py --candidate camera-sprites` (sixty-four gates, five leaves cold) PASS, 107,519 frames, bit-exact, `artifacts/gods/verify-camera-sprites-leaves-2026-09-18r` -- every witnessed `005700` state now has its own gated candidate, closing the coordinator's own frequency-order list in full; MILESTONE SEAL commit follows
2026-09-18 | 009D6C creature-attack (`docs/gods/blockers/2026-09-18-00A578.md`'s own escalation, scoping `00A578`/`00A772` beyond this session) / the creature update's own attack timer, called once per active creature per frame from `00A772` (00A578's own 9-slot list walk).  A full-tree census (all five recordings, 273 retained fixtures) found no seam at all -- every native/device boundary this leaf touches is itself already-recovered pure 68000 code -- and four real dispatch families past two early 'skip' exits and 'waiting': aim quadrant 1 draws two jitters from the shared random table (`game.effects.next_random`) around the tracked position and launches through the projectile pool's own body entered directly at `0091C8` (a new public split, `game.projectiles.launch_toward`, sharing `launch`'s own downstream body -- `0091C8` is `0091BC`+12 bytes, exactly past its own two GRID_X/GRID_Y reads and biases); quadrants 2-3 launch at the tracked position with no jitter, through `0091BC` (`launch`) itself; quadrant 0 never launches -- it hands `timers._spawn`'s own already-recovered BACK/FORWARD hazard-pool fill a locally-derived frequency (the same `swap(power*FREQUENCY_SCALE)+1` shape `timers._frequency` uses, fed from the attack byte's own high nibble) through a tail-JUMP, not a call: `009D6C`'s own frame never unwinds an extra level, so `01158C`/`0115D4`'s own inner `rts` returns straight past it to `00A772` (the same shape `countdown_check_plan` already prices, `_CC_CALLEE_HEAD`/`_CC_WINDOW_*`/`_CC_POOL_*` reused verbatim).  Which BACK/FORWARD variant runs is chosen by whichever of two independent ROM tests fires first (a fixed one-byte ROM table indexed by bit when a shared word is negative, or the instance's own field directly otherwise) -- both mirror real, disassembled ROM bytes and both reach the identical already-proven tail.  `'skip-quadrant0'` (that second field exactly zero) and both launch arms' own `'pool-full'` are real ROM code, unwitnessed, declined by name.  A `movem.l d0-d7/a0-a5` save/restore shields every caller-owned register from the launch call's own internal mutation, collapsing the exit register formula to six words (D0-D5) instead of the launch's own complex sign-extension logic.  Six real bugs found via `factcheck check`/`--perturb-upper-halves` before the corpus matched: an `lsr.w #4` transcribed as 4 cycles instead of 14 (and its own instruction miscounted); an 8-vs-7-instruction miscount in the launch setup block (cycles right, count wrong); the jsr's own return address confused with the shared tail's own final `last_pc` (two DISTINCT, near-identical ROM copies of `movem.l (a7)+,d0-d7/a0-a5; rts`, at `009D66`/`009D6A` for the jittered arm and `009D16`/`009D1A` for the direct arm -- not one shared tail); `moveq #$10,d4` (the reload's own head) clobbering D4's upper word, missed on the exit-register formula; D3 wrongly assumed to carry the frequency the way `countdown_check_plan`'s own D3 does (creature code's own D5 does; D3 still holds "kind", an untouched word-op residue, through the whole call); and a double negation of the frequency for BACK (`creatures.attack_update` already negates it; the boundary planner negated it again).  A second `_mutate_register` register-mutant PASSed cleanly (no divergence) over a 20,000-frame window with 446 real hits: confirmed observationally, not assumed, that D0 is clobbered unconditionally by `00A772`'s own very next instruction and D1-D5 by the kind-handler `jsr` between there and their next read -- every register this leaf sets is dead on return, so `_mutate_result` (one write byte off by one) is the real negative control here, with every arm's own write order fixed so the LAST write is a gameplay-visible effect (the countdown reload, the projectile pool fill, `LAUNCHED_FLAG`, or the random cursor advance) rather than the tail-jump's own dead stack residue | costed one instruction-block at a time from the tracer on real fixtures over `census-009D6C*` (all five recordings, 273 retained), reusing `launch_plan`'s own `_PL_*`/`_WR_STEP`/`_WR_TAIL` cost constants and `countdown_check_plan`'s own `_CC_CALLEE_HEAD`/`_CC_WINDOW_*`/`_CC_POOL_*`/`_NR_COST` directly for the shared tails, fresh `_CU_*` constants only for `009D6C`'s own genuinely new head and dispatch | new module `gods_sega.game.creatures` (`attack_update`); `gods_sega.game.projectiles` split (`launch_toward`, `launch` now a thin wrapper over it); plan `gods_sega.boundary.attack_update_plan`; candidate 'creature-attack' (a standalone gate, kept out of `camera-sprites` for the same sixty-four-gate native capacity `state-18` is); mutant `creature-attack-mutant-result` (`recovery._mutate_result`, not the usual `_mutate_register`, for the reason above) | `tests/games/gods/test_creatures.py` (new: semantics unit tests for every arm plus all 273 retained fixtures across `census-009D6C*` on five recordings, 0 mismatches; `factcheck check --perturb-upper-halves`: 273 MATCH, 0 mismatch) | `fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887`, 34,904 frames, `artifacts/gods/verify-creature-attack-fb408bc75597`, **PASS, 10,494 hits, 9 fallbacks (1 observation deadline, 8 z80 bank guard, both `ADAPTER_REFUSALS`)**; segment_verify at boundary-6000 over 20,000 frames PASS (446 hits: 240 skip, 205 waiting, 1 spawn-window); mutant `creature-attack-mutant-result` DIVERGENCE at frame 12,922 on the full history-verify (553 hits before divergence) and at frame 11,159 on the 20,000-frame segment | 20,865 hits, 24 fallbacks (all `ADAPTER_REFUSALS`: 1 machine admission, 1 vblank in span, 21 z80 bank guard, 1 observation deadline) | this commit; MILESTONE SEAL: `tree_verify.py --candidate creature-attack` (a standalone gate, five leaves cold) PASS, 107,519 frames, `artifacts/gods/verify-creature-attack-2026-09-18`; `00A772` (the family) and `00A578` (the walk) are NOT recovered -- escalated in full as `docs/gods/blockers/2026-09-18-00A578.md`, naming ten new unrecovered ROM routines (`00AA38`, `00B920`, `00AA50`, `00A922`, `00B944`, `00A9F2`, `00AF3C`, `00AF52`, `00AD68`, and the ROM table `00AE4C`) and the two genuinely unwitnessed kind-handler slots (`00AA80`, `00AB5A`)
2026-09-18 | 005700 player-state (the family over `FFFFF192`, `docs/gods/blockers/2026-09-17-005700.md`'s own Decision fulfilled) / the dispatcher's own prefix (`d7 <- FFFFF190` STATE_COUNTER, `d0 <- FFFFF192` STATE_INDEX, the `FFFFEECD`/`FFFFF210` inactive arm falling straight into the already-armed `player-tail` gate at `0075D6`, the `STATE_TABLE` lookup at `005618` otherwise) now owns the jump into each recovered state's own planner directly: `player_state_plan` calls the target state's own already-existing planner from a machine parked at its own entry (`d7`/`d0`/`a0` set exactly as the real head leaves them) and merges the head's own cost onto whatever `AtomicPlan`/`Seam` it returns -- the same "call the callee's own planner, prepend this region's own cost" composition `achievement_slot_dispatch_plan` already draws over `achievement_slot_reset_plan`, but with no device access in the head, so the callee is reused directly, never ceded to the machine.  A real defect the FAST tier caught before the corpus matched: `_prefix_atomic_or_seam` (state19/18's own merge helper) discards a register the OUTER head itself sets when the INNER callee never revisits it (most plain state-transition arms never touch `d0`/`a0` again) -- `AtomicPlan.registers` is a delta against the CALLEE's own entry, not the real machine's entry at `005700`, so the stale pre-005700 value would have reverted silently; a new `_merge_head` restates the head's own overlay first, the callee's own registers applied on top.  Declines two real gaps by name, not guessed: states 7 and 15 (`game.player.UNWITNESSED_STATES`, never `FFFFF192`'s own value on any of the eight recordings) and, newly found while building this composition's own dispatch table, real `STATE_TABLE` index 26 (ROM `005724`) -- the existing `'state-26'` candidate's own name is a misnomer for index 20 (ROM `0069AC`, `STATUS.md`'s own 18 September entry); index 26's own handler has never been recovered as its own leaf, despite being the coordinator's own seventh most frequent state (634 of 13,488 activations on `fb408bc75597`) -- confirmed still real and witnessed on the retained census (6 occurrences) and on `ca2b703b6fd5` (314 of 366 fallbacks on a full cold run), the clear next bite.  The 25 individual player-state gates `camera-sprites` used to arm (states 0-6, 8-14, 16-28 except 18, plus the movement-cluster pair 24/25) are retired from that candidate's own gate set (their own hits replaced by this one dispatcher's); their own standalone candidates, gates and tests are untouched | costed directly from the tracer, both branches of `FFFFEECD`/`FFFFF210` poked (unwitnessed by any recording) plus the active dispatch's own `asl.w`/`movea.l`/`jmp`, over `census-005700-fb408bc75597` (405 retained fixtures) and `census-005700-plain` | `gods_sega.boundary` new `player_state_plan`/`_merge_head`/`_PLAYER_STATE_PLANNERS`; `recovery.py` new candidate `'player-state'` (armed inside `camera-sprites`, which drops from sixty-four to forty gates net); mutant `player-state-mutant-result` (`recovery._mutate_player_state_counter`, a `d7`-XOR-1 shape like states 0/1/14's own -- observable on every witnessed activation since d7 survives to the separately-armed `player-tail` gate one step later; blind only on the unwitnessed frozen/inactive arm, where this planner cedes nothing and `player_tail_plan`'s own prefix already bakes `STATE_COUNTER` into a RAM write) | `tests/games/gods/test_player_state.py` (new: all 405 + 3 retained fixtures MATCH or correctly DECLINE, including a dedicated test confirming state 26's own decline names `005724` on a real witnessed occurrence; `factcheck check --perturb-upper-halves`: 395 MATCH, 6 DECLINED, 4 SKIPPED); 24 pre-existing `test_stateN.py`/`test_movement_states.py` membership assertions updated (`STATEN_ENTRY not in camera-sprites`, `PLAYER_STATE_ENTRY in camera-sprites`) since the combined candidate no longer arms them directly; full `run_tests.py gods` 16,023 passed | `ca2b703b6fd595a8d8bd0109ff67f09511d1cda614c44e3c93aca4bfa215ced8`, 14,583 frames, `artifacts/gods/verify-player-state-ca2b703b6fd5`, **PASS, 4,300 hits, 366 fallbacks (314 state-26 decline, 28 z80 bank guard, 20 state-11 retry-budget decline, 2+2 box-overlap-scan declines already known from states 0/1)**; segment_verify at boundary-6000/12000 PASS (129/25, 161/5 hits/fallbacks); mutant `player-state-mutant-result` DIVERGENCE at frame 2,251 | 4,300 hits, 366 fallbacks (tree: see MILESTONE SEAL) | this commit; MILESTONE SEAL: `tree_verify.py --candidate camera-sprites` (forty gates, five leaves cold) PASS, 107,519 frames, bit-exact, `artifacts/gods/verify-camera-sprites-leaves-2026-09-18t` -- fallbacks 8,857 (1,975 state-26 decline, the second-largest reason after 4,468 z80 bank guard, both already broken out above); the composition that turns twenty-seven state gates into one owned entry, the first place the frontier is measured as "the player tick is owned from `005700` to the tail's own RTS" for every witnessed state; MILESTONE SEAL commit follows
2026-09-18 | 00AA50 creature-frame-offset (`docs/gods/blockers/2026-09-18-00A578.md`'s own "Next question", the first of `00A772`'s own three unconditional callees, smallest first) / the creature's own per-kind, per-frame offset: `((KIND_TABLE[kind].value + instance.frame_step) << 4) + FRAME_TABLE[type.frame << 4]`, `KIND_TABLE` (`00A538`) the SAME 8-entry table `00A772`'s own kind dispatch reads (confirmed by trace and by reading the ROM directly: all eight `+4` longs have a zero upper word, so the full 32-bit exit register equals the 16-bit result exactly).  A genuine, RAM/ROM-read-only, twelve-instruction, ONE UNCONDITIONAL PATH leaf -- no branch at all, confirmed across the four recordings that reach it (19,798 real occurrences; the fifth, `ca2b703b6fd5`, never does) -- the simplest shape the census tool can report (`paths 1` on every recording).  A real cross-reference finding along the way: the instance record's own field at `+0xA`, already named `DIRECTION_INDEX` from `attack_update`'s own Q0 arm (a bit index into `DIRECTION_BIT_TABLE`), is the SAME field `00A772`'s own dispatch and this routine both read as the record's own general KIND selector -- aliased as `creatures.KIND`, not renamed, so nothing already shipped changes meaning.  Reached both from `00A772`'s own `$f38a.w` "moving" arm (its own result fed straight to the next unrecovered call, `00A922`, via `move.w d2,-(a7)`) and from inside the still-unrecovered ground/fall kind handlers -- a standalone candidate for now, composed upward once those callers are recovered too | costed directly from the tracer (a single instruction-by-instruction trace suffices: every one of the 8 retained fixtures across four recordings, plus their own distinct exit-CCR variants, is instruction-for-instruction, cycle-for-cycle identical) | `gods_sega.game.creatures` extended (`kind_frame_offset`, `KIND`/`KIND_TABLE`/`KIND_TABLE_VALUE_OFFSET`/`FRAME_STEP`/`TYPE_FRAME_BYTE`/`FRAME_TABLE_PTR`); plan `gods_sega.boundary.kind_frame_offset_plan`; candidate `'creature-frame-offset'` (a standalone gate, kept separate until its own callers are recovered); mutant `creature-frame-offset-mutant-result` (`recovery._mutate_kind_frame_offset`, D2 off by one -- NOT D0, this leaf's own dispatch scratch, fully overwritten from a `moveq` before ever being used, dead by the time any caller could read it back) | `tests/games/gods/test_creatures.py` extended (semantics unit tests for the kind-table/frame-table reads and a direct ROM check that every kind table `+4` long has a zero upper word; all 8 retained fixtures MATCH, `factcheck check --perturb-upper-halves` also 8/8 MATCH; full suite 16,035 passed) | `fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887`, 34,904 frames, `artifacts/gods/verify-creature-frame-offset-fb408bc75597`, **PASS, 10,009 hits, 14 fallbacks (4 observation deadline, 2 vblank in span, 8 z80 bank guard, all `ADAPTER_REFUSALS` -- no declines needed, the routine has no unwitnessed arm at all)**; segment_verify at boundary-12000 PASS (18 hits, 0 fallbacks; boundary-6000 never reaches it, 0/0); mutant `creature-frame-offset-mutant-result` **faults the M68000 with an address error** on both the 300-frame segment and the full history (a real consequence of the corruption a few calls downstream, in the still-unrecovered chain `00A922 -> ... -> 00126A`'s own sprite emitter -- the SAME "real consequence, not a control failure" class states 22/23's own mutants hit; `history-verify`'s own crash-tolerant comparison reports it as `CANDIDATE_ERROR`, the same claim a clean `DIVERGENCE` would make) | 10,009 hits, 14 fallbacks (tree: see MILESTONE SEAL) | this commit; MILESTONE SEAL: `tree_verify.py --candidate creature-frame-offset` (a standalone gate, five leaves) -- **PASS, bit-exact, on the four recordings that reach it** (`7251bbd0ecf7` 9 fallbacks, `f0ac19738f19` 1, `f40d7bcc9dda` 1, `fb408bc75597` 14, all `ADAPTER_REFUSALS`); `ca2b703b6fd5` is honestly `NOT_EXERCISED` (the census already showed no entry fires there at all: this one recording's own playthrough never puts a creature into the "moving" state or reaches a ground/fall kind handler) -- not a failure, and not claimed as evidence, per `docs/common/recovery-process.md`'s own "A PASS with zero candidate hits is NOT_EXERCISED, not evidence"; `artifacts/gods/verify-creature-frame-offset-2026-09-18`.  Next: `00A922` and `00B944` (the remaining two of `00A772`'s own three unconditional callees, `00B944` wrapping the already-recovered pickup check `00BA8E` per the task), smallest/most bounded first, per the blocker's own "Next question"
2026-09-18 | 005724 real STATE_TABLE index 26 (the tree's largest single decline before this session, 1,975 of 8,857 fallbacks) / four top-level arms, `FFFFEA20`/`FFFFEA23` bit 2/`FFFFEA1E`-selected (`census_all.py --entry 005724`, all five recordings, 606 real path classes; every one falls into the shared tail at `0075D6`).  `FFFFEA20 != 0` (126 fixtures): an unconditional transition to state 28/27, `d7` forced to 0.  `FFFFEA20 == 0`, `FFFFEA23` bit 2 SET (202 fixtures): `state_26_actor_scan` -- gated by `PROXIMITY_ACTIVE` (F24A, the ONLY gate ever witnessed as deciding; `PROXIMITY_BUSY`/`PROXIMITY_PENDING` decline, real ROM, never witnessed blocking), a bounded 200-entry scan of the ALREADY-recovered `game.movement.BOX_SCAN_TABLE` (FFFF4342) through `achievements.RECORD_TABLE`'s own two-step ADDA.W index (`achievements._record_address`, a THIRD consumer of that shared table, gated on status `3` here vs `2` at `004790`) and `conditions.FLAGGED`'s own 64-entry linear id search (`_state_26_id_search`, the same table kinds 11/12's own predicate already names) plus a position box test (`_state_26_actor_box_test`); the first entry passing both stops the scan (an id match among `PROXIMITY_MATCH_IDS` or not, both play `hazard.SOUND_COMMAND`); a fully exhausted id search is real ROM, never witnessed (0 of 269 real entries reaching it ever exhaust), declined by name.  `FFFFEA20 == 0`, bit 2 clear, `FFFFEA1E >= 0` (115 fixtures): `FFFFF24A` cleared, then a plain fallthrough into state 15's own entry (`006D68`) -- the SAME shared body `state4_step`'s own `FFFFEA20 == 0` arm already reaches, factored out this session into `_state15_ground_probe` (boundary.py) so both callers share it; every occurrence finds ground on the first test, the second test and the ground-not-found transition to state 12 stay declined exactly as `state4_plan`'s own module note already declines them.  `FFFFEA20 == 0`, bit 2 clear, `FFFFEA1E < 0` (186 fixtures): `FFFFF24A` cleared, then `state_26_box_scan` (a 20-entry table at FFFFDAD4, distinct from `game.movement.BOX_SCAN_TABLE` though it shares the shape); a match (14 fixtures) stores it (`STATE_26_BOX_FOUND`/`_STATUS`/`_PAYLOAD`, `pickups.MOVEMENT_SOUND_CUE`) and exits unchanged; no match re-runs the already-recovered grid cell lookup and tests `POSITION_X`'s own low 5 bits -- below `0x10` (85 fixtures) the FIRST of two grid-byte tests transitions to state 14 at the SAME `578E` merge point the high-low5 arm's own double-pass reaches, but WITHOUT its `+0x20` advance (14 fixtures, every one via the first test, never the second, which stays declined); at/above `0x10` (64 fixtures) a two-test chain exits unchanged (58) or transitions to state 14 WITH the `+0x20` advance (6), reusing `_state0_state14_stores` directly (a separate ROM copy of state 0's own identical logic).  Four real register-residue bugs the FAST tier caught before the corpus matched: `005726`'s own unconditional `moveq #$1c,d0` (dead through every path but the two transitions) was missed as the exit register's own upper-half base on several arms; the box scan's own d0-d6/a0 residue (near_x/near_y/far_x/far_y at the head, the LAST examined entry's own x/y/index) was omitted entirely for the 'not-found' continuation; the actor scan's own d0-d3 (008180's own `movem.w` SIGN-EXTENDS into the full register for every entry reaching skip-kind or deeper, not just ones reaching `2E0A`/`81EA` -- the LAST such entry decides, which may be a LATER, shallower one than the last to reach the id search) and its own d3 tripled (`008184`-`008192`'s own record-address arithmetic doubles d3 three times building `a1`, for every entry reaching skip-kind, not only reached-id ones); and the ID-compare block's own four chained comparisons read `achievements.RECORD_TABLE`'s own entry (`a1`), not `conditions.FLAGGED`'s own found one (`a3`, eori'd but never compared).  Two real cost bugs: the box scan's own final `00581C rts` (the exhausted exit) was uncosted, and `0081EA`'s own `move.w #8,-(a7); rtr` / `clr.w -(a7); rtr` exit pair was missing from every one of its own four early-exit branches, both found via `factcheck check`'s own exact byte-for-byte diff, not guessed | costed one instruction-block at a time from the tracer on real fixtures over `census-005724-*` (all five recordings, 606 retained) | `gods_sega.game.player` new `state_26_step`/`state_26_box_scan`/`state_26_actor_scan`/`_state_26_id_search`/`_state_26_actor_box_test` (named `state_26_*`, underscore, to stay distinct from the existing misnomer's own `state26_*`); `gods_sega.boundary` new `state_26_plan` plus `_state15_ground_probe` (factored out of `state4_plan`, shared); candidate `'state-26'` (a standalone gate, the SAME shape every other player-state candidate is); the PRE-EXISTING candidate named `'state-26'` (0069AC, real index 20, `STATUS.md`'s own 18 September misnomer entry) is renamed `'state-20'` in `recovery.py` -- gate PC and every fixture/test unchanged, since every gate and fixture is keyed by PC, not by name; mutant `state-26-mutant-result` (`recovery._mutate_outcome`, the same STATE_INDEX/F24A-feeds-a-future-activation reasoning every other player-state mutant uses) | `tests/games/gods/test_state_26.py` (new: semantics unit tests for the head/box-scan/actor-scan arms plus all 606 retained fixtures across `census-005724-*` on five recordings, 598 MATCH, 8 correctly DECLINED, 0 mismatch; `factcheck check --perturb-upper-halves`: 583 MATCH, 7 DECLINED, 16 SKIPPED, 0 mismatch); `tests/games/gods/test_state26.py` updated (candidate name only, `'state-26'` -> `'state-20'`); `tests/games/gods/test_player_state.py` updated (`test_state_26_is_declined_by_name_not_guessed` replaced with `test_state_26_is_recovered_not_declined`, asserting MATCH not decline; candidate list extended); full `run_tests.py gods` 16,649 passed | `ca2b703b6fd595a8d8bd0109ff67f09511d1cda614c44e3c93aca4bfa215ced8`, 14,583 frames, `artifacts/gods/verify-state-26-ca2b703b6fd5`, **PASS, 312 hits, 2 fallbacks (z80 bank guard, exact)**; segment_verify at boundary-6000/12000 PASS (16/8, 1/0 hits/fallbacks -- the boundary-6000 fallbacks are the state-15-ground-not-found decline); mutant `state-26-mutant-result` DIVERGENCE at frame 2,645 (`artifacts/gods/verify-state-26-mutant`); `vblank_slide.py` on a VBlank-preempted fixture (`census-005724-fb408bc75597/005724-entry-p228.state`, 20 ticks): every landing EQUIVALENT | 312 hits, 2 fallbacks (tree: 1,883 hits, 92 fallbacks -- 22 z80 bank guard, 70 the state-15-ground-not-found decline) | this commit; MILESTONE SEAL: `tree_verify.py --candidate state-26` (a standalone gate, five leaves cold) PASS, 107,519 frames, `artifacts/gods/verify-state-26-leaves-2026-09-18`; `tree_verify.py --candidate camera-sprites` (forty gates, a shared-helper edit -- `_state15_ground_probe`/`_PLAYER_STATE_PLANNERS` -- reseals it too) PASS, 107,519 frames, bit-exact, `artifacts/gods/verify-camera-sprites-leaves-2026-09-18u`, fallbacks 8,857 -> **6,970** (the state-26 decline is gone, `player_state_plan` now composes it directly); MILESTONE SEAL commit follows
2026-09-18 | 00B944 creature pickup check (`docs/gods/blockers/2026-09-18-00A578.md`'s own ordering, second of `00A772`'s own three unconditional callees) / a probe into the already-recovered pickup check (`00BA8E`), the SAME "push scratch, call, write D2 back, force -1 on negative" shape `pickup_probe_plan` already proves over `010CD2`, but with no camera add (D0/D1 are the creature's own tracked position, already read by `00A772`'s own head) and one extra unconditional write before the call: `FFFFF382.l = 0x00200020`, i.e. `zones.HALF_WIDTH = zones.HALF_HEIGHT = 0x20` -- pickup_check's own caller-supplied box half-extents (010CD2's own call instead trusts whatever an unrelated, earlier part of the frame already left there).  A genuine NEW composition problem this session's own earlier leaves never hit: since this planner calls `pickup_check_plan` on the SAME live machine (an `AtomicPlan` is a description, never a mutation -- nothing this region's own head "writes" is actually applied before the composed sub-plan runs), the zone check two levels down would otherwise read whatever HALF_WIDTH/HALF_HEIGHT the RETAINED FIXTURE's own RAM happens to hold, not the 0x20/0x20 this instruction really seeds on real hardware -- caught immediately by `factcheck check` (482 of 733 fixtures MISMATCHed on exactly these two words, byte-for-byte, before the fix).  Resolved with `_ConstMachine`, a read-only view of the real machine with those two words overridden, passed to `pickup_check_plan` in place of the real one (only `peek_ram`/`peek_rom` are used by `_reader` and its own cost helpers, so the substitution is exact).  `creatures.LIFECYCLE` (the instance's own +8 word) is both the call's own D2 argument and its own result -- the SAME "lifecycle word $8(a5)" `docs/gods/blockers/2026-09-18-00A578.md`'s own `00A578` recon already named as gating whether `00A772` runs for this creature AT ALL next tick; on a negative pickup-check result, `creatures.FRAME_STEP` (already named, the animation step at +4) is forced to -1 and a second instance word at +6 (`creatures.LIFECYCLE_RESET`; what tracks it is not established this session) is cleared.  One further real bug the FAST tier caught after the `_ConstMachine` fix: the exit CCR used the write-back's own value (D2's own result) as the last flag-setter on BOTH arms, but the negative arm's own last flag-setting instruction is `clr.w 6(a5)` (Z=1, N=V=C=0), not the earlier write-back -- both are real, distinct MOVE-class instructions and only one runs last per arm | costed one instruction-block at a time from the tracer on real fixtures over `census-00B944-*` (all five recordings; 733 retained -- pickup_check's own internal branch diversity, not 00B944's own, drives the class count, since the path signature includes whatever the composed callee does) | `gods_sega.game.creatures` extended (`creature_pickup_check`, `LIFECYCLE`, `LIFECYCLE_RESET`); `gods_sega.boundary` new `creature_pickup_check_plan` plus `_ConstMachine` (a Gods-local RAM-overlay helper, not shared); candidate `'creature-pickup-check'` (a standalone gate, kept out of `camera-sprites` for the same native gate cap `state-18`/`creature-attack`/`creature-frame-offset` are); mutant `creature-pickup-check-mutant-result` (`recovery._mutate_result`, the same "every register restored to entry, the writes are the real effect" reasoning `creature-attack`'s own mutant already uses) | `tests/games/gods/test_creature_pickup_check.py` (new: a unit test for the pure wrapper's own negative derivation plus all 733 retained fixtures across `census-00B944-*` on five recordings, 732 MATCH, 1 correctly DECLINED (pickup_check's own found-special-timer arm, already unwitnessed there), 0 mismatch; `factcheck check --perturb-upper-halves`: identical, 732/1/0); full `run_tests.py gods` 17,385 passed | `ca2b703b6fd595a8d8bd0109ff67f09511d1cda614c44e3c93aca4bfa215ced8`, 14,583 frames, `artifacts/gods/verify-creature-pickup-check-ca2b703b6fd5`, **PASS, bit-exact, 148 hits, 0 fallbacks**; segment_verify at boundary-6000/12000 PASS (0/18 hits, 0/0 fallbacks); mutant `creature-pickup-check-mutant-result` DIVERGENCE at frame 11,429 (`artifacts/gods/verify-creature-pickup-check-mutant`); `vblank_slide.py` on a VBlank-preempted fixture (`census-00B944-fb408bc75597/00B944-entry-p326.state`, 20 ticks): every landing EQUIVALENT | 148 hits, 0 fallbacks (tree: 20,811 hits, 78 fallbacks -- 36 observation deadline, 21 z80 bank guard, 20 vblank in span, 1 the found-special-timer decline) | this commit; MILESTONE SEAL: `tree_verify.py --candidate creature-pickup-check` (a standalone gate, five leaves cold) PASS, 107,519 frames, `artifacts/gods/verify-creature-pickup-check-leaves-2026-09-18`.  Next: `00A922` (the last of `00A772`'s own three unconditional callees; full recon already done this session, see the same blocker doc's own addendum), then the witnessed kind handlers
2026-09-18 | 00A922 world-event consume (`docs/gods/blockers/2026-09-18-00A578.md`'s own ordering, last of `00A772`'s own three unconditional callees) / four early declines by semantics (`mode-inactive`, `no-events`, `already-has-event` and `count-clamped` -- the last real ROM, never witnessed, declined by name).  Loop 1 (the event-list scan, `EVENT_LIST`, stride 6, up to 20 entries): each slot is skipped by its own `QUADRANT_WORD` mask (`skip-mask`) or by an excluded kind (`skip-excluded`, kind `0x45`), else box-tested against the creature's own tracked position (four compares: `ADDQ.w #8` nets a +8 far bound, then `SUBI.w #$30` off that same value nets a -0x28 near bound -- NOT the state-26 box scan's own margins, a real defect this session's own `factcheck check` caught directly against a traced fixture before the ROM's own two immediates were read correctly); the first entry whose (x, y) falls in the box is `found` and stops the scan, everything else is `tested`/skip and continues (a full sequential simulation, `_ec_walk_loop1`, tracks X across entries since ADDQ/SUBI set it but CMP/TST/ANDI don't); exhaustion is `no-match` (decline).  On `found`, loop 2 walks `game.movement.BOX_SCAN_TABLE` BACKWARDS (up to 200 entries) for an active object of the SAME (x, y, kind); `skip-inactive`/`skip-kind`/`skip-x` continue (their own `dbra` falls at the shared `00A9C4`); `skip-negative`/`skip-y` break the loop and full exhaustion (`no-object-match`) are real ROM, never witnessed, declined by name; a match stores the event into the instance and marks both slots consumed.  Three further real defects the FAST tier and `--perturb-upper-halves` caught in turn: `_ec_loop2_entry_cost` computed its own `continues` flag but never actually charged the shared `dbra`'s own cost for a continuing entry; the `lsr.w d3,d2` instruction (6cy when d3=0, 10cy when d3=2) was missing from the loop-1 walk entirely; the `found` exit path's own final `rts` was uncosted; and D6/D7 (`move.w #$c7,d6` / `move.w $f260.w,d7`, both word ops) didn't carry the entry's own upper half forward | RAM-only leaf, two bounded loops in sequence, no seam; every register the routine ever writes is a WORD op | gods_sega.game.creatures.event_consume; plan gods_sega.boundary.event_consume_plan / _ec_walk_loop1 / _ec_loop2_entry_cost; candidate 'event-consume' | tests/games/gods/test_event_consume.py (new: four unit tests for the semantics' own early declines, 61 fixtures MATCH across all five census-00A922-* directories, factcheck check and --perturb-upper-halves both clean 61/61, candidate-name test, segment_verify+mutant reference test); full run_tests.py gods 17,452 passed, 9 skipped | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-event-consume-fb408bc75597, PASS, bit-exact, 10,014 hits, 9 fallbacks (1 machine admission, 8 z80 bank guard); segment_verify at boundary-6000/12000 PASS (0 hits, the region isn't exercised at either instant); mutant event-consume-mutant-result DIVERGENCE at frame 28,474 (artifacts/gods/verify-event-consume-mutant); no VBlank-preempted (cut>0) fixtures in the census, vblank_slide not needed | tree: 19,779 hits, 19 fallbacks (1 machine admission, 17 z80 bank guard, 1 vblank in span) | this commit; MILESTONE SEAL: `tree_verify.py --candidate event-consume` (a standalone gate, five leaves cold) PASS on four (fb408bc75597, 7251bbd0ecf7, f40d7bcc9dda, f0ac19738f19), `ca2b703b6fd5` honestly NOT_EXERCISED (0 retained fixtures in its own census, matching), 107,519 frames total, `artifacts/gods/verify-event-consume-leaves-2026-09-18`.  Next: the witnessed kind handlers and their callees, then `00A772` as the family and `00A578` as the walk (per the same blocker doc's own ordering).

2026-09-18 | 00AA38 creature grid-cell lookup (first of the six further callees the ground/fall kind handlers need, `docs/gods/blockers/2026-09-18-00A578.md`'s own ordering) / one unconditional path, no branch, no store -- byte-for-byte `game.grid.grid_cell_at`'s own arithmetic (0063FA/010CBC's shared shape), a third call site parameterised on the creature instance's own POSITION_X/POSITION_Y instead of the fixed words or a caller's D0/D1; address left in A1 (not A0) | RAM/ROM-read only leaf, no seam | gods_sega.game.creatures.creature_grid_cell (reuses grid.grid_cell_at directly); plan gods_sega.boundary.creature_grid_cell_plan; candidate 'creature-grid-cell' | tests/games/gods/test_creatures.py (new: arithmetic-matches-grid_cell_at unit test, 4 fixtures MATCH across all four exercising census-00AA38-* directories, factcheck check and --perturb-upper-halves both clean, candidate-name test, segment_verify+mutant reference test); full run_tests.py gods 17,479 passed, 9 skipped | f0ac19738f190b5a1efa7ac5cd2677f6e0ce582df16483525fad1f95f9bc7068, 15,148 frames, artifacts/gods/verify-creature-grid-cell-f0ac19738f19, PASS, bit-exact, 109 hits, 0 fallbacks; segment_verify at boundary-6000 (0 hits) / boundary-12000 (1 hit) both PASS; mutant creature-grid-cell-mutant-result (A1 off by one) DIVERGENCE at frame 12,459 (artifacts/gods/verify-creature-grid-cell-mutant) | tree: 1,283 hits, 0 fallbacks | this commit; MILESTONE SEAL: `tree_verify.py --candidate creature-grid-cell` (a standalone gate, five leaves cold) PASS on four (fb408bc75597, 7251bbd0ecf7, f40d7bcc9dda, f0ac19738f19), `ca2b703b6fd5` honestly NOT_EXERCISED (0 retained fixtures, matching every other creature-family leaf), 107,519 frames total, `artifacts/gods/verify-creature-grid-cell-leaves-2026-09-18`.
2026-09-18 | 00AD68 ground-edge test (second of the six further callees) / a two-cell test over the grid table's own bytes at the address 00AA38 leaves in A1, gated by the creature's own POSITION_X low 5 bits; only 'no-match-near-edge' and 'no-match' (both D1=0) witnessed -- 'cell-low' and 'cell-high' (the flag actually found, D1=1) are real ROM, declined, unwitnessed by any of the four recordings that reach this leaf; `--perturb-upper-halves` caught a real defect first pass (moveq #0,d1 clears the WHOLE 32-bit register, not the low word only -- the plan wrongly preserved the entry upper half) | RAM/ROM-read only leaf, two witnessed arms, no seam | gods_sega.game.creatures.ground_edge_test; plan gods_sega.boundary.ground_edge_test_plan; candidate 'ground-edge-test' | tests/games/gods/test_creatures.py (new: four unit tests for all four semantic arms including the two declined ones, 14 fixtures MATCH across all four exercising census-00AD68-* directories, factcheck check and --perturb-upper-halves both clean after the D1 fix, candidate-name test, segment_verify+mutant reference test); full run_tests.py gods 17,479 passed, 9 skipped | f0ac19738f190b5a1efa7ac5cd2677f6e0ce582df16483525fad1f95f9bc7068, 15,148 frames, artifacts/gods/verify-ground-edge-test-f0ac19738f19, PASS, bit-exact, 29 hits, 0 fallbacks; segment_verify at boundary-6000 / boundary-12000 both PASS (0 hits, the region isn't exercised at either instant); mutant ground-edge-test-mutant-result (D1 XOR 1) DIVERGENCE at frame 13,979 (artifacts/gods/verify-ground-edge-test-mutant) | tree: 394 hits, 0 fallbacks | this commit; MILESTONE SEAL: `tree_verify.py --candidate ground-edge-test` (a standalone gate, five leaves cold) PASS on four (fb408bc75597, 7251bbd0ecf7, f40d7bcc9dda, f0ac19738f19), `ca2b703b6fd5` honestly NOT_EXERCISED (0 retained fixtures, matching), 107,519 frames total, `artifacts/gods/verify-ground-edge-test-leaves-2026-09-18`.  Next: 00ACA0/00AD88 (the ground-contact kind handlers, now unblocked -- both of the six further callees they need are recovered), then 00AE6C/00AED4 (the fall kind handlers, same two callees), then 00AA76/00AB50 (need 00AF3C/00AF52 first), then 00A772 as the family and 00A578 as the walk (per the blocker's own ordering).
2026-09-19 | 00ACA0 ground-contact kind handler (kind 4 of 00A772's own eight-entry table, the most frequent witnessed kind handler, `docs/gods/blockers/2026-09-18-00A578.md`'s own Decision -- 35 retained fixtures, 13 real path classes over four recordings) / idle (the hold timer's own decrement stays non-negative), the reload's own POSITION_X -4 step (applied unless a matching cell flag at -1(a1)/0x7F(a1) skips it -- `GROUND_SKIP_TEST_LOW_OFFSET`/`GROUND_SKIP_TEST_HIGH_OFFSET`), near-trigger (the near test's own 'cell-low' match: KIND cleared to 0, POSITION_Y masked to `& 0xFFF0`), the table-tail's own POSITION_Y step through `GROUND_STATE_TABLE` (ROM `00AE4C`, 13 signed words) and the far test into settle-continue / settle-reset (`FALL_PHASE`'s own decrement landing below `GROUND_SETTLE_FLOOR` transitions KIND to `KIND_FALL` = 2, the still-unrecovered fall kind handler `00AE6C`); near-trigger-cell-high and both far-trigger arms decline, unwitnessed by any of the four recordings that reach this leaf | composes creature_grid_cell (00AA38, called twice, the second time over the routine's own pending POSITION_X/Y write through an overlay reader) and ground_edge_test (00AD68, through two entry points: 00AD46, the near test, feeding cell_addr+0x100 through the (d16,An) addressing mode -- confirmed byte-for-byte identical arithmetic to the direct 00AD68 call, a real cross-reference finding -- and 00AD68 itself, the far test, over 0/1(a1) directly); never returns to its own caller -- the ROM's own tail is always a tail-jump into kind_frame_offset's own separately-armed gate (00AA50), the SAME "one gate hands off to a separately-armed gate" shape `player_state_plan`'s own states use for the shared tail, so the plan's own cost/writes cover only 00ACA0's own body, ending at `pc = 0x00AA50` with `a7` unchanged | `gods_sega.game.creatures.ground_contact_update`; plan `gods_sega.boundary.ground_contact_update_plan`; candidates 'creature-ground-contact', 'camera-sprites' (extended to forty-seven gates) | `tests/games/gods/test_creatures.py` (new: ten unit tests over the idle, reload/skip-test, near-trigger, the two declined near/far-trigger arms and settle-continue/-reset arms; all 35 retained fixtures across `census-00ACA0-*` on four recordings, 0 mismatches, 0 declines under the plain sweep; `factcheck check --perturb-upper-halves`: 35 MATCH, 0 mismatch -- two real defects the perturb sweep caught: D0's own upper word is NOT preserved from entry for any non-idle arm (`moveq #0,d0` at the reload head drives it instead, not the plan's first-draft assumption), and the fall-phase decrement's own X flag was not threaded into the final CMP/MOVE -- both fixed before this seal); full `run_tests.py gods` 17,524 passed, 9 skipped | `f0ac19738f190b5a1efa7ac5cd2677f6e0ce582df16483525fad1f95f9bc7068`, 15,148 frames, `artifacts/gods/verify-creature-ground-contact-f0ac1973`, PASS, bit-exact, 7 hits of 7 gates, 0 fallbacks; `segment_verify` at boundary-6000/12000 both PASS (0/0 hits, too rare for a 300-frame window); mutant `creature-ground-contact-mutant-result` DIVERGENCE at frame 14,033 (`artifacts/gods/verify-creature-ground-contact-mutant`) | 7 hits, 0 fallbacks | this commit
