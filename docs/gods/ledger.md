# Gods recovery ledger

Append-only, one line per recovered region or escalation, newest last.  The
grinder's only per-region record; `STATUS.md` is updated once per milestone.

Line format (pipe-separated, one line):

    date | entry / arms | shape | semantics | tests | cold run (node, frames, artifacts dir, status) | hits, fallbacks | commit or blocker

`hits` and `fallbacks` are the candidate worker's `candidate_stats` from the
named cold run's `comparison.json`.  A region whose hits are zero on the
recorded histories is `NOT_EXERCISED` and does not belong here.

## Rows

2026-09-15 | baseline (no region) | - | - | tests/games/gods/test_boot.py, test_recording.py | f0ac19738f19 (main at the time), 15,148 frames, artifacts/gods/verify-main-2026-09-15, PASS (original vs original); tree of two recordings 29,731 frames, artifacts/gods/verify-tree-2026-09-15, PASS | - | b117d68
2026-09-15 | 002806 camera follow step / hold, right, left x arms; y clamp; the x-negative, x-limit, y-negative clamps declined (unwitnessed) | RAM-only leaf returning to its caller | gods_sega.game.camera.camera_follow; plan gods_sega.boundary.camera_follow_plan; candidate 'camera' | tests/games/gods/test_camera.py (17) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-camera-main, PASS; tree 29,731 frames, artifacts/gods/verify-camera-tree, PASS; mutant camera-mutant-result DIVERGENCE at frame 329 (artifacts/gods/verify-camera-mutant) | 6,678 hits, 0 fallbacks (tree: 11,448 hits) | 708404e
2026-09-16 | 002806 camera follow step / the x-limit clamp arm (00282C), witnessed by f40d7bcc9dda (three path classes, 1,130 hits on the tree) | RAM-only leaf, one more arm | camera.camera_follow unchanged; boundary._LIMIT_ARMS / WITNESSED_CLAMPS | tests/games/gods/test_camera.py over every census directory (61 fixtures MATCH) | f40d7bcc9dda, 17,620 frames, artifacts/gods/verify-camera-f40d7bcc, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-tree-2026-09-16b, PASS; mutant DIVERGENCE at frame 1,540 (artifacts/gods/verify-camera-mutant-f40d7bcc) | tree: 41,109 hits, 0 fallbacks (was 39,979 hits, 1,130 fallbacks) | this commit
2026-09-16 | 0018C8 sprite emitter / off-screen x, off-screen y, cache hit after 1–8 slots, cache miss into an empty slot or the ninth (all witnessed on three recordings); the flipped-sprite arm declined (unwitnessed) | a leaf with one platform operation inside: RAM-only prefix, the inline VDP tile upload 001974–001988 ceded to the machine, the register-restore suffix — the first Gods seam, Aladdin's proven shape reproduced; the pause/resume runner extracted to genesis_re.seam | gods_sega.game.sprites.emit_sprite; plan gods_sega.boundary.sprite_emit_plan (+ sprite_emit_suffix); candidates 'sprites', 'camera-sprites' | tests/games/gods/test_sprites.py (148: 134 fixtures MATCH across census-0018C8-{fb408bc7,7251bbd0ecf7,f40d7bcc9dda}), tests/common/test_seam.py | f0ac19738f19, 15,148 frames, artifacts/gods/verify-sprites-f0ac1973b, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16b, PASS (camera-sprites); mutants sprites-mutant-result and sprites-mutant-register DIVERGENCE at frame 429 (artifacts/gods/verify-sprites-mutant-*) | 40,935 hits, 306 fallbacks (all scheduler refusals; 13,218 seams entered, 13,108 completed, 0 foreign returns); tree: 306,295 hits, 2,077 fallbacks (1,733 scheduler refusals, 344 seam deadlines), 82,757 seams entered, 81,822 completed | this commit
2026-09-16 | 001164 sprite emitter sibling / offscreen-x, offscreen-y, placed (no flip), placed (flip), all witnessed over four recordings; the list-full guard (`0011B8`, `LIST_FULL` at `FFEE30`) declined (unwitnessed) | RAM-only leaf, no platform operation; the same descriptor layout as `0018C8` but its own id-to-descriptor table (`0011E6`) and a fixed tile field instead of a cache | gods_sega.game.sprites.emit_static_sprite; plan gods_sega.boundary.static_emit_plan; candidates 'sprites-static', 'camera-sprites' (extended to three gates) | tests/games/gods/test_static_sprites.py (23 fixtures MATCH free of an interposed VBlank, of 82 retained; the VBlank-interposed ones are the scheduler's own admission refusal, not a plan gap) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-sprites-static-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16c, PASS (camera-sprites, three gates); mutant sprites-static-mutant-result DIVERGENCE at frame 457 (artifacts/gods/verify-sprites-static-mutant) | 15,551 hits, 134 fallbacks (all scheduler admission: an interrupt due mid-activation); tree: 454,447 hits, 3,571 fallbacks (3,227 scheduler admission incl. 1,494 at 001164, 344 seam deadline) | this commit
2026-09-16 | 004150 work-table reset / zero-fill (common, all four recordings) and poison-fill (0xFE, witnessed once on fb408bc75597) | RAM-only leaf, fully unrolled (no data-dependent loop count, so cost is constant per arm) | gods_sega.game.tables.reset_table; plan gods_sega.boundary.table_reset_plan; candidates 'table-reset', 'camera-sprites' (extended to four gates) | tests/games/gods/test_tables.py (9 fixtures MATCH over four recordings) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-table-reset-f0ac1973, PASS; tree of eight recordings, artifacts/gods/verify-camera-sprites-tree-2026-09-16d, PASS (camera-sprites, four gates); mutant table-reset-mutant-result DIVERGENCE at frame 429 (artifacts/gods/verify-table-reset-mutant) | 6,669 hits, 0 fallbacks; tree: 495,476 hits, 3,571 fallbacks (3,227 scheduler admission, 344 seam deadline, unchanged from before this region) | this commit
2026-09-16 | 0049DA spawn queue / 0, 1 or 2 active slots per tick, continuing or retiring, all witnessed over four recordings | RAM-only leaf that calls the already-recovered 001164 once per active slot (0-2 calls); no platform operation of its own | gods_sega.game.spawn_queue.scan_spawn_queue; plan gods_sega.boundary.spawn_queue_plan (reuses boundary._static_emit_cost, factored out of static_emit_plan); candidates 'spawn-queue', 'camera-sprites' (extended to five gates) | tests/games/gods/test_spawn_queue.py (18 fixtures MATCH over four recordings) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-spawn-queue-f0ac1973, PASS; tree of eight recordings, artifacts/gods/verify-camera-sprites-tree-2026-09-16e, PASS (camera-sprites, five gates); mutant spawn-queue-mutant-result DIVERGENCE at frame 1,021 (artifacts/gods/verify-spawn-queue-mutant) | 6,597 hits, 72 fallbacks (all scheduler admission); tree: 536,020 hits, 3,939 fallbacks (3,595 scheduler admission incl. 368 at 0049DA, 344 seam deadline) | this commit
2026-09-16 | 0063FA grid cell lookup / a single path class witnessed on every recording (no branch, no store) | RAM-only leaf, straight-line, no calls | gods_sega.game.grid.grid_cell; plan gods_sega.boundary.grid_cell_plan; candidates 'grid-cell', 'camera-sprites' (extended to six gates) | tests/games/gods/test_grid.py (6 fixtures MATCH over four recordings) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-grid-cell-f0ac1973, PASS; tree of eight recordings, artifacts/gods/verify-camera-sprites-tree-2026-09-16f, PASS (camera-sprites, six gates); mutant grid-cell-mutant-result (a register, since the routine stores nothing) DIVERGENCE at frame 6,185 (artifacts/gods/verify-grid-cell-mutant) | 6,709 hits, 86 fallbacks (all scheduler admission); tree: 577,899 hits, 4,366 fallbacks (4,022 scheduler admission incl. 427 at 0063FA, 344 seam deadline) | this commit
2026-09-16 | 00FDB8 footprint stamp / rows × cells from the definition's height and width bytes (1×1, 3×1, 1×3, 1×2 witnessed over four recordings); the no-footprint arm (width bit 7) declined (unwitnessed); negative heights and footprints over 64 cells declined | RAM-only leaf over a caller-supplied record (A2 the definition, A5 the undo cursor, D0/D1 the position); loops bounded by record bytes, cost a formula in (rows, cells) | gods_sega.game.grid.stamp_footprint; plan gods_sega.boundary.footprint_stamp_plan; candidates 'footprint', 'camera-sprites' (seven gates) | tests/games/gods/test_footprint.py (32: 27 fixtures MATCH across census-00FDB8{,-7251bbd0ecf7,-f40d7bcc9dda,-fb408bc75597}, the VBlank-pre-empted ones through pathfacts.region_only) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-footprint-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16g, PASS (camera-sprites); mutant footprint-mutant-result DIVERGENCE (artifacts/gods/verify-footprint-mutant) | 11,517 hits, 65 fallbacks (all scheduler refusals); tree: 648,130 hits, 4,882 fallbacks (4,538 scheduler refusals, 344 seam deadlines) | this commit
2026-09-16 | 00FC8E solid drawer / 0-9 visible cells per call over a 1×1 to 3×3 rows×cells grid (the same definition bytes 00FDB8 reads), off-screen cells skipped on the x or the y test, the type-id table scan matching within 1-2 mismatches on every recording; the inline VDP upload arm (a table entry with the tile index's sign bit set) and an unterminated-or-unmatched table scan declined (unwitnessed) | RAM-only leaf over the same caller-supplied definition as 00FDB8, appending into the sprite list 0018C8/001164 already write; the earlier "per-object-type diversity" screening did not hold once censused with the current tracer | gods_sega.game.solids.draw_solid; plan gods_sega.boundary.draw_solid_plan; candidates 'solid-draw', 'camera-sprites' (eight gates) | tests/games/gods/test_solids.py (60: 9 unit + 51 fixtures MATCH across census-00FC8E{,-7251bbd0ecf7,-f40d7bcc9dda,-fb408bc75597}) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-solid-draw-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16h, PASS (camera-sprites, eight gates); mutant solid-draw-mutant-result (a register: the routine's own last write is the unconditional list-head pointer) DIVERGENCE at frame 429 (artifacts/gods/verify-solid-draw-mutant) | 11,472 hits, 110 fallbacks (all scheduler admission); tree: 718,111 hits, 5,648 fallbacks (5,304 scheduler admission incl. 766 at 00FC8E, 344 seam deadline) | this commit
2026-09-16 | 00FE08 animation step / the 'idle' arm (the live record's own moving flag negative) on every recording; the 'moving' arm declined -- common (about 0.6% of calls), not merely unwitnessed, but it calls the unrecovered coroutine and per-type dispatch at 00FFF0 | RAM-only leaf, refreshes the shared frame-budget word on both arms; a3 = a1+6 left changed regardless of arm | gods_sega.game.animation.animation_step; plan gods_sega.boundary.animation_step_plan; candidates 'animation-step', 'camera-sprites' (nine gates) | tests/games/gods/test_animation.py (53: 3 unit + 6 idle-arm fixtures MATCH, 42 moving-arm fixtures DECLINED, across census-00FE08{,-7251bbd0ecf7,-f40d7bcc9dda,-f0ac19738f19}) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-animation-step-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16i, PASS (camera-sprites, nine gates); mutant animation-step-mutant-result DIVERGENCE at frame 430 (artifacts/gods/verify-animation-step-mutant) | 11,542 hits, 40 fallbacks (38 scheduler admission, 2 unsupported domain); tree: 788,232 hits, 6,274 fallbacks (5,609 scheduler admission, 344 seam deadline, 321 unsupported domain at 00FE08) | this commit
2026-09-16 | 010332 countdown check / the 'idle' (control byte zero) and 'waiting' (countdown not yet zero) arms on every recording; the 'trigger' arm (countdown reaches zero) declined -- calls one of two unrecovered routines (01158C/0115D4) | RAM-only leaf, no frame, no calls on the recovered arms; caller-supplied control byte (A3) and countdown word (A5), not fixed globals | gods_sega.game.timers.countdown_check; plan gods_sega.boundary.countdown_check_plan; candidates 'countdown-check', 'camera-sprites' (ten gates) | tests/games/gods/test_timers.py (48: 3 unit + 10 idle/waiting fixtures MATCH, 33 trigger fixtures DECLINED, across census-010332{,-7251bbd0ecf7,-f40d7bcc9dda,-f0ac19738f19}) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-countdown-check-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16j, PASS (camera-sprites, ten gates); mutant countdown-check-mutant-result (a register, since the 'idle' arm stores nothing) DIVERGENCE at frame 469 (artifacts/gods/verify-countdown-check-mutant) | 3,238 hits, 68 fallbacks (33 scheduler admission, 35 unsupported domain); tree: 801,195 hits, 6,610 fallbacks (5,713 scheduler admission, 344 seam deadline, 553 unsupported domain across 00FE08/010332) | this commit
2026-09-16 | 010A14 collision gate / the 'held' (phase<=7, a global gate word set) and 'gated' (the phase advance's +-4 residue nonzero) arms, witnessed on the two of eight recordings that reach this entry; the 'collision' arm (residue zero, calls 010CBC) declined, and the phase>7 arm ('over', real ROM code) declined as unwitnessed on either recording | RAM-only leaf, no frame at all (a7 unchanged besides the return pop); caller-supplied state struct (A5, a phase word and a moving-state word), D0 the only other input | gods_sega.game.movement.collision_gate; plan gods_sega.boundary.collision_gate_plan; candidates 'collision-gate', 'camera-sprites' (eleven gates) | tests/games/gods/test_movement.py (25: 3 unit + 8 held/gated fixtures MATCH, 12 collision/over fixtures DECLINED, across census-010A14{,-f0ac19738f19}) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-collision-gate-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16k, PASS (camera-sprites, eleven gates); mutant collision-gate-mutant-result DIVERGENCE at frame 469 (artifacts/gods/verify-collision-gate-mutant) | 1,347 hits, 93 fallbacks (13 scheduler admission, 80 unsupported domain); tree: 806,765 hits, 6,983 fallbacks (5,748 scheduler admission, 344 seam deadline, 891 unsupported domain across three declined-call regions) | this commit
2026-09-16 | 00BCCE zone check / a box test around the player's own position (0063FA's GRID_X/GRID_Y), widened on two specific levels, against a caller-supplied position and half-extents; fully witnessed over four recordings -- no declines | RAM-only leaf, an 8-register save/restore frame the routine never writes back through (the whole box arithmetic is scratch); only a shared cooldown word and D2's own final value are durable | gods_sega.game.zones.zone_check; plan gods_sega.boundary.zone_check_plan; candidates 'zone-check', 'camera-sprites' (twelve gates) | tests/games/gods/test_zones.py (75: 6 unit + 69 fixtures MATCH across census-00BCCE-fresh{,-f0ac19738f19,-7251bbd0ecf7,-f40d7bcc9dda}; the stale pre-staged census-00BCCE directory was superseded -- its 13 "calls 0F4472" classes were a VBlank-in-interrupt-handler misattribution) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-zone-check-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16l, PASS (camera-sprites, twelve gates); mutant zone-check-mutant-result (a register: the 'held'/'outside' arms, the great majority, store nothing durable) DIVERGENCE at frame 475 (artifacts/gods/verify-zone-check-mutant) | 4,316 hits, 35 fallbacks (all scheduler admission); tree: 881,371 hits, 7,335 fallbacks (6,100 scheduler admission, 344 seam deadline, 891 unsupported domain across three declined-call regions) | this commit
2026-09-16 | 00126A particle drawer's own emitter / off-screen x, off-screen y, uploaded no-flip, uploaded flip, all witnessed over four recordings; no cache at all, so every on-screen call is a seam (no arm declined) | Aladdin recipe 2/3 (0018C8's own shape) reproduced with this routine's own addresses: no id-to-offset table (the caller passes the descriptor byte offset directly), its own fixed attribute bit (0x4000, not 0018C8's 0x2000); the ceded block's own data-loop length (the descriptor's tile byte count) is never modeled, only charged for real | gods_sega.game.sprites.emit_particle_sprite; plan gods_sega.boundary.particle_emit_plan (+ particle_emit_suffix); candidates 'particle-emit', 'camera-sprites' (thirteen gates) | tests/games/gods/test_particles.py (46: 3 unit + 58 fixtures MATCH across census-00126A-fresh{,-fb408bc75597,-7251bbd0ecf7,-f40d7bcc9dda}) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-particle-emit-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16m, PASS (camera-sprites, thirteen gates); mutant particle-emit-mutant-result DIVERGENCE at frame 469 (artifacts/gods/verify-particle-emit-mutant) | 7,330 hits (3,314 seams entered, 3,026 completed), 335 fallbacks (53 scheduler admission, 282 seam deadline); tree: 955,464 hits, 105,964 seams entered, 101,583 completed, 11,118 fallbacks (6,498 scheduler admission, 3,728 seam deadline, 891 unsupported domain, 1 gate-without-planner foreign-return edge, tree still bit-exact) | this commit
2026-09-16 | 014084 hazard tick / 'paint' (inactive, or an active object whose grid cell isn't 1: a tile-array write clamped to FFBBAA-FFC156) and 'spawn' (grid cell 1: a sound request, a bounded 20-entry FFF90E pool scan and fill, whether or not a slot was free), all witnessed over four recordings; the 'trigger' arm (rare, gated by a parallel-table byte and a counter, calling unrecovered 00F828) declined | RAM-only leaf, no save/restore frame at all (D4/D5/A0/A1/A2 are live scratch); two disjoint bodies behind one early branch, sharing the same definition-style position offset (OBJECT_X/OBJECT_Y) and the level grid 0063FA/00FDB8 already read; each adda.w in the tile-array address sign-extends its own word operand separately (not one combined offset) | gods_sega.game.hazard.hazard_tick; plan gods_sega.boundary.hazard_tick_plan; candidates 'hazard-tick', 'camera-sprites' (fourteen gates) | tests/games/gods/test_hazard.py (127: 7 unit + 113 fixtures MATCH/DECLINED across census-014084-fresh{,-f0ac19738f19,-7251bbd0ecf7,-f40d7bcc9dda}) | fb408bc75597, 34,904 frames, artifacts/gods/verify-hazard-tick-fb408bc75597, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16n, PASS (camera-sprites, fourteen gates); mutant hazard-tick-mutant-result DIVERGENCE at frame 2,628 (artifacts/gods/verify-hazard-tick-mutant) | 26,075 hits, 218 fallbacks (185 scheduler admission, 33 unsupported domain); tree: 1,018,584 hits, 11,755 fallbacks (7,038 scheduler admission, 3,728 seam deadline, 988 unsupported domain, 1 gate-without-planner foreign-return edge, tree still bit-exact) | this commit
2026-09-16 | 010A14 collision gate / the 'collision' arm extended: 010CBC (grid.grid_cell_at, 0063FA's own arithmetic parameterised on D0/D1 -- no direct caller, so no separate gate; used only as this arm's own callee) plus a near/mid/far grid test in the direction the moving-state word selects, witnessed over the two recordings that reach this entry (six census path classes); a further gate byte in a second caller-supplied record (A3) is zero on every occurrence ('collision-held', the tail runs untouched) -- nonzero would enter an unbounded grid search this module does not model ('collision-deep', declined as unwitnessed); the phase>7 arm ('over') stays declined as unwitnessed | RAM-only leaf that calls the already-recovered arithmetic inline (010CBC), the 0049DA-calls-001164 shape without a separate gate since 010CBC has no direct caller of its own | gods_sega.game.grid.grid_cell_at; gods_sega.game.movement.collision_gate extended ('collision-clear'/'collision-held'/'collision-deep' replacing the old declined 'collision' arm); plan gods_sega.boundary.collision_gate_plan extended; candidates 'collision-gate', 'camera-sprites' (unchanged gate count, fourteen gates) | tests/games/gods/test_movement.py (extended unit coverage plus all 20 retained fixtures across census-010A14{,-f0ac19738f19} MATCH, none declined) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-collision-gate2-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16p, PASS (camera-sprites, fourteen gates); mutant collision-gate-mutant-result DIVERGENCE at frame 469 (artifacts/gods/verify-collision-gate2-mutant) | 1,426 hits, 14 fallbacks (all scheduler admission, down from 93 fallbacks incl. 80 unsupported domain); tree: 1,019,144 hits, 11,195 fallbacks (7,040 scheduler admission, 3,728 seam deadline, 426 unsupported domain across two declined-call regions, 1 gate-without-planner foreign-return edge, tree still bit-exact) | this commit
2026-09-16 | 010332 countdown check / the 'trigger' arm extended: the countdown reload and frequency word (both unconditional once triggered), then either the unrecovered 0091BC pool ('trigger-deep', declined) or a direction-mirrored screen window test (01158C/0115D4's own shape, BACK and FORWARD testing the X bound in opposite order) and, inside it, a bounded 20-entry pool scan+fill shaped like hazard.py's own ('trigger-reject': outside the window, no pool touch; 'trigger-spawn': inside it, a free slot filled), witnessed over four recordings (43 fixtures); the pool exhausted ('trigger-pool-full') stays declined as unwitnessed | RAM-only leaf with a transient two-register frame plus a nested call's own stack residue (the bsr's return address and the callee's own move.l a0,-(a7), both durable until the matching pops); the callee never returns control anywhere but back into this activation | gods_sega.game.timers.countdown_check extended ('trigger-deep'/'trigger-reject'/'trigger-spawn'/'trigger-pool-full' replacing the old declined 'trigger' arm); plan gods_sega.boundary.countdown_check_plan extended | tests/games/gods/test_timers.py (extended unit coverage plus 38 of 43 retained fixtures across census-010332{,-7251bbd0ecf7,-f0ac19738f19,-f40d7bcc9dda} MATCH, 5 DECLINED (0091BC calls)) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-countdown-check2-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16p, PASS (camera-sprites, fourteen gates); mutant countdown-check-mutant-result DIVERGENCE at frame 469 (artifacts/gods/verify-countdown-check2-mutant) | 3,273 hits, 33 fallbacks (all scheduler admission, down from 68 fallbacks incl. 35 unsupported domain); tree: 1,019,144 hits, 11,195 fallbacks (7,040 scheduler admission, 3,728 seam deadline, 426 unsupported domain across two declined-call regions, 1 gate-without-planner foreign-return edge, tree still bit-exact) | this commit
2026-09-16 | 00470C trigger conditions / kinds 0-3, 5-12, 15, 16 with every witnessed (kind, arm) pair (membership kinds per compare position); kind 4 (unwitnessed), kinds 13/14 (the score through 00364C), divs by zero or overflow, and unwitnessed compare positions declined | the first Gods dispatcher: one gate, a ROM table indexed by D5 into seventeen small RAM-only predicates, the kinds as arms (Aladdin's family-dispatch shape with the selector in a register) | gods_sega.game.conditions.evaluate; plan gods_sega.boundary.condition_plan; candidates 'conditions', 'camera-sprites' (fifteen gates); census --classifier reg:d5.w | tests/games/gods/test_conditions.py (129 fixtures MATCH across census-00470C{,-fb408bc75597,-f40d7bcc9dda,-7251bbd0ecf7,-f0ac19738f19}) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-conditions-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16p, PASS (camera-sprites); mutant conditions-mutant-outcome (a failing condition reported as passing) DIVERGENCE (artifacts/gods/verify-conditions-mutant); the register mutant was blind (dead residue) and was dropped | 5,247 hits, 36 fallbacks (all scheduler refusals); tree: 1,055,949 hits, 11,521 fallbacks (7,366 scheduler refusals, 3,728 seam deadlines, 426 declined arms of other regions, none at 00470C) | this commit
2026-09-16 | 00364C score conversion / 1, 2 and 3 division-digit values (0-999) witnessed over four recordings, both from the score-update call sites 0035B2/003604; a fourth division (value >= 1000) and a negative value are declined as unwitnessed; kinds 13/14 of 00470C still cannot be admitted -- no recording ever calls the trigger conditions with kind 13 or 14 (checked against all five condition census directories: no 000D/000E fixture exists), so the (kind, arm) pair stays unwitnessed independent of this routine's own recovery | RAM-only leaf: packs a value into four caller-supplied words as BCD digit pairs on top of whatever they already hold (each digit a 68000 abcd extended-add, threading the caller's own X flag in as the first digit's carry-in and BCD-carrying out across words); an internal bsr/rts chain (four entry points into one shared tail) stays inside the region -- its own return-address push is a real, transient RAM write the plan must model, and only the last digit's push survives in final RAM | gods_sega.game.score.convert_score; plan gods_sega.boundary.score_convert_plan; candidate 'score-convert', 'camera-sprites' (sixteen gates) | tests/games/gods/test_score.py (6 unit + 12 fixtures MATCH across census-00364C{,-f0ac19738f19,-7251bbd0ecf7,-f40d7bcc9dda}) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-score-convert-f0ac1973, PASS; segment_verify at boundary-6000/12000 PASS, 0 fallbacks; mutant score-convert-mutant-result DIVERGENCE at frame 675 (artifacts/gods/verify-score-convert-mutant) | 56 hits, 0 fallbacks | this commit
2026-09-16 | 00BA8E / 010CD2 (re-screened) | escalation: NEW_GODS_SUBSYSTEM | - | - | - | - | blocker docs/gods/blockers/2026-09-16-00BA8E.md; confirmed by fresh disassembly and a census of 00BA8E (32 real path classes, fb408bc75597): 25/32 classes are a clean, bounded, RAM-only "no neighbour found" arm (admissible in principle, not carried through this session); 7/32 call 013264, which jumps through a ROM table at 12D04 indexed by an object-type value -- a genuine per-type dispatch; 010CD2 calls the unrecovered 0091BC on one path and branches on a literal state value at 010D7C on the others.  Confirms the 15 Sep note with hard evidence; not attempted further | escalate commit
2026-09-16 | 00F828/00F86A proximity table search-and-add / the 'not-found' search (all 40 entries examined, three per-position shapes: miss, close (key's first word matches, second does not), stale (both match, timer not negative)) followed by the free-slot add, witnessed over two recordings (fb408bc75597, 7251bbd0ecf7; not exercised on f0ac19738f19 or f40d7bcc9dda); the 'trigger' arm (a matching, still-negative-timer entry, falling into 00F8A2's own caller-record dispatch and a double stack return past both frames) and 'pool-full' (all 40 occupied) stay declined -- the first real ROM code, unwitnessed | 00F828 owns its own internal call into 00F86A (the shape 0049DA's calls into 001164 proved); RAM-only, no separate gate for 00F86A; a redundant key recompute in the add step matches the ROM's own | gods_sega.game.hazard.proximity_search/proximity_add; plan gods_sega.boundary.proximity_plan; candidate 'proximity', 'camera-sprites' (seventeen gates) | tests/games/gods/test_proximity.py (45: 4 unit, 39 fixtures MATCH/DECLINED across census-00F828{,-7251bbd0ecf7}, candidate names, reference) | fb408bc75597, 34,904 frames, artifacts/gods/verify-proximity-fb408bc75597, PASS; mutant proximity-mutant-result DIVERGENCE at frame 12,890 (artifacts/gods/verify-proximity-mutant) | 8 hits, 25 fallbacks (all unsupported domain: trigger arm) | this commit
2026-09-16 | 013264 pickup award / item codes 1-27 (three groups, with and without the time bonus, the cue and the consumed slot), specials -1, -2 (sound off and on), -3; codes -4 and below (the continuation into 013316) and code 0 declined | RAM-only leaf over the pickup grid byte before A0 and the item record selected through the ROM table 012D04 (records, not handlers: the blocker of the same day corrected) | gods_sega.game.pickups.collect; plan gods_sega.boundary.pickup_award_plan; candidates 'pickups', 'camera-sprites' (nineteen gates) | tests/games/gods/test_pickups.py (36 fixtures across census-013264-{fb408bc75597,f40d7bcc9dda,7251bbd0ecf7,f0ac19738f19}: 25 MATCH, 11 declined -4) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-pickups-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16t, PASS (camera-sprites); mutant pickups-mutant-result DIVERGENCE (artifacts/gods/verify-pickups-mutant) | 154 hits, 2 fallbacks (1 scheduler refusal, 1 declined -4); tree: 1,033,633 hits, 11,631 fallbacks (7,199 scheduler refusals, 3,728 seam deadlines, 12 declined -4 pickups, the rest other regions declined arms) | this commit
2026-09-16 | 014A3C next-random draw / a single path class (no branch), 3,971 calls over 34,904 frames from many sites, only two of them 00BA8E's own found-effect composition | RAM-only leaf, fixed cost (no branch) | gods_sega.game.effects.next_random; plan gods_sega.boundary.next_random_plan; candidates 'next-random', 'camera-sprites' (twenty gates) | tests/games/gods/test_pickup_check.py (2 fixtures MATCH) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-next-random-f0ac1973, PASS; mutant next-random-mutant-result (a register: the drawn word, not the stored cursor -- the generic "flip the last write" mutation corrupts the cursor itself, which can wrap onto an odd address and fault the 68000 on its own next read, a crash rather than a clean divergence) DIVERGENCE at frame 1,341 (artifacts/gods/verify-next-random-mutant2) | 727 hits, 5 fallbacks (all scheduler admission) | this commit
2026-09-16 | 00932C effect pool add / the first free slot of a 20-entry pool (0-19 skips before the found test), or the pool-full arm (all 20 occupied, itself witnessed, 46/452 occurrences on its own census) | RAM-only leaf, cost bounded by the pool's own 20 slots (the "unrolled test count" shape 0018C8's cache scan and 00F828's table search share); the general form hazard.py's own '_spawn' inlines with d2=d3=0 | gods_sega.game.hazard.effect_pool_add; plan gods_sega.boundary.effect_pool_add_plan; candidates 'effect-pool-add', 'camera-sprites' (twenty-one gates) | tests/games/gods/test_pickup_check.py (21 fixtures MATCH) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-effect-pool-add-f0ac1973, PASS; mutant effect-pool-add-mutant-result DIVERGENCE at frame 1,341 (artifacts/gods/verify-effect-pool-add-mutant) | 89 hits, 0 fallbacks | this commit
2026-09-16 | 00BA8E pickup check / the clean arm (a two-axis box clamp bounding the pickup grid scan -- near/far/none sub-arms per axis, up to four ways to bail, the array-append side effect, common on some recordings); the found arm's own two compositions, found-sound (the box's own residue at or below the award: an immediate sound cue) and found-effect (residue above a nonzero award: two random jitters through the already-recovered next_random, one add into the already-recovered effect_pool_add); found-special (the -1 pickup's own further call), found-message (the digit-split tail), the award-zero bare exit, either jitter draw's own default-mask branch, the second draw's own negative branch and the pool-full arm within this composition declined as unwitnessed | a composition over the already-recovered zone check (00BCCE), pickup award (013264), next-random (014A3C) and effect-pool-add (00932C), each owned as a call (the shape 00462C's evaluator and 0049DA's spawn queue proved) | gods_sega.game.pickups.pickup_check; plan gods_sega.boundary.pickup_check_plan; candidates 'pickup-check', 'camera-sprites' (all twenty-two gates) | tests/games/gods/test_pickup_check.py (160: 5 unit + 155 fixtures MATCH/DECLINED across census-00BA8E-fresh{,-f0ac19738f19,-7251bbd0ecf7,-f40d7bcc9dda}, four recordings) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-pickup-check-f0ac1973b, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16v, PASS (camera-sprites, all twenty-two gates); mutant pickup-check-mutant-result DIVERGENCE at frame 469 (artifacts/gods/verify-pickup-check-mutant) | 4,260 hits, 91 fallbacks (34 scheduler admission, 12 declined -4 continuations across both gates, 15 award-zero, 41 second-jitter-negative); tree: 1,041,739 hits, 13,151 fallbacks (7,939 scheduler admission, 3,728 seam deadline, 1,483 declined arms, 1 gate-without-planner foreign-return edge, tree bit-exact) | this commit; a real defect caught before landing: composing 013264's own call re-applied its raw stores after the composition's own already-correctly-ordered write set, letting the award's own sound cue silently outlive this routine's own overwrite on the found-sound arm -- invisible to every 68000-side fact (cycles, registers, RAM) and to every per-region history-verify, since it only desyncs the Z80 sound co-processor by a few instructions; caught by the eight-recording tree (fb408bc75597 frame 6,840), fixed by not re-applying it, confirmed by re-running the tree
2026-09-16 | 013316 grid inverse and debris burst / the sound-off arm (the only one witnessed): a bounded 80-slot pool scan (the same shared-counter-across-particles shape, up to 8 particles per call, each a random impact cue through the already-recovered next-random and a ROM table offset word); a real precondition fixed the collect() semantics too -- codes -4 and below award BIG_VALUE (10000) unconditionally before falling into this routine, not a silent decline as first modelled; the sound-on arm (a fixed award, no burst), the rate-limit's own abort and arm-itself sub-arms and a pool-exhausted arm decline as unwitnessed | 013264's own code -4-and-below cascade falls straight through with no separate call, so 013316 has no gate of its own -- the composition owns it inside `pickup_award_plan` itself (PICKUP_AWARD_ENTRY), the shape 00BA8E owns its own callees | gods_sega.game.pickups.grid_inverse_award (+ grid_inverse_position); plan gods_sega.boundary._grid_inverse_award_plan, called from pickup_award_plan; candidates 'pickups', 'camera-sprites' | tests/games/gods/test_pickup_check.py (4 unit) + tests/games/gods/test_pickups.py (all eleven code -4 fixtures across census-013264-{7251bbd0ecf7,f0ac19738f19,f40d7bcc9dda,fb408bc75597} now admitted, not declined) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-pickups-f0ac1973b, PASS (155 hits, up from 154 -- the one code -4 occurrence on this history is now admitted); tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16w, PASS (camera-sprites, tree bit-exact); mutant pickups-mutant-result DIVERGENCE at frame 601 (artifacts/gods/verify-pickups-mutant2) | 155 hits, 1 fallback (scheduler admission); tree: 1,041,676 hits, 13,142 fallbacks (declined arms incl. 12 pickup-check-side -4 declines that still fall to the original's own 013264 gate, now admitted there) | this commit
2026-09-16 | 010CD2 pickup probe / a single path (no branch on the composition's own instructions): push the caller-supplied record's (d0,d1,d2), add the camera to (d0,d1), call the already-recovered pickup check 00BA8E with the record's own +8 word as d2, write the result back to the record, and on a negative result store a -1 sentinel to the record's +4 word; fully witnessed on all 412 calls across the four fixture recordings, no arm declined | a caller-supplied record's own camera-relative call into the already-recovered pickup check, composed by calling pickup_check_plan itself with a synthetic register file for the point 00BA8E is entered (one level deeper than the 0049DA-calls-001164 shape); the original 15-Sept blocker's reading that 010CD2 also called the unrecovered 0091BC and branched on 010D7C was a misattribution -- a fresh census found those belong to a separate adjacent routine (010CF8 onward) that 010CD2 never reaches | gods_sega.game.pickups.pickup_probe; plan gods_sega.boundary.pickup_probe_plan; candidates 'pickup-probe', 'camera-sprites' (twenty-three gates) | tests/games/gods/test_pickup_check.py (fixture-parametrized across all 32 retained fixtures, MATCH; unit test) | f0ac19738f19, 15,148 frames, artifacts/gods/verify-pickup-probe-f0ac1973, PASS; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-16x, PASS (camera-sprites, all twenty-three gates, tree bit-exact); mutant pickup-probe-mutant-result DIVERGENCE at frame 469 (artifacts/gods/verify-pickup-probe-mutant3 -- the generic result-byte and a register mutant both proved unusable first: the byte mutant crashes the 68000 by corrupting a creature record an unrecovered coroutine reads later, the register mutant is blind since d0 is scratch on return, so this mutant drops the composition's own writes entirely instead) | 3,248 hits, 58 fallbacks (23 scheduler admission, 35 unsupported domain: pickup check found-jitter-y-negative arm not witnessed); tree: 1,041,708 hits, 13,293 fallbacks, tree bit-exact | this commit
2026-09-16 | 00FFF0 line walker resume (object copy) / all four bodies (toward ±x, shallow/steep), the yield on the budget and the completion on the counter, with and without the minor-axis wrap; a record whose continuation is not a body, and a zero budget, declined | the first stateful subsystem: a persistent 18-byte walker record (phase, position, sign, spans, error, counter) stepped by a budget and re-armed; game state, not a recovery continuation | gods_sega.game.walker (start/run/load/stores, two copies); plan gods_sega.boundary.walker_resume_plan; candidates 'walker', 'camera-sprites' (twenty-four gates) | tests/games/gods/test_walker.py (7, the semantics), test_walker_resume.py (35 fixtures MATCH across census-00FFF0{,-f40d7bcc9dda,-7251bbd0ecf7,-f0ac19738f19}; consecutive invocations over 300 frames) | fb408bc75597, 34,904 frames, artifacts/gods/verify-walker-fb408bc7-b, PASS; tree of eight recordings, artifacts/gods/verify-camera-sprites-tree-walker-b, PASS (camera-sprites); mutant walker-mutant-result (the re-armed x one off) DIVERGENCE at frame 2,260 (artifacts/gods/verify-walker-mutant); a counter-off mutant drove the original into a cartridge write, a fault not a divergence, and was replaced | 169 hits, 1 fallback (a z80 bank guard refusal) of 170 calls; tree: 1,047,974 hits, 6,906 fallbacks (the 321 `moving` declines of 00FE08 remain until its arm owns the call) | this commit
2026-09-17 | 00FE08 animation step / the 'moving' arm's own call into the already-recovered walker resume 00FFF0 (game/walker.py), owned the way 0049DA owns 001164: 'moving-continue' (the walk has not finished as of this call) and 'moving-complete' (it has, more than one waypoint left -- the next waypoint loaded from the definition's own table, the slot byte reset to -1), both witnessed on every recording that reaches the arm; 'moving-coldstart' (the walk finishes with at most one waypoint left, falling into a per-object-type waypoint dispatch 00FEC0/00FF54) censused fresh over all eight recordings and unwitnessed on every one: declined, along with a zero budget and a record whose continuation is not one of the walker's own bodies | a leaf composing a call into an already-recovered routine (00FFF0's own cost fragments, _WR_HEAD/_WR_STEP/_WR_TAIL, reused directly -- 00FFF0 *is* this arm's bsr target, not a twin), plus its own head/tail cost fragments and register residue (the frame restore undoes d0-d2/a1/a2, so only d3/d4/d5/d6/d7/a0/a3/a5 differ from entry; d3's own X residue on 'moving-complete' comes from the waypoint-index SUBQ/ADD chain, not the walker's) | gods_sega.game.animation.animation_step extended; plan gods_sega.boundary.animation_step_plan extended (_animation_idle_plan/_animation_moving_plan); candidates 'animation-step', 'camera-sprites' (twenty-four gates, unchanged count) | tests/games/gods/test_animation.py (57: unit coverage for all four 'moving-*' arms plus idle, and all 43 retained fixtures across census-00FE08{,-7251bbd0ecf7,-f40d7bcc9dda,-f0ac19738f19} MATCH, none declined) | fb408bc75597, 34,904 frames, artifacts/gods/verify-animation-step2-fb408bc75597, PASS (30,043 hits, up from 11,542 -- every occurrence on this history now admits: 39 fallbacks, all exact adapter refusals, 0 unsupported domain); segment_verify at boundary-6000/12000 PASS, 300 hits each, 0 fallbacks; tree of eight recordings 107,519 frames, artifacts/gods/verify-camera-sprites-tree-2026-09-17a, PASS (camera-sprites, tree bit-exact); mutant animation-step-mutant-result DIVERGENCE at frame 2,260 (artifacts/gods/verify-animation-step2-mutant -- the first frame the tree ever entered the 'moving' arm) | fb408bc7: 30,043 hits, 39 fallbacks (2 machine admission, 5 observation deadline, 6 vblank in span, 26 z80 bank guard); tree: 00FE08's own fallbacks 321 -> 118, all now exact adapter refusals (0 unsupported domain at this gate); 0091BC still fires 8 times on the tree through 010332's own declined 'trigger-deep' arm (unrecovered) | this commit
2026-09-17 | 00FEC0/00FF54 (the animation step's per-object-type waypoint dispatch) | census only, no recovery | - | - | census-00FEC0[-<node>] and census-00FF54[-<node>] over all eight recordings: `no entry fired on this history` on every one -- the 'moving-coldstart' arm (a walk completing with at most one waypoint left) is real ROM code no recording enters; stays declined per grinder-protocol | - | this commit
2026-09-17 | 0091BC projectile launch / the 20-entry pool scan (0-3 skips witnessed) then the cold start into the walker's own projectile copy (game/walker.py: start/run), all four quadrants and both y-sign cases witnessed across four recordings; the pool exhausted (0091DE) declined, unwitnessed | no save/restore frame at all (D0-D7/A3 live scratch, the hazard tick's own shape); the projectile cold start's own quadrant-dependent setup cost reused with WALKER_RESUME_ENTRY's own per-step and re-arm-tail cost fragments (_WR_STEP/_WR_TAIL) -- the loop bodies are cost-identical between the resume and the cold start, confirmed instruction-for-instruction | gods_sega.game.projectiles.launch (calls game.walker.start/run/stores); plan gods_sega.boundary.launch_plan; candidates 'projectile-launch', 'camera-sprites' (twenty-five gates) | tests/games/gods/test_projectiles.py (44: 2 unit + 40 fixtures MATCH across census-0091BC{,-4492103be245,-7251bbd0ecf7,-f40d7bcc9dda} on four recordings, one segment-tier PASS) | fb408bc75597, 34,904 frames, artifacts/gods/verify-projectile-launch-fb408bc75597, PASS, 50 hits (all 50 witnessed occurrences), 0 fallbacks; mutant projectile-launch-mutant-result DIVERGENCE at frame 29,278 (artifacts/gods/verify-projectile-launch-mutant), the tree's first entry; tree of eight recordings, artifacts/gods/verify-camera-sprites-tree-2026-09-17b, PASS (camera-sprites, twenty-five gates, tree bit-exact) | 50 hits, 0 fallbacks | this commit; the caller(s) are now identified from the retained fixtures' own return addresses and a fresh disassembly: on `f40d7bcc9dda…` (0103C0/010D38) it *is* `010332`'s own `'trigger-deep'` tail, one instruction past the current `COUNTDOWN_CHECK_LAST_PC` (`0103AA`-`0103C8`: the record's own position +0x10 in x, a budget from the rate byte `(rate>>1)+2`, a full 15-register frame, `d6=1` fixed, `jsr 0091BC`, full restore) -- not yet composed into `countdown_check_plan` (a real next bite, not a blocker: every register is restored afterward, so the composition is simpler than `animation-step`'s); on `fb408bc75597…`/`4492103be245…`/`7251bbd0ecf7…` (`009D16`) it is a second, still undisassembled call site -- `009D12`-ish is not `010332`'s own code and remains open
2026-09-17 | 0093D2 line walker resume, projectile copy / a byte-for-byte duplicate of the already-recovered 00FFF0's own body at a second ROM address, over the projectile pool's own records; all four bodies, the yield on the budget (the projectile copy never completes via a counter -- only the object copy's dbra does), with and without the minor-axis wrap; a record whose continuation is not a body, and a zero budget, declined | the same cost fragments WALKER_RESUME_ENTRY's own plan uses (_WR_HEAD/_WR_STEP/_WR_TAIL), confirmed instruction-for-instruction identical between the two ROM copies; only the re-arm table and the RTS addresses differ | gods_sega.game.walker (unchanged, already generic over both copies); plan gods_sega.boundary.walker_resume_projectile_plan (a close twin of walker_resume_plan); candidates 'projectile-resume', 'camera-sprites' (twenty-six gates) | tests/games/gods/test_projectile_resume.py (147 fixtures MATCH across census-0093D2-{fb408bc75597,4492103be245,7251bbd0ecf7,f40d7bcc9dda}, four recordings; one segment-tier PASS) | fb408bc75597, 34,904 frames, artifacts/gods/verify-projectile-resume-fb408bc75597, PASS, 628 hits of 630 calls (2 exact adapter refusals: 1 observation deadline, 1 vblank in span); mutant projectile-resume-mutant-result DIVERGENCE at frame 12,971 (artifacts/gods/verify-projectile-resume-mutant), the frame after the tree's first entry (12,970); tree of eight recordings, artifacts/gods/verify-camera-sprites-tree-2026-09-17c, PASS (camera-sprites, twenty-six gates, tree bit-exact) | 628 hits, 2 fallbacks | this commit; the driver 009210 (calling this resume once per pooled projectile per tick) is censused fresh over the four projectile-firing recordings (artifacts/gods/evidence/census-009210-<node>, 32 real path classes on three of them) but still read for context only, per grinder-protocol ("do not recover the whole driver unless a parent composition requires it")
2026-09-17 | 00FFF0 line walker resume (object copy) / review-gate VBlank-slide check (`scripts/gods/vblank_slide.py`, --ticks 20) on a retained gameplay-tick fixture (census-00FFF0/00FFF0-entry-p0.state) | verdict: DIFFERS at every one of three placements tried (burns of 57,244/57,284/57,324 cycles; two further burns refused by the engine as reaching the VBlank itself) -- but the only differences are the global VBlank counter (130 vs 131 at the compared tick boundaries) and the 60 Hz elapsed/countdown/pad tuple's own elapsed field (96 vs 97, the same counter surfaced a second way); every live work-RAM byte, every register and the sound block are equal at the region's own exit, and both future tick observations agree once the counter offset is accounted for. This is the tool's own documented artifact of a large burn (order of a frame) shifting *which* tick boundary the interrupt lands inside, not a byte the handler and the region share: no live game byte is at stake, so the finding does not reopen `00FFF0`'s own admission (proven through the full ladder, tree bit-exact) -- it is written up, not treated as a blocker, per grinder-protocol | - | - | - | - | this commit; a smaller-burn or different-fixture re-run to isolate whether any *non-counter* byte ever differs is the next question if the walker's own admission is ever challenged again
2026-09-17 | 010332 countdown check / the 'trigger' arm's own deep gate composed at last: the record's own position (biased +0x10 in X only, unlike BACK/FORWARD's -8/+0x20) and a rate-derived budget ((rate>>1)+2, an arithmetic shift) go straight into the already-recovered projectile launch (game/projectiles.py) inside two nested save/restore frames (the shared outer d0-d1 push every trigger arm makes, then a full d0-d7/a0-a5 frame of its own) that leave every register but D3 (the reload) and D4 (the budget) at its entry value; 'trigger-deep-launch' replaces the old declined 'trigger-deep', witnessed at last -- a fresh full-history census of 010332 on f40d7bcc9dda (the original census's window had missed it) retained 5 fixtures (8 real occurrences); 'trigger-deep-pool-full' (the projectile pool exhausted) stays declined, unwitnessed | a leaf composing a call into an already-recovered routine (0091BC's own cost fragments -- and, through it, WALKER_RESUME_ENTRY's own -- reused directly, the shape 00FE08 owns its own call into 00FFF0 proved one level deeper) | gods_sega.game.timers.countdown_check extended; plan gods_sega.boundary.countdown_check_plan extended (_countdown_check_deep_plan); candidates 'countdown-check', 'camera-sprites' (unchanged gate count, twenty-six gates -- no new gate, an existing one's own domain widened) | tests/games/gods/test_timers.py (76: extended unit coverage for 'trigger-deep-launch'/'trigger-deep-pool-full' plus all 64 retained fixtures across census-010332{,-7251bbd0ecf7,-f0ac19738f19,-f40d7bcc9dda,-fresh-f40d7bcc9dda} MATCH/DECLINED as appropriate) | f40d7bcc9ddae7a9377d43712a008c0355f3289274545f0662a308273d6f70d1, 17,620 frames, artifacts/gods/verify-countdown-check3-f40d7bcc9dda, PASS, 3,277 hits, 33 fallbacks (all z80 bank guard, 0 unsupported domain); segment_verify at boundary-6000/12000 PASS (228 hits/4 fallbacks, 116 hits/1 fallback, all z80 bank guard); mutant countdown-check-mutant-result DIVERGENCE at frame 2,696 (artifacts/gods/verify-countdown-check3-mutant); tree of eight recordings, artifacts/gods/verify-camera-sprites-tree-2026-09-17d, PASS (camera-sprites, twenty-six gates, tree bit-exact, the `countdown check trigger arm calls unrecovered 0091BC` fallback reason gone from the tree) | 3,277 hits, 33 fallbacks | this commit; the second 0091BC caller (009D16 on fb408bc75597/4492103be245/7251bbd0ecf7, not 010332's own code) is still unidentified -- the one remaining open question in docs/gods/blockers/2026-09-16-0091BC.md
2026-09-17 | 00BA8E pickup check / every found-arm the eight-recording tree actually witnesses is now admitted: 'found-bare' (box residue positive, award zero -- a bare restore, RESULT_WORD still stored); the special-1 pickup's own extra timer decrement (SPECIAL_TIMER -= the box's own D2 residue) folded into the SAME found-sound/bare/effect tail every other collect() result reaches, declining only when the decrement goes negative ('found-special-timer', jsr 011540, unrecovered); found-effect's own two jitter draws corrected on both axes -- the "default mask" branch (the halved box size's own AND collapsing to zero) reloads a fixed 0xF instead of declining, and the second (Y) draw's own negative branch mirrors the first (X) draw's already-modelled subtract-instead-of-add, neither a decline; the effect pool's own 'full' arm (00932C, already proven through its own direct gate) composed into this activation too, X threaded from the second draw's own add/sub since the pool's own addq never runs when full; and a grid code of -4 or below (013264's own cascade to BIG_VALUE) composed straight into 013316's already-recovered debris burst (game.pickups.grid_inverse_award) the way the direct 013264 gate already owns it, one activation of the same jsr -- the one prior semantic bug this pass also fixed in passing: box residue of exactly zero had been folded into 'found-bare' by a same-day misreading of the branch fall-through, when the ROM's own `bmi`-then-`bne` sequence actually falls to the SAME sound-cue code as a negative residue (`<=0` was already correct; only the arm's own true boundary needed re-deriving to admit 'found-bare' at all). The only three sub-arms that stay declined are genuinely unwitnessed by all eight recordings even at `--max-classes 400` (up from the default 32, which was silently discarding real classes past the cap): the special-1 timer's own negative branch, the digit-split message tail, and 013316's own sound-on/pool-exhausted arms | a composition, not a new shape: every admitted piece reuses an already-recovered routine (013264, 013316, 014A3C, 00932C) or a bounded default already proven elsewhere in this same routine (the mask default mirrors the mask itself, the Y draw mirrors the X draw) | gods_sega.game.pickups.pickup_check extended (SPECIAL_TIMER, the -4-and-below inline call into grid_inverse_award, both jitter axes' default mask and negative branch, py_base/jitter_y exposed for the boundary's own X derivation); plan gods_sega.boundary.pickup_check_plan extended (_grid_inverse_debris_cost, _pickup_award_cost's own 'unrecovered' cost, the debris frame's own transient stack writes at 013264's own entry sp); candidates 'pickup-check', 'pickup-probe', 'camera-sprites' (unchanged gate count) | tests/games/gods/test_pickup_check.py (all 665 retained fixtures across census-00BA8E-fresh{,-4492103be245,-7251bbd0ecf7,-ca2b703b6fd5,-cfd02803e937,-da660c605b4a,-f0ac19738f19,-f40d7bcc9dda,-fb408bc75597-wide} MATCH, none declined -- the last of the eight recordings censused fresh this session, plus a `--max-classes 400` re-census of fb408bc75597 that surfaced the -4 and pool-full fixtures the default cap had discarded); test_pickups.py unchanged (013264's own gate already proved 013316) | f0ac19738f190b5a1efa7ac5cd2677f6e0ce582df16483525fad1f95f9bc7068, 15,148 frames, artifacts/gods/verify-pickup-check2-f0ac1973, PASS, 4,320 hits, 31 fallbacks (all z80 bank guard, 0 unsupported domain, up from 4,260 hits/91 fallbacks); segment_verify at boundary-6000/12000 PASS (231 hits/1 fallback, 132 hits/3 fallbacks, all z80 bank guard); mutant pickup-check-mutant-result DIVERGENCE at frame 469 (artifacts/gods/verify-pickup-check2-mutant); pickup-probe (010CD2, which composes this same plan one level deeper) re-verified too: PASS, 3,283 hits, 23 fallbacks (up from 3,248/58), artifacts/gods/verify-pickup-probe2-f0ac1973; tree of eight recordings, artifacts/gods/verify-camera-sprites-tree-2026-09-17e, PASS (camera-sprites, twenty-three gates, tree bit-exact, 1,046,640 hits, 5,845 fallbacks, down from 13,151 -- the pickup-check declines are now only 145 found-message + 1 found-special-timer) | 4,320 hits, 31 fallbacks | this commit
2026-09-17 | 00F828/00F86A proximity table search-and-add / the found-entry continuation at 00F8A2 recovered at last (game.hazard.proximity_trigger): a caller-record decrement of the SAME timer word the search tested for negativity, gated by which of conditions.py's own TRACKED ids TRIGGER_SELECTOR (FFFFF1FC) currently selects -- all three cmpi/bne checks execute regardless of which one matches (three independent redundant tests, not a mutually-exclusive dispatch), so the cost is the same whichever of 0/1/2 is selected; a decrement past TRIGGER_FLOOR (-200) clears the timer to 0 instead of wrapping negative; a selector outside 0/1/2 (leaves the ROM's own d5 uninitialised) stays declined, unwitnessed. Re-censused 00F828 over all eight recordings (four never censused before) with --max-classes 400 (the default 32-class cap was silently discarding real classes, as found the same session for 00BA8E) and reached 25 real 'trigger' fixtures | proximity_plan refactored: its own body extracted into _proximity_resolve(read, sp, a1, a2) (everything 00F828 does relative to its own entry sp, reused by hazard_tick_plan's own composition below); the double return (docs/gods/STATUS.md) nets the SAME +4 stack effect as any nested call once traced through both the internal bsr and the addq -- no special-casing needed at either caller | gods_sega.game.hazard.proximity_trigger; plan gods_sega.boundary._proximity_resolve extended into proximity_plan | tests/games/gods/test_proximity.py (39 retained fixtures across census-00F828{,-7251bbd0ecf7} MATCH, none declined; two new unit tests for proximity_trigger's own bonus/floor-clamp/selector-decline) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-proximity2-fb408bc75597, PASS, 33 hits, 0 fallbacks (up from 8 hits/25 fallbacks); mutant proximity-mutant-result DIVERGENCE at frame 12,890 (artifacts/gods/verify-proximity2-mutant) | 33 hits, 0 fallbacks | this commit
2026-09-17 | 014084 hazard tick / the 'trigger' gate composed over the now-fully-recovered proximity table: 0140BC-0140CA's own type-match-and-counter-check gates a jsr into 00F828 (game.hazard.proximity_search/proximity_add/proximity_trigger) that lands back at 0140D2 either way -- the SAME shared pool fill 'spawn' already owns runs next regardless of which of 00F828's own outcomes ('added' or 'trigger') the call reaches, so the composition folds into the existing 'spawn' arm (a new 'proximity' sub-result) rather than a separate arm; only a type match with the counter still under 2 (real ROM code, unwitnessed by any of the eight recordings) and 00F828's own remaining declines (a selector outside 0/1/2, the table itself full) still decline as 'trigger' | a leaf composing a call into an already-recovered routine (00F828's own _proximity_resolve, reused directly via the same shared helper proximity_plan now calls) whose own exit registers (A0, D5's own upper half, D6) survive into this activation's own exit since the shared pool-fill tail never touches them again except where it already resets D4/D5 fresh | gods_sega.game.hazard._spawn extended (now takes a1, a proximity sub-result key); plan gods_sega.boundary.hazard_tick_plan extended (the 00F828 call's own cost via _proximity_resolve, its own transient jsr-return-address write, A0/D6/D5-upper survival into the shared exit) | tests/games/gods/test_hazard.py (all 147 retained fixtures across census-014084-fresh{,-7251bbd0ecf7,-f0ac19738f19,-f40d7bcc9dda} MATCH, none declined; the old declined-trigger unit test replaced by a gate-unwitnessed case and a composed-add case) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-hazard-tick2-fb408bc75597, PASS, 26,118 hits, 175 fallbacks (all z80 bank guard, 0 unsupported domain, up from 26,075 hits/218 fallbacks incl. 33 unsupported domain); segment_verify at boundary-6000/12000 PASS (57 hits/0 fallbacks, 195 hits/0 fallbacks); mutant hazard-tick-mutant-result DIVERGENCE at frame 2,628 (artifacts/gods/verify-hazard-tick2-mutant); tree of eight recordings, artifacts/gods/verify-camera-sprites-tree-2026-09-17f, PASS (camera-sprites, twenty-six gates, tree bit-exact, 1,046,716 hits, 5,672 fallbacks, down from 5,845 -- the `hazard tick trigger arm calls unrecovered 00F828` and `proximity trigger arm (00F8A2) is not recovered` fallback reasons both gone from the tree; only the trigger evaluator's firing/disabled arms and pickup check's found-message/found-special-timer remain) | 26,118 hits, 175 fallbacks | this commit
2026-09-17 | 007986 message display gate + 0079DC string copy / part 1 of the trigger firing subsystem blocker (docs/gods/blockers/2026-09-16-00462C-firing.md), recovered as two shared candidates (both have real callers outside the trigger evaluator's own firing arm, 0046AA): 0079DC is the plain byte copy the Split predicted, bounded by the string's own length (the NUL is read but not written -- the caller's own clr.b (a1)+ supplies it), matching all 39 retained fixtures across 19 distinct lengths (the "19 kinds" the 15 September census misread). 007986 is a real priority gate over a shared display buffer, not a pass-through: an occupied buffer with a sufficient stored priority still succeeds, through MESSAGE_BUFFER_ALT -- a second real arm the first two-recording census never saw at all; re-censused over all eight recordings with --max-classes 100 (the default 32-class cap already shown this session, on 00BA8E and 00F828, to silently discard real classes) turned up the one occurrence that exercises it. An occupied buffer with insufficient priority ('blocked') and one with a stored bound of exactly 0 (skips the compare outright, 'unrecovered') stay declined, unwitnessed by any of the eight recordings | two ordinary leaves, neither a new shape: 0079DC is the "unrolled test count" shape bounded by traced data rather than a fixed table (0018C8's cache scan, 00F828's own search); 007986 is a caller-supplied-priority gate with two success paths and one real failure path | gods_sega.game.messages.message_gate/copy_message (new module: not trigger-specific, 007986 has unrelated callers at 0x5BC6/0x86B6); plan gods_sega.boundary.message_gate_plan/string_copy_plan; candidates 'message-gate', 'string-copy', 'camera-sprites' (extended to twenty-eight gates) | tests/games/gods/test_messages.py (new: 6 unit tests plus 20 retained 007986 fixtures across census-007986-{,4492103be245,7251bbd0ecf7,ca2b703b6fd5,cfd02803e937,da660c605b4a,f0ac19738f19,f40d7bcc9dda} and census-triggerfire MATCH/DECLINED, and 39 retained 0079DC fixtures across census-triggerfire MATCH, none declined) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames: message-gate PASS 31 hits/0 fallbacks (artifacts/gods/verify-message-gate-fb408bc75597), string-copy PASS 69 hits/9 fallbacks all z80 bank guard (artifacts/gods/verify-string-copy-fb408bc75597); segment_verify at boundary-6000/12000 PASS for both (0/0, 1/0 and 0/0, 2/0 hits/fallbacks); both mutants DIVERGENCE at frame 2,328 (artifacts/gods/verify-message-gate-mutant, verify-string-copy-mutant); tree of eight recordings, artifacts/gods/verify-camera-sprites-tree-2026-09-17g, PASS (camera-sprites, twenty-eight gates, tree bit-exact, 1,046,975 hits, 5,681 fallbacks) | 31+69 hits, 0+9 fallbacks | this commit; a real defect caught before landing: string-copy's own new cost constants accidentally reused the "_SC_" prefix already bound to score_convert_plan's own module-level constants (_SC_TAIL in particular), silently overwriting them and breaking every 00364C test until renamed to "_STRCPY_" -- a reminder that this file's per-region cost constants share one flat namespace
2026-09-17 | 00BA8E pickup check / the found-message arm recovered at last, and it is not a message at all: fresh disassembly of 00BBEA-00BC1C (AWARD_SCALE_LEVEL, formerly misnamed MESSAGE_FLAG) shows straight-line RAM/register arithmetic -- no bsr/jsr anywhere in it -- that derives an ALTERNATE (box_result, d3) pair (an award right-shift by `3-level`, or a halving correction when the shift removes no bits) and rejoins the SAME found-sound/found-bare/found-effect tail every other collect() result already reaches, at its own entry points (00BC12/00BC18 instead of 00BC1E). A gate scan at the tst.w instruction itself (not the 32/400-class census, whose path-signature budget the box/scan arithmetic's own diversity always exhausted first) found the level is 1 on every one of 104 real occurrences across all eight recordings, all on 7251bbd0ecf7 -- 'found-sound' (8), 'found-bare' (4) and 'found-effect' (84), including the halving-correction sub-branch (4); any other level stays declined (`WITNESSED_SCALE_LEVELS`, unwitnessed). A real defect caught before landing: the scaled path's own two transient stack pushes (00BBF0/00BBFE) share their two stack slots with 013264's OWN call frame (its `move.l d2,-(a7)` before the jsr), so the boundary's write-ordering had to place the scaled pushes AFTER that frame's own writes in `order`, not before, for "last write in execution order wins" to land the right final byte | a composition, not a new shape: the scaled path is a second front end for the existing tail, so `game.pickups._scaled_box_result` returns the same `box_result`/`d3` (plus an `x_op` tuple naming the real last flag-setting instruction, `add.w d3,d2` here vs the unscaled path's `sub.w d3,d2`) that `pickup_check`'s own downstream found-sound/found-bare/found-effect code already consumes | gods_sega.game.pickups extended (`AWARD_SCALE_LEVEL`, `_scaled_box_result`, `_lsr_word`/`_asr_word`); plan gods_sega.boundary.pickup_check_plan extended (`WITNESSED_SCALE_LEVELS`, `_pk_scale_cost`, the transient-write fix); candidates 'pickup-check', 'pickup-probe', 'camera-sprites' (unchanged gate count) | tests/games/gods/test_pickup_check.py (5 new unit tests plus all fixtures across every census-00BA8E-fresh* directory, now including a fresh `census-00BA8E-fresh-7251bbd0ecf7-wide` at `--max-classes 600`, 407 real path classes vs the old 32: 1107 of 1108 total 00BA8E/010CD2 fixtures MATCH, 1 correctly DECLINED (found-special-timer, still unwitnessed)) | f0ac19738f190b5a1efa7ac5cd2677f6e0ce582df16483525fad1f95f9bc7068, 15,148 frames, artifacts/gods/verify-pickup-check3-f0ac1973, PASS, 4,320 hits, 31 fallbacks (all z80 bank guard, unchanged); 7251bbd0ecf717086df9f8029c56dd0914620e59ab459610eea0e849d625fc42, 25,264 frames, artifacts/gods/verify-pickup-check3-7251bbd0ecf7, PASS, 13,606 hits, 30 fallbacks (13 observation deadline, 4 vblank in span, 12 z80 bank guard, 1 found-special-timer -- the found-message fallback reason is gone); segment_verify at boundary-6000/12000 PASS (231 hits/1 fallback, 132 hits/3 fallbacks, all z80 bank guard, unchanged); mutant pickup-check-mutant-result DIVERGENCE at frame 2,034 on 7251bbd0ecf7 (artifacts/gods/verify-pickup-check3-mutant) | 13,606 hits, 30 fallbacks | this commit
2026-09-17 | 0047DA achievement slot reset / trigger firing subsystem blocker part 2, first bite: the collectible/achievement-slot tracker's own reset call, owned through its caller as a platform-tail seam (Aladdin's "one native call inside the branch" shape, recipe 6b, reproduced with Gods' own device convention -- 0018C8's own seam runner and Seam type, unchanged). 0047DA marks one of four tracked-id slots (FFFFF22E array) empty and always calls the collected-item icon upload (001648, disassembled fresh: a VDP control command from the table at 0016C2 indexed by D0, then either a tile-descriptor stream through the remap table 0B5D04/0B5E04 or -- the only arm reachable from THIS caller, since 0047DA sets D2=-1 unconditionally before every call -- a fixed 8-longword fill, FFFFFFFF or DDDDDDDD depending on D1) as a seam: the prefix is 0047DA's own RAM-only head (its d1/d2/a0 frame, the slot store, deriving D1 from whether D0 matches a caller-supplied "highlighted" id at FFFFF236) ending at the bsr itself; the ceded block is all of 001648; the resume is 0047FA; the suffix is 0047DA's own frame restore + rts, RAM-only. D0 (0-3) selects which of 001648's own four VRAM icon slots the table at 0016C2 addresses; 0, 1 and 3 are witnessed across the eight recordings, 2 is real ROM code but no recording ever calls 0047DA with it: declined (`WITNESSED_ICON_SLOTS`). Two defects caught before landing: MOVEQ sign-extends to the FULL 32-bit register (D1<-0/1, D2<--1), not just the low word like a `.w` move elsewhere in this file, and MOVEQ *does* set N/Z/V/C (only X is unaffected) -- both wrongly modelled at first, caught by `factcheck check`'s own register/CCR comparison; and the mutant's own generic "flip the last write" needed the return-address write moved earlier in the composition's own `order` so the semantic slot-store byte, not part of the bsr's return address, is the one that ends up last (a flipped return address is an odd-address instruction fetch -- a 68000 address error, not a clean divergence) | a platform tail via bsr, not 0018C8's own inline-upload shape (the device work lives entirely inside a separately-called subroutine): the whole of 001648 is the ceded block regardless of which of its own two device sub-arms the ROM takes, so the composition need not know which one ran | gods_sega.game.achievements (new module: achievement_slot_reset); plan gods_sega.boundary.achievement_slot_reset_plan (+ achievement_slot_reset_suffix); candidates 'achievement-slot-reset', 'camera-sprites' (extended to twenty-nine gates) | tests/games/gods/test_achievements.py (new: 4 unit tests plus all 9 retained fixtures across census-0047DA-fresh-{fb408bc75597,f0ac19738f19,7251bbd0ecf7,f40d7bcc9dda,4492103be245,ca2b703b6fd5,cfd02803e937,da660c605b4a} MATCH, none declined at the fixture level -- the one unwitnessed icon slot (2) is confirmed separately by a synthetic D0 poke) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-achievement-slot-reset-fb408bc75597, PASS, 12 hits (6 seam entries/completions), 1 fallback (unsupported domain: icon slot 2, exactly the one real occurrence of it on this history); segment_verify at boundary-6000/12000 PASS, 0/0 hits/fallbacks both (the region is too rare for a 300-frame window); mutant achievement-slot-reset-mutant-result DIVERGENCE at frame 20,282 (artifacts/gods/verify-achievement-slot-reset-mutant2) | 12 hits, 1 fallback | this commit
2026-09-17 | 004790 achievement slot dispatch / trigger firing subsystem blocker part 2, second bite: a caller-supplied record pointer's own tracked id, gated by a shared 10-byte-stride record table's own status word at FFFFF8C2 (only status 2 is witnessed across all 12 retained fixtures; a real "blocked" arm for any other status stays declined) then checked in turn against the already-recovered 0047DA's own four ids (FFFFF22E/F230/F232/F234) -- all four comparisons always run, so at most one can differ (the ids are distinct) and 0047DA is called at most once, D0 the matched slot. A miss is a plain RAM-only leaf (4 redundant compares, no calls, witnessed on 6 of 12 fixtures). A match is a platform tail ONE LEVEL UP from achievement_slot_reset_plan: genesis_re.seam.run_seam narrows the gate array to just its own resume_pc while a seam's ceded block runs, so 0047DA's own separate gate never fires inside this seam even though it is armed in the SAME composite candidate -- the whole of 0047DA (itself a seam over 001648) is one opaque, unmodelled ceded block, and this composition needs nothing about 0047DA's own internals beyond what its own candidate already proved. The resume is 004790's own continuation after the ONE bsr that ran (0047BA/0047C4/0047CE/0047D8, one per matched slot, dispatched by the suffix on the live PC alone); the suffix then runs whichever later comparisons the ROM still makes (always misses) and 004790's own final rts. A second defect of the same MOVEQ-flags class as 0047DA's own: the true last flag-setter before the bsr is `moveq #n,d0`, not the `cmp.w` before it (nothing between them touches CCR) -- caught the same way, by factcheck check's own register/CCR comparison | a seam composed over another seam, not a new mechanism: 0047DA's own admission already proved 001648's ceded block; this bite only had to prove that a SECOND gate's own seam can cede straight through a THIRD gate's own entry without the dispatcher ever seeing it fire, which run_seam's own gate-narrowing already guarantees | gods_sega.game.achievements extended (achievement_slot_dispatch, RECORD_TABLE/RECORD_STRIDE/WITNESSED_STATUS/TRACKED_IDS, the exact ADDA.W sign-extending record-address arithmetic); plan gods_sega.boundary.achievement_slot_dispatch_plan (+ achievement_slot_dispatch_suffix); candidates 'achievement-slot-dispatch', 'camera-sprites' (extended to thirty gates) | tests/games/gods/test_achievements.py extended (7 new unit tests plus all 12 retained fixtures across census-004790-fresh-{fb408bc75597,f0ac19738f19,7251bbd0ecf7,f40d7bcc9dda,4492103be245,ca2b703b6fd5,cfd02803e937,da660c605b4a} MATCH, none declined -- the "blocked" status arm stays unwitnessed by every one) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-achievement-slot-dispatch-fb408bc75597, PASS, 8 hits (2 seam entries/completions), 0 fallbacks; segment_verify at boundary-6000/12000 PASS, 0/0 hits both (too rare for a 300-frame window); mutant achievement-slot-dispatch-mutant-result (a register: D0, real either way -- 0047DA marks slot D0 empty and 001648 indexes its own VDP table by it, and the no-match arm does not touch D0 at all so wrongly claiming it changed is itself observable) DIVERGENCE at frame 24,370 (artifacts/gods/verify-achievement-slot-dispatch-mutant) | 8 hits, 0 fallbacks | this commit
2026-09-17 | 00475E slot scan / the trigger firing subsystem blocker's own part 3 preamble, `00475E`: gates on bit 7 of the caller's own record's $10 byte; when set, checks three independent flag words in program order ((a1), $4(a1), $8(a1), each against 1) and, for whichever is 1, calls the already-recovered achievement_slot_dispatch (004790) with a pointer two bytes past the flag -- the 0049DA-calls-001164 shape, up to three times in one activation. Censused fresh over all eight recordings (`--max-classes 400`): every occurrence has at most one flag true (gate-false, all-three-false, or exactly one call, never two or three); the third flag (0x08(a1)) is real ROM code no recording ever sets, and only the first position's call is ever witnessed to match a tracked id (the second, when true, is always a plain miss). A match composes one level up from achievement_slot_dispatch_plan exactly as that planner composes one level up from achievement_slot_reset_plan (the whole of 0047DA/001648 stays one opaque ceded block; only the resume PC, already one of 004790's own `_AD_RESUME_ADDRESSES`, is gated) -- `genesis_re.seam` is not extended; a second independent match in the same activation (never witnessed) would decline inside the suffix rather than chain a second seam | RAM-only leaf composing up to one call into the already-recovered 004790, a seam two levels up from `achievement_slot_reset_plan` when it matches | gods_sega.game.achievements.slot_scan_gate/slot_scan_flag; plan gods_sega.boundary.slot_scan_plan; candidates 'slot-scan', 'camera-sprites' (extended to thirty-one gates) | tests/games/gods/test_achievements.py extended (6 new unit/fixture tests plus all 30 retained fixtures across census-00475E-{fb408bc75597,f0ac19738f19,7251bbd0ecf7,f40d7bcc9dda,4492103be245,ca2b703b6fd5,cfd02803e937,da660c605b4a} MATCH, none declined at the fixture level) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-slot-scan-fb408bc75597, PASS, 68 hits (2 seam entries/completions), 0 fallbacks; segment_verify at boundary-6000/12000 PASS (0/0, 1/0 hits/fallbacks); mutant slot-scan-mutant-result (a register, D0: real on the seam arm and untouched on every no-call arm, achievement-slot-dispatch's own reasoning) DIVERGENCE at frame 24,370 (artifacts/gods/verify-slot-scan-mutant) | 68 hits, 0 fallbacks | this commit
2026-09-17 | 004800 record id scan / the trigger firing subsystem blocker's own part 2, last caller (`docs/gods/blockers/2026-09-16-00462C-firing.md`'s Split): gates on d5 = ($10(a1)) & 0x7fff being one of {2,3,4,7,8} (real ROM code for {5,6} or anything above 8 skips the whole routine); when it is, checks the SAME three (flag, id) field pairs `00475E`'s own slot scan does, but the id word must fall in one of two ranges (0x12-0x17 or 0x7f-0x81, the achievement item ids the collectible tracker actually uses) rather than equal 1. A witnessed pair calls a freshly-disassembled `0048B4`: the SAME four-tracked-id compare achievement_slot_dispatch's own tail runs, with no status gate at all, reaching 0047DA through a tail JUMP (`bra.w`, not `bsr`) -- so 0047DA's own rts returns directly to whichever of 004800's own three call sites made the jump, needing no 004790-style resume-and-continue layer of its own; only 0048B4's own id-compare body (charged explicitly, since the composition jumps straight from 004800's entry to 0047DA's) had to be modelled fresh. Every witnessed call (first or second position) is a match; the third position, a miss from either witnessed one, and a second independent match in the same activation are all real ROM code no recording enters -- declined, not guessed. `achievement_slot_dispatch`'s own id-compare tail was factored out into a shared `match_tracked_id` reused here | RAM-only leaf composing up to one call into 0048B4, a seam over `achievement_slot_reset_plan` reached without any intermediate resume layer at all (0048B4's tail jump leaves 0047DA's own rts returning straight to 004800's own call site) | gods_sega.game.achievements extended (`record_id_scan_gate`, `id_in_range`, `record_id_scan_check`, `match_tracked_id`); plan gods_sega.boundary.record_id_scan_plan; candidates 'record-id-scan', 'camera-sprites' (extended to thirty-two gates) | tests/games/gods/test_achievements.py extended (5 new unit tests plus all 58 retained fixtures across the same eight census-004800-* directories: 54 MATCH, 4 correctly DECLINED (icon slot 2, unwitnessed)) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-record-id-scan-fb408bc75597, PASS, 68 hits (3 seam entries/completions), 1 fallback (unsupported domain: icon slot 2, the one real occurrence); segment_verify at boundary-6000/12000 PASS (0/0, 1/0 hits/fallbacks); mutant record-id-scan-mutant-result (NOT a register this time -- a witnessed match includes icon slot 3, and D0+1 there reaches table index 4, outside 001648's own four VRAM slots, which faults the platform rather than diverging; "flip the last write" instead, which always lands on achievement_slot_reset's own real semantic store, the last write `achievement_slot_reset_plan` itself inserts) DIVERGENCE at frame 20,282 (artifacts/gods/verify-record-id-scan-mutant) | 68 hits, 1 fallback | this commit
2026-09-17 | 0048E4 action reset-elapsed + 004ACA action clear-group / part 3 of the trigger firing subsystem blocker's Split, its first two admissible action-table entries: the evaluator's own firing tail dispatches its action through the ROM table at 0046D0 via a tail JUMP (`jmp (a5)`, 0046CE, disassembled fresh -- `move.w $10(a1),d0` doubled twice into a 15-entry table), so a handler's own `rts` returns straight past the whole evaluator activation to whoever called it: no seam, no frame, the same opaque-tail shape `0048B4`'s own jump into `0047DA` already proved. `0048E4` is `clr.l ELAPSED; rts` unconditionally (the SAME elapsed-seconds counter `conditions.py`'s own kinds 9/10 already read), fully witnessed, no declines, one real occurrence per recording. `004ACA` compares a caller-record's own word at `+0x12` against `pickups.py`'s own three group active-id words (`GROUP_TABLES`) in turn and clears whichever matches -- each of the three has its OWN `rts` (004ADA/004AE8/004AF6, not one shared exit), and only the second group is ever witnessed to match (a miss against all three, and a match against the first or third, are real ROM code no recording enters). The other action-table entries (`004A0A`, `004D04`, `004E1C`, `004E74`, `0048EA`, `005024`, `00772E`; `004A74`/`005074` unwitnessed) call still-unrecovered helper routines (`004AAA`, `004926` -- itself touching the already-recovered spawn queue's own `FFF39A` table, a real lead for later -- `004ECE`/`004D7E`, `0050A4`, `0077A8`, `004F16`) or write the level grid across several blocks: an ordinary decline (a kind that calls unrecovered code), not a new mechanism, so no blocker package | two RAM-only leaves with no frame of their own, reached by a tail jump with no seam needed | gods_sega.game.actions (new module: reset_elapsed, clear_matched_group); plan gods_sega.boundary.action_reset_elapsed_plan, action_clear_group_plan; candidates 'action-reset-elapsed', 'action-clear-group', 'camera-sprites' (extended to thirty-four gates) | tests/games/gods/test_actions.py (new: 6 unit tests plus all 8 retained 0048E4 fixtures and all 5 retained 004ACA fixtures MATCH, across census-{0048E4,004ACA}-{fb408bc75597,f0ac19738f19,7251bbd0ecf7,f40d7bcc9dda,4492103be245,ca2b703b6fd5,cfd02803e937,da660c605b4a} where each fired) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames: action-reset-elapsed PASS 1 hit/0 fallbacks (artifacts/gods/verify-action-reset-elapsed-fb408bc75597), action-clear-group PASS 1 hit/0 fallbacks (artifacts/gods/verify-action-clear-group-fb408bc75597); segment_verify at boundary-6000/12000 PASS for both (0/0 hits both, too rare for a 300-frame window); both mutants (the last write; both arms always store) DIVERGENCE, action-reset-elapsed at frame 15,102 (artifacts/gods/verify-action-reset-elapsed-mutant), action-clear-group at frame 17,112 (artifacts/gods/verify-action-clear-group-mutant) | 1+1 hits, 0 fallbacks | this commit
2026-09-17 | 0075D6 player state machine shared tail / the supervisor's Decision on `docs/gods/blockers/2026-09-17-005700.md`: a platform-tail seam, not a new subsystem.  Gate `0075D6` (reached by a plain `bra.w` from every witnessed state handler and the dispatcher's own 'inactive' arm, so there is no frame of its own on the stack).  Prefix, in ROM order: the tile trigger scan's clean arm (`00773A`, already semantics; the 'trigger' arm -- an event through 007850 -- declines by name); the follow-point step (new, `game.camera.follow_point_step`: FOLLOW_X/FOLLOW_Y, `camera_follow`'s own words, eased toward the player's position -- X by a fixed step of four inside a 0x50/0xD0 dead band, Y by half the distance beyond its own 0x70/0x20 band with a minimum step of one -- clamped to 0xEC0/0x340; only when FFFFEF4E is zero, true on all 55,326 activations of every one of the eight recordings, checked by a full-history scan since `recovery_census.py`'s own path-signature classifier overflows on 0075D6 exactly as it does on 005700 itself -- 521 fixtures retained anyway before the overflow was found, kept as `census-0075D6`; the sibling arm at 00755A declines by name); the state-table re-index (new, `game.player.state_table_reindex`: STATE_TABLE's own entry plus the live D7, an unbounded ROM table read exactly like `game.sprites`' own descriptor tables, fully witnessed on every branch -- both mask tables, both flip outcomes).  Ceded operation: `0076AA`'s `jmp 001312` through the upload's own `rts` (0013CE) -- 0048B4's own "no intermediate resume layer" shape one level further removed: that `rts` pops the return address ALREADY on the stack when the activation began (witnessed as both 001FD6, the main loop's own per-tick call, and 012BA0, a nested return through the tile scan's own declined cascade), so there is no suffix beyond admitting the `rts` itself (gated at 0013CE, not after it: the native adapter refuses a zero-cost atomic).  Fixtures: since the entry-level census overflowed (521 fixtures, `overflow 4523` at `--max-classes 400`), retained instead by re-parking `00773A`'s own 399-real-class census at the parent gate `--parent 0x0075D6` (406 fb408bc75597 + 168 f40d7bcc9dda fixtures, `census-0075D6`/`census-0075D6-f40d7bcc9dda`), plus 80 fixtures a full-history scan retained directly by (x-branch, y-branch, mask-table, flip) combination (`census-0075D6-tail`, `/tmp/full_scan_validate.py`-style scan kept as a documented method here, not committed) to reach the rarer bands the proxy census under-samples (x-increase, y-increase/increase-clamped).  Two arms declined as genuinely unwitnessed on the full tree: the x follow-point clamp at 0xEC0, and the y follow-point clamp at 0; a third, narrower edge also declines: the y-increase band's own minimum-step override (00763C, dy=0x71 exactly, the only value that rounds the halved step to zero) is unwitnessed even though the y-increase band itself is common -- confirmed by a direct fixture (`tail-fb408bc75597-f6705-...`).  D0's own upper half is zero from the tile scan's own `moveq #$10,d0` onward (not the caller's own entry upper half, unlike every other register this planner touches); X survives MOVE untouched from the re-index's own unconditional `add.w d2,d2` (007694), the SAME instruction on every branch, so X is simply the re-index's own index bit 15 regardless of which follow-point arm ran.  Mutant: a follow-point byte off (FOLLOW_X or FOLLOW_Y, whichever this occurrence's own step wrote last); a 'hold'/'hold' occurrence (about half of them) writes neither and passes through unmutated rather than falling back to STATE_COUNTER, which an earlier version of this control corrupted and which faulted the machine (a real M68000 address error inside 001312's own tile-copy loop on a garbage descriptor pointer, confirmed once) rather than diverging cleanly | `game.camera.follow_point_step` (+`FOLLOW_X_STEP`, `FOLLOW_X_LIMIT`, `FOLLOW_Y_LIMIT` etc.), `game.player.state_table_reindex`, `game.player.SHARED_TAIL_ALT_GATE`; plan `gods_sega.boundary.player_tail_plan`; candidates 'player-tail', 'camera-sprites' (extended to thirty-five gates); mutant `player-tail-mutant-result` (`recovery._mutate_follow_point`) | tests/games/gods/test_player_tail.py (new: 8 unit/fixture tests; every one of the 654 retained fixtures across `census-0075D6`, `census-0075D6-f40d7bcc9dda` and `census-0075D6-tail` either MATCHes as a seam (prefix to 001312, suffix from 0013CE) or DECLINEs for a named reason -- 84 MATCH, 570 correctly DECLINED (the tile scan's own trigger arm, or the y=0x71 edge), 0 unexplained) | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-player-tail-fb408bc75597, **PASS, 10,042 hits (5,025 seam entries, 5,017 completions), 6,059 fallbacks (5,939 tile-scan trigger declines, 87 z80 bank guard, 31 y-minimum-step declines, 2 seam deadline)**; segment_verify at boundary-6000/12000 PASS (76/107, 188/31 hits/fallbacks, the one y=0x71 decline appearing naturally in the second window); mutant `player-tail-mutant-result` DIVERGENCE at frame 539 on f0ac19738f19 (artifacts/gods/verify-player-tail-mutant -- fb408bc75597's own full 34,904-frame run hits a REAL, unrelated M68000 address error far downstream of an early clean divergence, a tooling limit of running a mutant to completion rather than stopping at the first difference, not evidence against the control: the short segment_verify mutant already diverges cleanly at frame 6,013); `camera-sprites` (all thirty-five gates) on the tree of all eight recordings: **PASS, 107,519 frames, 1,075,426 hits, 24,106 fallbacks (18,249 tile-scan trigger declines, 4,403 z80 bank guard, 451 seam deadline, 345 observation deadline, 304 vblank in span, 133 y follow-point declines (61 clamp, 72 minimum-step), 189 evaluator firing/disabled, 27 machine admission, 5 achievement/pickup declines), tree bit-exact** | 10,042 hits, 6,059 fallbacks | this commit
2026-09-17 | 008222 / 012DA0 / 012E5A movement states' shared hit-resolution web | escalation: NEW_GODS_SUBSYSTEM | - | - | - | - | blocker docs/gods/blockers/2026-09-17-008222.md; every movement-cluster state handler disassembled after 0075D6 (state 1 `007282`, state 0 `006FFE`, and state 24 `006AD8` surveyed as a simpler alternative once state 5's own `00746A` turned out to be a shared fallthrough of state 1's own body, not an independent handler) reaches `008222` (a 40-entry hit-list search, `FFFFF374`/`F378`/`F37C` its own persistent slots) or its consumers `012DA0`/`012E5A` (a two-level per-object-type dispatch through ROM tables at `012D04`/`012C3E`) on a real, frequently-witnessed fraction of activations -- 800 of 3,181 state-1 activations on `fb408bc75597` reach `008222` (25%), and 7 of 14 retained state-24 path classes reach `012DA0`'s own per-type `jmp`.  `00722C`, a separate bounded 200-entry box scan states 0/1 also call, is confirmed NOT a blocker (its returned CCR is provably unused at either call site) but doesn't unblock either state on its own.  Next question: whether `008222` is recoverable as a leaf on its own first, before `012DA0`/`012E5A`'s own per-type consumers are | escalate commit
2026-09-18 | 0075D6 player state machine shared tail / the tile trigger scan's own 'trigger' arm characterised in full: 0077A8 disassembled to its own `rts` (0077A8-007878) -- a cell over TILE_THRESHOLD reads its own byte as a record kind and looks up `EVENT_STATUS_WORDS` (== `game.conditions.STATUS_WORDS`) at `kind - 3`; zero or negative **declines**, and the scan (called by `bsr`, or `bra` at the scan's own absolute last position) tries the next cell exactly as a clean scan would -- composed as a general per-cell cost/write model (`boundary._tile_scan_cost`, `_tc_position`, replacing the two fixed clean-arm constants) reproducing 0077A8's own decline body (head + bmi/beq-taken tail) and its abandoned stack residue (sp-12/sp-16, shared between bsr positions 0-4 and, for sp-12 only, the bra position 5); only a STRICTLY POSITIVE status word raises a real event through `EVENT_TABLE`/`EVENT_HANDLERS` (1-11; kind 3 is the already-recovered trigger evaluator `00462C`) and still declines by kind, named, real code this composition does not reach into.  A pre-existing, previously-invisible defect found and fixed the same session, exposed only once 'trigger-but-declined' scans started being admitted: the y follow-point band's own decrease-side d3 setter is `moveq #$20,d3` (clears d3's own upper half), not the increase band's plain `.w` moves (`boundary.player_tail_plan` had carried the caller's own entry upper half through unconditionally) -- found on the *tree* run (node ca2b703b6fd5, frame 9661, a silent `state_sha256` divergence of two RAM bytes matching or a stale copy of d3's own wrongly-carried upper half; video/audio bit-identical), not by any of the 654 fixtures (all had a zero d3 upper half at entry) | `game.player.event_status` (+`EVENT_STATUS_WORDS`, `EVENT_STATUS_INDEX_BIAS`, `EVENT_TABLE`, `EVENT_HANDLERS`); `game.player.tile_trigger_scan` extended (`checks`, `fires`, `arm`/`trigger_index` unchanged for backward compatibility); plan `gods_sega.boundary.player_tail_plan` extended (`_tile_scan_cost`/`_tc_position`/`_ES_*`/`_TC_*` replacing `_PT_TILE_SCAN_NARROW`/`_WIDE`, the sp-12/sp-16 residual-write fix, the d3-upper-half fix); candidates 'player-tail', 'camera-sprites' (unchanged gate count, thirty-five) | tests/games/gods/test_player.py extended (5 new unit tests: `event_status` declines/fires, the scan continuing past a decline, a fire reported in `fires`); tests/games/gods/test_player_tail.py extended (1 new regression test for the d3 fix; the fixture-parametrized test's decline condition narrowed from `scan['arm']=='trigger'` to `scan['fires']`) -- all 654 retained fixtures across `census-0075D6`, `census-0075D6-f40d7bcc9dda` and `census-0075D6-tail` re-verified: 161 now MATCH (86 clean + 75 triggered-but-declined, up from 84), 493 correctly DECLINE by kind (`scan['fires']` non-empty) or the other named reasons | fb408bc75597ee196ec2d03eb807ea2f209ab29fd7785459a567d9c3ec5e5887, 34,904 frames, artifacts/gods/verify-player-tail3-fb408bc75597, **PASS, 16,386 hits (8,201 seam entries, 8,185 completions), 2,891 fallbacks (2,704 tile-scan event-found by kind: 3→2,003, 6→650, 4→27, 2→14, 1→3, 5→3, 8→2, 7→1, 9→1; 133 z80 bank guard, 42 y-minimum-step declines, 10 seam deadline, 2 observation deadline)**, up from 10,042 hits/6,059 fallbacks; segment_verify at boundary-6000/12000 PASS (220/35, 220/15 hits/fallbacks, unchanged in kind); mutant `player-tail-mutant-result` DIVERGENCE at frame 2,261 (artifacts/gods/verify-player-tail3-mutant); `camera-sprites` (thirty-five gates) on the tree of all eight recordings: **PASS, 107,519 frames, 1,095,639 hits (130,432 seam entries, 129,355 completions), 14,007 fallbacks, tree bit-exact** -- up from 1,075,426 hits/24,106 fallbacks (a first tree attempt caught the d3 defect above: DIVERGENCE at frame 9,661 on node ca2b703b6fd5, artifacts/gods/verify-camera-sprites-tree-2026-09-18c, fixed and re-verified before this PASS) | 16,386 hits, 2,891 fallbacks (tree: 1,095,639 hits, 14,007 fallbacks) | this commit
