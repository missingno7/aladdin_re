# Blocker: `003480` -- not a small screen-visibility gate, a second real dispatcher over `pickups`/message tables

## Where

- `003480`-`0034B8`/`0034C2`, bsr'd from at least three sites inside the
  `003284` tail (returns observed at `003324`, `0033A6`, and other slots;
  `docs/gods/blockers/2026-09-19-003186.md`'s own reconnaissance named this
  as "a screen-visibility gate returning through `rtr`", reused at every
  `0018C8`-emitting arm).  Censused fresh this session
  (`census_all.py --entry 0x003480 --max-classes 400`, all five recordings,
  no overflow: 19/25/31/40/60 classes, `fb408bc75597` the largest at 60).

## Code

`NEW_GODS_SUBSYSTEM`.  The bounds-check head (`003480`-`0034B0`: `tst.w
$ef3c` bypass, then an x-range and a y-range test against `$f18c`/`$f18e`,
the camera position) *is* exactly the small gate the reconnaissance named,
and both its short exits are real: `0034B6: clr.w -(a7); rtr` (pass/bypass)
and, reached only from deeper inside the body, `0034BE: move.w #$8,-(a7);
rtr` (fail) -- both `00B8C2`'s own convention.  But **every retained
fixture's own trace** (`factcheck facts --path` run on all 40 fixtures of
`f0ac19738f19`, and a sample of the other four recordings) shows the
function does NOT return at the bounds check: the in-range arm
(`0034B0 ble.b $34c4`, taken in 35 of 40 fixtures on this recording) falls
through into a second, much larger body reaching `0034B8`/`0034C2` only
after real additional work -- confirmed by tracing, not guessed from the
ROM:

1. `0034C4`-`0034EA`: reads `+4(a0)` (an object status), declines (`bge.w
   $3620`) to a THIRD body when `>= 0xC0`; otherwise indexes a RAM table
   at `FFFFF8C2` (6 bytes/entry, by the status) and dispatches on its own
   `+4` word: `>1` -> the fail exit directly; `==1` -> a further
   range/table dispatch at `00354C` over a 4-entry RAM table at
   `FFFFF22E`; anything else falls through to (2).
2. `0034F0`-`003548`: a sound-request write (`FFFFFDF6 <- 0x34`), then a
   ROM-constant scan (`0x14B42`, up to 192 bytes, counting zeros in the
   first `status+1` of them -- data-independent given the status, i.e. a
   pure function of one RAM value over a ROM table), doubled twice into an
   index into a 16-entry ROM function-pointer table at `0x5958` (a SECOND
   real indirect-`jsr` table, the same shape as `003186`'s own `0x370E`
   finding; one entry, `0x5984`, IS the already-recovered `0075D6` platform
   tail), `jsr (a0)`, then `bsr.w $2f2e` (see below) before rejoining the
   shared exit.
3. `003620`-`003648` (the `>= 0xC0` arm, real and witnessed -- 3 of 40
   fixtures on `f0ac19738f19`, exit `003BE0`): `jsr $12c80.l` directly (the
   SAME pickup-award entry `003186`'s own blocker already named
   unrecovered), confirmed by `factcheck facts`' own call trace: `003632 ->
   012C80`, which itself calls `012CCA -> 012CD6` twice before returning --
   real internal calls into ROM this project has not read yet, distinct
   even from the `012D30`/`012A3E`/... chain `003186`'s blocker listed
   (same neighbourhood, different entry addresses -- `012C80`'s own
   internals are wider than one straight-line chain).  Then `bsr.w $2f2e`
   and rejoin.
4. `002F2E` (`bsr`'d from both (2) and (3)): a real, SELF-CONTAINED 10-slot
   queue append at `FFFFF09E` (10 bytes/entry, free slot marked by a
   negative first word) -- bounded, RAM-only, plain `rts`, no unrecovered
   callee.  This one piece IS a clean leaf on its own (the `pool shape`
   the Decision's own text names for `FFF262`), but it sits inside, not
   ahead of, this blocker's own body.  Its own DRAIN half (`0x2F68`,
   pulling the queue's own head and calling `0x011468`) is not reached
   from `003480` at all -- a separate, still-unrecovered caller.
5. `00354C`'s own "no F22E match" arm (`003590`-`0035B2`+): a second sound
   request cluster, a `+2(a1)`/`$f296` accumulation, then `bsr.w $364c` --
   a real, self-contained number-to-digits routine (seven `divs.w #$a`
   steps, each feeding one of three digit-encoder callees, `0x36C6`/
   `0x36CA`/`0x36CE`/`0x36D2`) whose own further destination (a text/score
   display?) was not traced to its end this session.

Instruction counts across the 40 `f0ac19738f19` fixtures span **4 to 522**
(the bypass exit alone is 4; the deep dispatch/message/pickup arms run
84-522), confirming real, materially different sub-mechanisms rather than
one small leaf with incidental length variance.

## Observed

- All 40 fixtures on `f0ac19738f19` (and spot checks on the other four
  recordings) exit through exactly one of the two rtr's named above
  (`0034B8` in 39/40, `0034C2` -- the fail exit -- in 1/40): the function
  IS a single, properly self-returning routine, not an inlined dispatcher
  fragment -- the earlier working assumption that it "falls through
  without returning" was wrong; it returns, but only after real
  additional work on most paths.
- `003632 -> 012C80` (real `jsr`), `012CCA -> 012CD6` x2 (real internal
  calls inside `012C80`'s own body), `003644 -> 002F2E`, `002F50 -> `(rts):
  the full call trace of fixture `p9` (`census-0X003480-f0ac19738f19`),
  reproduced by `factcheck facts --path`.
- `0x5958`: 16 real ROM long-word entries read directly from the
  cartridge (`5ABE, 5B5E, 5B5C, 5AE8, 13442, 5A84, 5A8E, 5A6A, 5A74, 5A7C,
  5A48, 75D6, 59B4, 5A5C, 142A6, 14292`) -- one of them (`75D6`) is the
  already-recovered platform-tail seam; the rest are unread.
- `0x14B42`: a 192-byte ROM table, confirmed present (not yet interpreted
  byte-by-byte).
- `FFFFF8C2` (6 bytes/entry, RAM), `FFFFF22E` (a 4-entry RAM table),
  `FFFFF09E` (10x10-byte RAM queue, self-contained) -- three further
  persistent structures beyond the four `003186`'s own blocker already
  named, none of them `BOX_SCAN_TABLE`, `TRIGGER_TABLE`, `RECORD_TABLE` or
  `pickups`' own tables.

## Why the current mechanism does not fit

The reconnaissance in `docs/gods/blockers/2026-09-19-003186.md` (ROM read
only, no trace) undersold this region: it is not "a screen-visibility gate
returning through `rtr`" so much as a compound per-object post-processing
dispatcher that happens to open with one.  It shares a real unrecovered
callee (`jsr $12c80`) with the pickup-award integration the Decision's own
order placed LATER (bite 5, "`012C80` and its chain"), a second real
indirect-`jsr` function-pointer table (`0x5958`, alongside `003186`'s own
`0x370E`), and at least one more self-contained mechanism (a
number-to-digits routine, `0x364C`) whose own destination is unread.
Census bounded (no overflow) is necessary but not sufficient for a small
leaf: the 4-522 instruction spread and the five distinct real
sub-mechanisms above are `NEW_GODS_SUBSYSTEM` scale on the same standard
`003186`'s own escalation used, not a family over one witnessed selector.

## Fixtures used

`census_all.py --game gods --entry 0x003480 --max-classes 400`, all five
recordings, no overflow (`fb408bc75597` 60 classes, `7251bbd0ecf7` 31,
`f40d7bcc9dda` 19, `f0ac19738f19` 40, `ca2b703b6fd5` 25) --
`artifacts/gods/evidence/census-0X003480-*`.  `factcheck facts --path` run
on all 40 fixtures of `f0ac19738f19` (instruction counts 4-522, three
distinct exit addresses: `003324`, `0033A6`, `003BE0`) and spot-traced on
`p2`/`p3`/`p9`/`p12` for the calls list and RAM-write detail quoted above.

## What was tried

- Full disassembly of `003480`-`0034B8`/`C2` (the bounds-check head),
  `0034C4`-`003648` (the status/table/pickup dispatch), `002F2E`-`002FA0`
  (the queue append/drain pair), `003620`-`003680` and `00364C`-`0036C0`
  (the pickup arm and the digit routine), and the `0x5958` table's own 16
  entries read directly from the cartridge.
- `factcheck facts --path` on every retained fixture of `f0ac19738f19`
  (40) plus a handful from the other four recordings, to replace the ROM
  reconnaissance with real traced evidence before deciding.

## Next question

Whether `002F2E` (the queue append, self-contained) should be recovered
now on its own as the Decision's own "pool shape" bite regardless of
`003480`'s own fate, since nothing here blocks it; and whether the
`0x5958` table's own individual handlers (most unread) are, like
`003186`'s own `0x370E` table, mostly small leaves once censused one at a
time -- the one already-identified entry (`0075D6`) suggests some of them
compose cleanly. `jsr $12c80`'s own real internal shape (`012CCA`/`012CD6`
looping, distinct from the `012D30`/`012A3E`/`012A34`/`012A9E`/`011468`
chain `003186`'s blocker named) needs its own fresh census before the
Decision's own bite 5 opens it.

## Progress, 20 September

`012C80` (the pickup-award group dispatch reached from this blocker's own `>= 0xC0` arm) and `002F2E`
(the queue append both arm 2 and arm 3 reach) are recovered independently as their own leaves --
`docs/gods/ledger.md`'s own two 20 September entries.

The `0x5958` table itself is corrected: it is a real **23 entries**, not the 16 this blocker's own
reconnaissance read -- entries 16-22 are real, witnessed handlers too, confirmed by tracing every
`jsr (a0)` this dispatch performs across the whole tree (`00352C`'s own `movea.l (a0,d3.w),a0`) and by
reading the ROM past entry 15 directly (real code, not further table data, begins right after entry
22's own long word).  Fifteen of the twenty-three are witnessed by the five recordings (indices 0, 1,
2, 3, 4, 6, 10, 14, 15, 16, 17, 18, 19, 21, 22); seven are recovered this session as their own leaves
(`docs/gods/ledger.md`'s own third 20 September entry): the "phase accumulator" family (indices 0, 3,
21 -- EF3C/EF3E, folding an excess into `pickups.SPECIAL_TIMER`), a fixed sound-cue pair (index 10,
which also has a second, independent real caller elsewhere: exit `008616` in this session's own
census), and a shared 24-byte ROM-to-RAM copy into `FFFFF53A` (indices 14/15/16, each with its own
source block).  What remains of the table: indices 1, 2, 4, 6, 17, 18, 19, 22 (witnessed, not yet
recovered -- index 1/2 call `jsr $12CC2.l`, the SAME internal tally-cascade entry `pickup_award_group_
plan` already composes, so recovering them likely means exposing that cascade as a callable unit
rather than reimplementing it; index 6 calls an unrecovered `jsr $141BA`; index 18 calls the
already-recovered `message_gate_plan`/`string_copy_plan` in a real, larger composition; indices 4, 17,
19, 22 are not yet disassembled past their own head), the table's own generic index (any that exceeds
22, real ROM since the scan's own zero-byte count is unbounded above by the table's own size, never
witnessed), and the dispatch's own head/tail (`0x34F0`-`003548`: the sound-request write, the 192-byte
`SOUND_SCAN_TABLE` scan, the table lookup itself, and the post-call tail composing `002F2E`) -- none
of this blocker's own three arms (the bounds-check head's own further body, the `FFFFF8C2`/`FFFFF22E`
RAM-table dispatch of arm 1, or arm 2/3's own tails) has been composed into one region yet.  The seven
new leaves are standalone candidates only, kept out of `camera-sprites` for native gate capacity (the
`state-18`/`creature-attack` precedent) until `003480` itself is composed, which will retire their
individual gates the way `00A772` retired the creature leaves'.

Next, in order: the remaining witnessed `0x5958` handlers (index 1/2 first, since they reuse the
already-modelled tally cascade); arm 1's own `FFFFF8C2`/`FFFFF22E` dispatch (not yet disassembled at
all); then `003480` itself composing all three arms, admitting `0036E2`'s own range arm (kinds
`0x40`-`0x44`, 7,197 declines on the last tree).
