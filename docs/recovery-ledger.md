# Recovery ledger

Append-only, one line per qualified leaf or escalation, newest last.  This is
the grinder's only per-leaf record; `docs/STATUS.md` is updated once per
milestone commit, `docs/recovery-cost-log.md` once per meaningful milestone.

Line format (pipe-separated, one line):

    date | entry kind/arm | recipe | semantics function | tests added | cold run (frames, artifacts dir, status) | fallbacks before -> after | commit or blocker

`fallbacks` is the candidate worker's total fallback count from
`comparison.json` of the named cold run; a leaf that does not lower it on the
recorded history was recovered from constructed evidence only and says so.

## Baseline rows

2026-09-14 | baseline of main (44223150b7d6, 82,161 frames) | - | - | 1,631 tests / 154 s | 82,161 frames, artifacts/factory-baseline-main, PASS (parallel, 403 s, 0 restores) | 37,458 fallbacks (72,249 hits) | 4284daf plus the leaves below
2026-09-14 | 1AF590 kind 55 / direct and selected-guard arms (FFF0BE set) | RAM-only leaf | contact_type55_guard, contact_type55_return | tests/test_contact_type55.py (+12) | included in the baseline run above | recorded history hits the FFF0BE-clear arm only; constructed evidence | landed with this batch
2026-09-14 | 1AF590 kind 55 / owned inside the contact scan (both callback maps) | scan ownership | - | test_type55_is_owned_inside_the_complete_contact_scan | included in the baseline run above | scan-slot 1AF590 fallbacks 3 -> 0 on the 26,378-frame node; 0 on main | landed with this batch
2026-09-14 | 1AE64C kind 43 / active, sound-on arm | single sound seam (two native calls inside the seam) | contact_type43_update | tests/test_contact_family.py (+8) | included in the baseline run above | 34 recorded hits: 18 recovered, 16 decline on the inactive arm (type43 inactive early return) | landed with this batch
2026-09-14 | evidence tooling (no leaf) | census retains by executed path; index.json; segment_verify; original caches survive source edits; fallbacks by gate | - | tests/test_segment_verify.py, test_recorded_evidence.py, census/pathfacts/history/recovery/ledger tests | 82,161 frames, artifacts/evidence-tooling-main, PASS (parallel, 537 s alongside tests, 0 restores) | 37,458 -> 37,458 (no leaf) | landed after the replay-evidence study; evidence in artifacts/evidence/main (33 classes, about 190 paths, 385 fixtures)
2026-09-14 | decoupled observation boundary (no leaf) | GenesisRun.step: input at the nominal tick, observation and deadline at OBSERVATION_OFFSET_TICKS; cache contract 2 | - | tests/test_history.py (+1, overshoot test updated) | 82,161 frames, artifacts/decoupled-baseline-main, PASS (parallel, 0 restores) | 37,458 -> 21,840 (scheduler refusals 15,691 -> 103; contact ticks owned 53,342 -> 65,338; instructions replaced 12.64M -> 15.41M) | landed from docs/execution-model-research.md
2026-09-14 | 1AF5F0 kind 58 / direct, guard-fail and guard-pass arms (both sides of the 0xC distance guard) | RAM-only leaf (Type-55 distance-guard shape, limit 0xC, offset +2) | contact_type58_guard, contact_type58_fail, contact_type58_pass | tests/test_contact_type58.py (+52) | 82,161 frames, artifacts/type58-guard, PASS (parallel, 0 restores) | 21,840 -> 15,171 | 1AF5F0 owned inside the contact scan too (both callback maps); commit e24b25a
2026-09-14 | 1AFA84 kinds 74/75 / direct, guard-fail, guard-pass, window/kind/state gate and pool-slot spawn arms | RAM-only leaf (Type-55/58 distance-guard shape plus a window/kind/state gate and a child spawn reusing game.free_object/game.initialize) | contact_type74_guard, contact_type74_fail, contact_type74_pass, contact_type74_target, contact_type74_retype, contact_type74_position | tests/test_contact_type74.py (+57) | 82,161 frames, artifacts/type74-guard, PASS (parallel, 0 restores) | 15,171 -> 12,341 | 1AFA84 owned inside the contact scan too (both callback maps, kind 74 and kind 75); commit 36fa755
2026-09-14 | 1AFB36 kinds 6E-73 / inactive, negative-state, direct, bit4-inactive, guard-fail and guard-pass (immediate and FFF103-reinit) arms, every arm owned | RAM-only leaf (Type-55/58/74 distance-guard shape plus a new FFF0E7/FF7E5A gate cascade ahead of the familiar FFF0BE/FFF0C0/bit4 gates, a second distance axis, and an FFF103-keyed reinitialisation tail) | contact_type6e_guard, contact_type6e_fail, contact_type6e_pass, contact_type6e_reinit | tests/test_contact_type6e.py (+70) | 82,161 frames, artifacts/type6e-guard, PASS (parallel, 546 s, 0 restores) | milestone batch with 1AE9E0/1AEECA: 12,341 -> 5,473 | 1AFB36 owned inside the contact scan too (both callback maps, all six kinds 6E-73); all 36 recorded path classes and every unrecorded arm (--vary, 256/256 MATCH) recovered in this leaf; commit d70d7a3
2026-09-14 | 1AE9E0 kind 1A / inactive and active (self pair-release + 1B7940 re-template) arms, every arm owned | RAM-only leaf composed of two already-proven adapters (`_clear_objects`'s exact external-1ABE6E pair-release, `_initialize_object_effects`'s exact 1AE30A template expansion), neither a native re-entry | begin_contact_family_type1a reuses game.clear_pair/game.initialize via the existing adapters; no new game.* semantics | tests/test_contact_type1a.py (+16) | 82,161 frames, artifacts/type6e-guard, PASS (parallel, 546 s, 0 restores) | milestone batch with 1AFB36/1AEECA: 12,341 -> 5,473 | 1AE9E0 owned inside the contact scan too (both callback maps); all recorded path classes (inactive, and the linked+buffered active arm) plus every unrecorded buffer/link combination (--vary, 16/16 MATCH) recovered in this leaf; commit 9d3116b
2026-09-14 | 1AEECA kind 23 / inactive, primary-exhausted, secondary-exhausted and both-found double-spawn arms, every arm owned | RAM-only leaf: self-retype plus two sequential pool-slot spawns, both reusing the exact 1AE262/1AE30A adapters already proven for begin_contact_family_type74 (free_object/initialize/position-copy); a plan view exposes the primary spawn's own writes to the secondary pool's free_object scan exactly as the real second 1AE27A scan would see them | contact_type23_retype, contact_type23_override (reuses game.free_object, game.initialize, game.contact_type74_position) | tests/test_contact_type23.py (+20) | 82,161 frames, artifacts/type6e-guard, PASS (parallel, 546 s, 0 restores) | milestone batch with 1AFB36/1AE9E0: 12,341 -> 5,473 | 1AEECA owned inside the contact scan too (both callback maps); both recorded path classes plus every unrecorded pool-index/exhaustion combination (--vary, 12/12 MATCH across three separate --vary runs, plus 2 constructed exhaustion fixtures) recovered in this leaf; commit 2d65053
2026-09-14 | 1AEB7A kind 0D and 1AEBFE kinds 14/2B / unconditional-RTS stubs, every arm owned | RAM-only leaf: single-instruction body (RTS), no gate, no write, no flag change; shared _begin_contact_family_noop helper for both entries | - (no new game.* semantics; nothing to name) | tests/test_contact_type0d.py (+12) | 82,161 frames, artifacts/type0c-noop, PASS (parallel, 550 s, 0 restores) | milestone batch with 1AE9A8: 5,473 -> 5,136 | both entries owned inside the contact scan too (both callback maps, all three kinds 0D/14/2B); factcheck check MATCH on all three recorded fixtures; commit 12f07b5
2026-09-14 | 1AE9A8 kind 0C / inactive and active (own-buffer release + 1B7CC4 re-template) arms, every arm owned | RAM-only leaf composed of two already-proven adapters (_clear_objects with pair=False, the same RAM-domain adapter already proven for clear_auxiliary_buffer; _initialize_object_effects, already proven for initialize_object/finish_object/begin_contact_family_type1a), neither a native re-entry | begin_contact_family_type0c reuses game.release_buffer/game.initialize via the existing adapters; no new game.* semantics | tests/test_contact_type0c.py (+10) | 82,161 frames, artifacts/type0c-noop, PASS (parallel, 550 s, 0 restores) | milestone batch with 1AEB7A/1AEBFE: 5,473 -> 5,136 | 1AE9A8 owned inside the contact scan too (both callback maps); both recorded path classes (no buffer, buffer present) plus the buffer-length --vary sweep (6/6 MATCH) recovered in this leaf; commit 041aea5
2026-09-14 | 1AEBDC kinds 78/7A / FFF0D8 gate, +/-8 window guard (both fail arms) and nested BSR into the shared 1AE4F8 contact root (RAM-only and sound-seam), every arm owned | nested-call composition: the wrapper's own FFF0D8/window prefix plus an explicit BSR into CONTACT_ENTRY, composed exactly as `begin_contact_dispatch`/`begin_contact_dispatch_sound` compose type 7B's own BSR, around the already-proven `begin_contact`/`begin_contact_sound`; the extra local RTS that unwinds this wrapper's own frame is made explicit (+16 cycles/+1 instruction, matching `begin_contact_dispatch`'s own adjustment) | contact_type78_route | tests/test_contact_type78.py (+26, 2 skipped) | milestone pending (batch with the next two leaves); segment_verify PASS on all 6 child and 6 parent fixtures against artifacts/type0c-noop's reference | 1AEBDC's own recorded-history count 183; batch delta to be recorded at the next milestone cold run | 1AEBDC owned inside the contact scan too (both callback maps, both kinds 78/7A); factcheck check MATCH on all 6 recorded fixtures (4 RAM-only, 2 sound-seam via `--stop 1E58B8`) plus a 10-combination FFF0D8/window `--vary` sweep and an exact window-boundary `--vary` sweep; commit da0df05
2026-09-14 | 1AF81C kinds 62/63 / bounded-distance guard (limit 0xA), self-kind check and command-0x45 mismatch seam, every arm owned | Type-55/58-shaped guard (no delta adjustment, limit 0xA) whose pass continues into a CMPI.B #$63 self-kind check; a mismatch retypes the record then either returns locally or continues into its own private MOVEM/PEA/JSR command-0x45 seam, the same 24/28/28 ABI shape as `begin_contact_family_type46_sound_seam` | contact_type63_guard, contact_type63_fail, contact_type63_pass, contact_type63_retype | tests/test_contact_type63.py (+64) | milestone pending (batch with the previous leaf); segment_verify PASS on all 4 child and 4 parent fixtures against artifacts/type0c-noop's reference | 1AF81C's own recorded-history count 156; batch delta to be recorded at the next milestone cold run | 1AF81C owned inside the contact scan too (both callback maps, RAM-only arms, both kinds 62/63); factcheck check MATCH on all 4 recorded fixtures (3 RAM-only, 1 sound-seam) plus borrow/no-borrow/selector `--vary` sweeps; fixed a real SoundSeam.stack_basis bug (outer-caller SP used instead of the callback's own SP) found only by the end-to-end qualify() harness, not by factcheck's prefix-only check; commit b9d8b01
2026-09-14 | 1AF228 kind 3A / shared FFEFE2/FFEFE3 counter (1B0394), command-0x0D sound seam and the 1AF4C2 replace tail, gate-pass arms owned | reuses `game.collection_state('secondary', ...)`/`game.increment_counter` (already proven for the existing 'secondary' collection route at 0x1AF21E) for the counter advance, and `replace_object(increment_total=True)` (already proven for `begin_object_transition`) for the retire/re-template tail; composed with a new command-0x0D sound seam, the same 24/28/28 ABI shape as `begin_object_transition`'s own command-11 seam | begin_collection_type3a_dispatch, finish_collection_type3a (no new game.* semantics) | tests/test_collection_type3a.py (+12) | milestone due now (3 leaves: 1AEBDC/1AF81C/1AF228); full suite + cold comparison to follow before push | 1AF228's own recorded-history count 80; delta to be recorded once the cold run lands | kind 0x3B (the only other table slot reaching 1AF228, as a fall-through from the already-recovered 'secondary' route) is unaffected, not a fresh dispatch this leaf owns; the FFEFE2==0x3939 capped-counter arm (1AE6DE's own table-write formula) is not observed on the recorded history and declines explicitly; factcheck-style check_plan MATCH on all 4 recorded fixtures for both sound and poked no-sound prefixes; segment_verify PASS on all 4 child and 4 parent fixtures; fixed a real bug where the no-sound arm's final registers dropped the outer dispatch prefix's own a4/d1, found only via the full recovery.py dispatch composition; commit 6bcdfda
2026-09-14 | 1AEE40 kinds 2C/2D/2E/31/6D / FFF0D8 gate, own-buffer release (1AE372) and nested BSR into the shared 1AE4F8 contact root (RAM-only and sound-seam), FFF0D8-clear arm owned | composition of three already-proven pieces with no new game.* semantics: `_clear_objects(pair=False)` (already proven for Type-0C), `begin_contact`/`begin_contact_sound` (CONTACT_ENTRY, already proven), nested exactly as `begin_contact_dispatch`/Type-78's own BSR compose that same call, with the extra local RTS that unwinds this wrapper's own frame made explicit | - (no new game.* semantics) | tests/test_contact_type2c.py (+18) | 82,161 frames; segment_verify PASS on all 17 usable child and parent fixtures against artifacts/type0c-noop's reference; full cold comparison pending the next milestone | 1AEE40's own recorded-history count 65; delta to be recorded at the next milestone cold run | FFF0D8-set (a pool-scan-and-spawn arm behind a new subroutine 1AE2DA: a 5-slot pool scan at FF84B2, a 32-word copy loop, then a spawn) is not observed as this entry's primary recorded path in every fixture and declines explicitly, both from the RAM-only and sound entry points; 1AEE40 owned inside the contact scan too (both callback maps, all five kinds); factcheck check MATCH on all 17 usable recorded fixtures (2 RAM-only early-arm, 13 sound-seam via `--stop 1E58B8`, 2 correctly-declining pool-scan); commit 13d4adb
2026-09-14 | spawn dispatcher children 1B668A/1B688A/1B6864/1B674A/1B7000/1B673E / plain-wrapper spawn callbacks, every arm owned | six new `SPAWN_PLAIN_CALLER_FACTS`/`PLAIN_WRAPPERS` table rows; each target's ROM bytes exactly match the already-proven LEA/BSR/RTS plain-wrapper shape (`_plain_wrapper_shape` validates byte-for-byte) with its BSR displacement resolving to one of the four known `spawn_region` allocators (reverse/lower/upper), so `spawn_plain_caller` already handles all six with zero new code | - (no new game.* or boundary code; table facts only) | tests/test_spawn_oracle.py's existing parametrized suites, automatically extended via `oracle_witness.PLAIN_WRAPPERS` (+72 cases; full suite 693 passed) | 82,161 frames; segment_verify PASS on all six child and parent fixtures against artifacts/type78-window's reference; full cold comparison pending the next milestone | six targets' combined recorded-history count 382+44+224+157+173+152=1132; delta to be recorded at the next milestone cold run | from the spawn dispatcher children census (`--parent 1AE46C`, all 37 then-unrecovered targets) into artifacts/evidence/spawn; factcheck-style check_plan MATCH on all 38 recorded evidence fixtures across the six targets; the remaining 31 census targets have branch/guard suffixes, device-sound seams, or distinct closure-style bodies not matching any existing recipe and are deferred to a future leaf; commit 2c9a6db
2026-09-14 | spawn dispatcher child 1B6696 kind 33 / reverse-pool creation and script-pointer-only closure suffix, every arm owned | new `SPAWN_CLOSURE_CALLER_FACTS` table row: ROM bytes exactly match the closure-wrapper shape (LEA/BSR/BNE/MOVE.L-imm-to-offset/RTS) with its BSR resolving to the reverse-pool `spawn_region` allocator; no type retag, only the record+0x20 script pointer, so `spawn_closure_caller` already handles it with one new `game.finish_reverse_script_spawn` semantic function | finish_reverse_script_spawn | tests/test_spawn_oracle.py's existing parametrized suites, automatically extended via `oracle_witness.CLOSURE_WRAPPERS`/`CLOSURE_OUTPUTS` (+24 cases; suite 717 passed) | 82,161 frames; segment_verify PASS on all 10 child and 10 parent fixtures against artifacts/spawn-plain6's reference; full cold comparison pending the next milestone | 1B6696's own recorded-history count 460; delta to be recorded at the next milestone cold run | from the spawn dispatcher children census (`--parent 1AE46C`); 1B6696 owned inside the spawn dispatcher (parent fixtures confirm ownership); leaf_review PASS
2026-09-14 | spawn dispatcher child 1B6C0E kinds 00/33 / upper-pool creation and FFF175 tile-upload guard, RAM-only arms owned | new standalone planner (not table-driven): the same LEA/BSR upper-pool prefix as `spawn_upper_caller` (template 0x1B7A80), a plain RTS tail on allocation failure, and on success a TST.B FFF175/BNE guard whose set arm returns directly; the clear arm continues into 1B2650's VDP tile-data upload (MOVE.L to VDP control port $C00004, 16-word transfer through data port $C00000, inside the `1B263C..1B26D0` command-stream engine range) and declines explicitly -- no RAM-domain recipe reaches a device port | spawn_upper_tile_caller (no new game.* semantics) | tests/test_spawn_partial_arms.py (+11 for this entry: recovered skip arm x6 free/incoming_x combinations, VDP-arm decline x2, isolated-plan decline, mutants x3) | 82,161 frames; segment_verify PASS on all 24 child and 24 parent fixtures against artifacts/spawn-plain6's reference; full cold comparison pending the next milestone | 1B6C0E's own recorded-history count 520 (21 of 24 recorded path classes decline on the VDP arm; 3 recover, plus the unrecorded pool-exhausted arm caught by a `--vary` sweep); delta to be recorded at the next milestone cold run | from the spawn dispatcher children census; 1B6C0E owned inside the spawn dispatcher (parent fixtures confirm ownership on both the recovered and declining arms); factcheck check MATCH on the 3 recovered fixtures plus a constructed pool-exhausted `--vary` fixture and an FFF175 boundary `--vary` sweep, DECLINED on the 21 VDP-arm fixtures; leaf_review PASS
2026-09-14 | spawn dispatcher child 1B72FC kinds 00/02 / FFEFE0 cap guard, recorded arm owned | new standalone planner (not table-driven): CMPI.W #$3030,FFEFE0.L then BNE; the recorded not-equal arm returns directly with no writes; an exact match falls into an unrecorded four-slot allocation sequence (387 instructions, no retained fixture) and declines explicitly | spawn_cap_guard_two (no new game.* semantics) | tests/test_spawn_partial_arms.py (+10 for this entry: recovered arm x4 cap values, cap-reached decline, isolated-plan decline, mutants x3) | 82,161 frames; segment_verify PASS on both child and both parent fixtures against artifacts/spawn-plain6's reference; full cold comparison pending the next milestone | 1B72FC's own recorded-history count 295 (both recorded path classes are the not-equal arm); delta to be recorded at the next milestone cold run | from the spawn dispatcher children census; 1B72FC owned inside the spawn dispatcher (parent fixtures confirm ownership); factcheck check MATCH on both recorded fixtures plus a boundary `--vary` sweep (0x0000/0x2FFF/0x3031/0xFFFF all MATCH, 0x3030 DECLINED); leaf_review PASS
2026-09-14 | spawn dispatcher child 1B71C4 kind 02 / lower-pool creation and script/Y-offset closure tail, every arm owned | new standalone planner: the same allocator and template as spawn_closure_guard_caller (SPAWN_REGION_LOWER_ENTRY, template 0x1B78F0) and the same finish-write-plus-Y-offset tail shape (record+0x20 script pointer, game.offset_spawn_position/_sub_sr for the -8 Y adjustment), just without that entry's FFF12A guard prefix; allocation failure and success both return locally with no undeclined branch | spawn_lower_offset_caller, finish_lower_offset_spawn | tests/test_spawn_oracle.py's existing parametrized suites, automatically extended via oracle_witness.CALLER_POOLS/DISPATCH_CALLBACKS (no guard dependency, so joins the dispatcher-chain bulk suites directly; 728 passed) | 82,161 frames; segment_verify PASS on all 3 child and 3 parent fixtures against artifacts/spawn-tile-cap's reference; full cold comparison pending the next milestone | 1B71C4's own recorded-history count 125 (all 3 recorded path classes recover) plus the unrecorded pool-exhausted arm caught by a --vary sweep; delta to be recorded at the next milestone cold run | from the spawn dispatcher children census; 1B71C4 owned inside the spawn dispatcher (parent fixtures confirm ownership); factcheck check MATCH on all 3 recorded fixtures plus a constructed pool-exhausted --vary fixture; leaf_review PASS
2026-09-14 | spawn dispatcher child 1B6FAE kind 00 / upper-pool creation, FFF175 guard and FF7E26 word-write arm, RAM-only arms owned | new standalone planner: the same upper-pool prefix as spawn_upper_tile_caller (1B6C0E, template 0x1B81EC); allocation failure and a successful allocation with FFF175 set both return locally at a shared early RTS with no writes; a successful allocation with FFF175 clear continues into a CMPI.B FF7E26,#5 selector whose match (the arm every recorded fixture exercises) writes one fixed word at record+0x1E and returns; a mismatch reaches 1B2650's VDP tile-data upload (the same command-stream engine device port as 1B6C0E) and declines explicitly | spawn_upper_tile_word_caller, finish_upper_tile_word_spawn | tests/test_spawn_partial_arms.py (+13 for this entry: recorded FF7E26==5 arm x6 free/incoming_x combinations, FFF175-set arm x2, VDP-arm decline, isolated-plan decline, mutants x3) | 82,161 frames; segment_verify PASS on all 5 child and 5 parent fixtures against artifacts/spawn-tile-cap's reference; full cold comparison pending the next milestone | 1B6FAE's own recorded-history count 132 (all 5 recorded path classes are the FF7E26==5 arm); the FFF175-set and pool-exhausted arms are unobserved and caught by --vary sweeps; delta to be recorded at the next milestone cold run | from the spawn dispatcher children census; 1B6FAE owned inside the spawn dispatcher (parent fixtures confirm ownership on both the recovered and declining arms); factcheck check MATCH on all 5 recorded fixtures plus FFF175/pool-exhaustion --vary sweeps, DECLINED on FF7E26 mismatch; a first draft undercounted the shared alloc-check BNE's own 8 cycles/1 instruction in the FF7E26==5 suffix (caught by factcheck check MISMATCH before qualification, not guessed); leaf_review PASS
2026-09-14 | spawn dispatcher child 1B6756 kind 33 / FF7E26 selector and unconditional reverse-pool spawn, recorded arm owned | new standalone planner: an FF7E26 != 0x0B selector (every recorded fixture) allocates from the reverse pool (SPAWN_REGION_REVERSE_ENTRY, template 0x1B7DDC) then branches unconditionally to a shared RTS with no Z-flag check at all -- allocation success and pool exhaustion both return the same way with no further writes, so one arm owns both sub-cases; an FF7E26==0x0B selector is unobserved and nests a second guard, a second reverse-pool spawn and a second 1B2650 VDP call with no retained fixture and declines | spawn_reverse_guard_caller (no new game.* semantics) | tests/test_spawn_partial_arms.py (+16 for this entry: recorded arm x6 free/incoming_x combinations, 0x0B decline, isolated-plan decline, mutants x3, plus segment fixtures) | 82,161 frames; segment_verify PASS on all 7 child and 7 parent fixtures against artifacts/spawn-tile-cap's reference; full cold comparison pending the next milestone | 1B6756's own recorded-history count 157 (all 7 recorded path classes are the FF7E26 != 0x0B arm); the 0x0B arm and the reverse-pool-exhausted sub-case are unobserved and caught by --vary sweeps; delta to be recorded at the next milestone cold run | from the spawn dispatcher children census; 1B6756 owned inside the spawn dispatcher (parent fixtures confirm ownership); factcheck check MATCH on all 7 recorded fixtures plus a reverse-pool-exhaustion --vary sweep, DECLINED on FF7E26==0x0B; a first draft inverted the guard condition (declined the recorded arm, matched the unrecorded one) -- caught immediately by factcheck check DECLINED on every recorded fixture, fixed before qualification; leaf_review PASS
2026-09-14 | spawn dispatcher children 1B6F4A/1B6F34 kind 33 / reverse-pool script-only closures, every arm owned | two new `SPAWN_CLOSURE_CALLER_FACTS` table rows sharing template `0x1B8264`: ROM bytes exactly match the closure-wrapper shape (LEA/BSR/BNE/MOVE.L-imm-to-offset/RTS) with BSR resolving to the reverse-pool allocator; no type retag, only the record+0x20 script pointer, so `spawn_closure_caller` already handles both with two new `game.finish_reverse_script_a_spawn`/`finish_reverse_script_b_spawn` semantic functions | finish_reverse_script_a_spawn, finish_reverse_script_b_spawn | tests/test_spawn_oracle.py's existing parametrized suites, automatically extended via oracle_witness.CLOSURE_WRAPPERS/CLOSURE_OUTPUTS (+22 cases; suite 787 passed) | 82,161 frames; segment_verify PASS on all 5+6 child and parent fixtures against artifacts/spawn-guards3's reference; full cold comparison pending the next milestone | 1B6F4A's own recorded-history count 115 (5 recorded path classes), 1B6F34's 110 (6 recorded path classes); delta to be recorded at the next milestone cold run | from the spawn dispatcher children census; both owned inside the spawn dispatcher (parent fixtures confirm ownership); the pre-existing tests/test_spawn_oracle.py constants ROW_UNKNOWN/SETUP_UNKNOWN used 0x1B6F34 as an example of an unrecovered callback for an unrelated walker-decline test; both were repointed to 0x1B67C2 (still unrecovered) since 0x1B6F34 is now a real recovered callback; leaf_review PASS
2026-09-14 | spawn dispatcher child 1B65C0 kind 00 / reverse-pool creation and Y-only offset (no script write), every arm owned | new standalone planner: reverse-pool allocation (template 0x1B7D14) then, on success only, an unsigned ADDI.W #$A to the record's own Y coordinate via game.offset_spawn_position/_add_sr -- no script write, no X adjustment, unlike the closure/offset table shapes; allocation failure and success both recovered | spawn_reverse_y_offset_caller (no new game.* semantics; reuses offset_spawn_position) | tests/test_spawn_oracle.py's existing parametrized suites, automatically extended via oracle_witness.CALLER_POOLS (no guard dependency, joins the dispatcher-chain bulk suites directly; 787 passed) | 82,161 frames; segment_verify PASS on all 3 child and 3 parent fixtures against artifacts/spawn-guards3's reference; full cold comparison pending the next milestone | 1B65C0's own recorded-history count 72 (all 3 recorded path classes recover) plus the unrecorded reverse-pool-exhausted arm caught by a --vary sweep; delta to be recorded at the next milestone cold run | from the spawn dispatcher children census; 1B65C0 owned inside the spawn dispatcher (parent fixtures confirm ownership); factcheck check MATCH on all 3 recorded fixtures plus a constructed pool-exhausted --vary fixture; leaf_review PASS
2026-09-14 | spawn dispatcher child 1B6EEE kind 33 / upper-pool creation, cleared field, script pointer and object type, every arm owned | new `SPAWN_CLOSURE_CALLER_FACTS` table row: ROM bytes exactly match the closure-wrapper shape with an extended suffix (CLR.L record+0xA, MOVE.L script at +0x20, MOVE.B type=0x22 at offset 0, in that ROM order); `spawn_closure_caller` already handles it with one new `game.finish_upper_type22_spawn` semantic function | finish_upper_type22_spawn | tests/test_spawn_oracle.py's existing parametrized suites, automatically extended via oracle_witness.CLOSURE_WRAPPERS/CLOSURE_OUTPUTS (833 passed) | 82,161 frames; segment_verify PASS on child and parent fixtures against artifacts/spawn-closures4's reference; full cold comparison pending the next milestone | 1B6EEE's own recorded-history count 84 (1 recorded path class) plus the unrecorded upper-pool-exhausted arm caught by a --vary sweep; delta to be recorded at the next milestone cold run | from the spawn dispatcher children census; 1B6EEE owned inside the spawn dispatcher; leaf_review PASS
2026-09-14 | spawn dispatcher children 1B7158 kind 02 and 1B6FEE kind 02 / lower-pool guarded closure and primary-pool flag-set caller, every arm owned | 1B7158: new standalone planner, the exact same shape as spawn_closure_guard_caller (1B71A0) -- same allocator/template (SPAWN_REGION_LOWER_ENTRY, 0x1B78F0), same -8 Y-offset tail -- differing only in guard address (FFF128 vs FFF12A) and script value (0x1242E4 vs 0x124318); both the guard-clear (majority recorded) and guard-set arms recover, no decline. 1B6FEE: new standalone planner, primary-pool creation (SPAWN_REGION_ENTRY, template 0x1B7C74) then an unconditional ST.B at record+9 -- Scc does not touch CCR, so this does not fit the closure table's always-applied _logic_sr and needed its own function; both allocation failure and success recover | spawn_closure_guard_two_caller, finish_guarded_lower_two_spawn; spawn_primary_flag_caller | tests/test_spawn_oracle.py's existing parametrized suites, automatically extended via oracle_witness.CALLER_POOLS (neither has an unrecoverable arm, so both join the dispatcher-chain bulk suites directly with zero bespoke test code; 833 passed) | 82,161 frames; segment_verify PASS on all child and parent fixtures (3+2) against artifacts/spawn-closures4's reference; full cold comparison pending the next milestone | 1B7158's own recorded-history count 79 (all 3 recorded path classes -- both guard states -- recover); 1B6FEE's own recorded-history count 79 (both recorded path classes recover) plus the unrecorded primary-pool-exhausted arm caught by a --vary sweep; delta to be recorded at the next milestone cold run | from the spawn dispatcher children census; both owned inside the spawn dispatcher; a first draft of spawn_closure_guard_two_caller copied spawn_closure_guard_caller's own entry+26/entry+22 return-address offsets verbatim without re-deriving them for the new entry's own 8-byte-longer TST/BEQ prefix -- caught by re-deriving from the ROM byte offsets before any check was run, not guessed; leaf_review PASS
2026-09-14 | spawn dispatcher children 1B717C kind 02 and 1B6F60 kind 33 / lower-pool guarded closure clone and reverse-pool script-only closure, every arm owned | 1B717C: new standalone planner spawn_closure_guard_three_caller, byte-for-byte the same shape as spawn_closure_guard_caller (1B71A0) and spawn_closure_guard_two_caller (1B7158) -- same allocator/template (SPAWN_REGION_LOWER_ENTRY, 0x1B78F0), same -8 Y-offset tail -- differing only in guard address (FFF129) and script value (0x1242FE); both guard states recover, no decline. 1B6F60: new SPAWN_CLOSURE_CALLER_FACTS row sharing template 0x1B8264 with 1B6F4A/1B6F34 (reverse-pool allocation plus a record+0x20 script write, no type retag) | spawn_closure_guard_three_caller, finish_guarded_lower_three_spawn; finish_reverse_script_c_spawn | tests/test_spawn_oracle.py's existing parametrized suites, automatically extended via oracle_witness.CALLER_POOLS/CLOSURE_WRAPPERS/CLOSURE_OUTPUTS (neither has an unrecoverable arm; 868 passed) | 82,161 frames; segment_verify PASS on all child and parent fixtures (2+3) against artifacts/spawn-guardtwo5's reference; full cold comparison pending the next milestone | 1B717C's own recorded-history count 79 (both recorded path classes -- both guard states -- recover); 1B6F60's own recorded-history count 4 (3 recorded path classes); the frontier_ledger fallback counts against the full 82,161-frame history are substantially higher than the evidence census window suggests (1B712C alone shows 110 fallbacks against a census count of 8); delta to be recorded at the next milestone cold run | from the spawn dispatcher children census; both owned inside the spawn dispatcher; leaf_review PASS
2026-09-14 | spawn dispatcher child 1B7018 kind 02 / lower-pool creation, type, script, cleared field and mode, every arm owned | new `SPAWN_CLOSURE_CALLER_FACTS` table row: byte-for-byte the same shape as `finish_type_4c_spawn`'s own callback (1B70D4), differing only in the type byte (0x47 vs 0x4C); the allocator is SPAWN_REGION_LOWER_ENTRY, not UPPER -- a first draft assumed UPPER from the shared template value (0x1B79B8, which multiple allocator entries reuse as static init data) and was caught by a 10-cycle/1-instruction MISMATCH against the recorded fixture before qualification, fixed by reading the fixture's own `calls:` line (1B525E) | finish_type_47_spawn | tests/test_spawn_oracle.py's existing parametrized suites, automatically extended via oracle_witness.CLOSURE_WRAPPERS/CLOSURE_OUTPUTS (892 passed) | 82,161 frames; segment_verify PASS on all 4 child and 4 parent fixtures against artifacts/spawn-guardtwo5's reference; full cold comparison pending the next milestone | 1B7018's own recorded-history count 4 (all 4 recorded path classes recover); delta to be recorded at the next milestone cold run | from the spawn dispatcher children census; 1B7018 owned inside the spawn dispatcher; leaf_review PASS
2026-09-14 | spawn dispatcher child 1B6836 kind 02 / FFF100 guard, reverse-pool creation, type/script and XY offset, every arm owned | new standalone planner: TST.B FFF100/BNE gates the spawn (both guard states recover, no decline); on success, reverse-pool creation (template 0x1B7D78) then a type/script write (like the closure-table shapes) plus both a +15 X offset and a -1 Y offset via the already-proven game.offset_spawn_position, the same X-then-Y ROM order as spawn_offset_caller so the final CCR comes from the last (Y) arithmetic instruction | spawn_reverse_guard_type_offset_caller, finish_type_6c_spawn | tests/test_spawn_oracle.py's existing parametrized suites, automatically extended via oracle_witness.CALLER_POOLS (both guard states recover, no cold-boot-sensitive default; 903 passed) | 82,161 frames; segment_verify PASS on the single child and parent fixture against artifacts/spawn-guardtwo5's reference; full cold comparison pending the next milestone | 1B6836's own recorded-history count 2 (1 recorded path class) plus the unrecorded guard-set arm and reverse-pool-exhausted arm both caught by --vary sweeps; delta to be recorded at the next milestone cold run | from the spawn dispatcher children census; 1B6836 owned inside the spawn dispatcher; leaf_review PASS
2026-09-14 | spawn dispatcher children 1B712C kind 02 and 1B70F8 kind 02 / two-flag and three-flag guard cascades over lower-pool creation, every arm owned | new standalone planners spawn_closure_guard_four_caller (FFF127!=0 and FFF128==0 gate) and spawn_closure_guard_five_caller (FFF126!=0, FFF127==0 and FFF128==0 gate), otherwise the exact same lower-pool/template-0x1B78F0/script/-8-Y-offset tail shape as the closure_guard family; every guard combination for both entries recovers, including the previously-unobserved three-flag decline-3 arm for 1B70F8 caught by a --vary sweep before qualification | spawn_closure_guard_four_caller, finish_guarded_lower_four_spawn; spawn_closure_guard_five_caller, finish_guarded_lower_five_spawn | tests/test_spawn_oracle.py's existing parametrized suites, automatically extended via oracle_witness.CALLER_POOLS (every guard combination recovers, no cold-boot-sensitive default; 936 passed) | 82,161 frames; segment_verify PASS on all child and parent fixtures (3+3 each) against artifacts/spawn-closures6's reference; full cold comparison pending the next milestone | 1B712C's own recorded-history count 8 (3 recorded path classes); 1B70F8's own recorded-history count 9 (3 recorded path classes; the fourth, three-flag decline arm is unrecorded and was caught by --vary); the frontier_ledger fallback counts against the full history are substantially higher (110 and 57) than the census window suggests; delta to be recorded at the next milestone cold run | from the spawn dispatcher children census; both owned inside the spawn dispatcher; leaf_review PASS
2026-09-14 | spawn dispatcher child 1B6654 kind 00 / lower-pool creation, type/flag write and Y-only offset, every arm owned | new standalone planner: lower-pool allocation (template 0x1B7FF8) then, on success, an object type write, a fixed flag byte at record+9, and a +0x10 Y-only offset via the already-proven game.offset_spawn_position -- no script pointer, no X adjustment | spawn_lower_type_flag_offset_caller, finish_type_1b_flag_spawn | tests/test_spawn_oracle.py's existing parametrized suites, automatically extended via oracle_witness.CALLER_POOLS (936 passed) | 82,161 frames; segment_verify PASS on both child and parent fixtures against artifacts/spawn-closures6's reference; full cold comparison pending the next milestone | 1B6654's own recorded-history count 4 (2 recorded path classes) plus the unrecorded lower-pool-exhausted arm caught by a --vary sweep; delta to be recorded at the next milestone cold run | from the spawn dispatcher children census; 1B6654 owned inside the spawn dispatcher; leaf_review PASS
2026-09-14 | spawn dispatcher children 1B70B0/1B7060/1B703C kind 02 / upper-pool finish_type_4c_spawn clones, every arm owned | three new `SPAWN_CLOSURE_CALLER_FACTS` table rows, byte-for-byte finish_type_4c_spawn's own shape (upper pool, template 0x1B79B8, script 0x123e36, cleared +0xA, mode byte) differing only by type byte (0x4B/0x49/0x48) | finish_type_4b_spawn, finish_type_49_spawn, finish_type_48_spawn | tests/test_spawn_oracle.py's existing parametrized suites, automatically extended via oracle_witness.CLOSURE_WRAPPERS/CLOSURE_OUTPUTS (1030 passed) | 82,161 frames; segment_verify PASS on all 6 child and 6 parent fixtures against artifacts/spawn-guardpair7's reference; full cold comparison pending the next milestone | combined recorded-history count 2+2+2=6 (all recorded path classes recover); delta to be recorded at the next milestone cold run | from the spawn dispatcher children census; all three owned inside the spawn dispatcher; leaf_review PASS
2026-09-14 | spawn dispatcher children 1B6636 kind 00 and 1B7084 kind 02 / lower-pool type+XY-offset and upper-pool guarded type/script clone, every arm owned | 1B6636: new standalone planner spawn_lower_type_xy_offset_caller, the same template (0x1B7FF8) and allocator as 1B6654 but with both X and Y offset by +0x10 and no flag byte, no script. 1B7084: new standalone planner spawn_upper_guard_type4a_caller, an FFF179 guard (both states recover) gating the same finish_type_4c_spawn-shaped tail as 1B70B0/1B7060/1B703C, type 0x4A | spawn_lower_type_xy_offset_caller, finish_type_1a_offset_spawn; spawn_upper_guard_type4a_caller, finish_type_4a_spawn | tests/test_spawn_oracle.py's existing parametrized suites, automatically extended via oracle_witness.CALLER_POOLS (1030 passed) | 82,161 frames; segment_verify PASS on all child and parent fixtures (1+1 and 2+2) against artifacts/spawn-guardpair7's reference; full cold comparison pending the next milestone | 1B6636's own recorded-history count 2 plus the unrecorded lower-pool-exhausted arm caught by --vary; 1B7084's own recorded-history count 3 (both recorded path classes recover) plus the unrecorded upper-pool-exhausted arm caught by --vary; delta to be recorded at the next milestone cold run | from the spawn dispatcher children census; both owned inside the spawn dispatcher; leaf_review PASS
2026-09-14 | spawn dispatcher child 1B6C2E kinds 00 / upper-pool creation and FFF175 tile-upload guard, RAM-only arms owned | new standalone planner spawn_upper_tile_two_caller, byte-for-byte the same shape as spawn_upper_tile_caller (1B6C0E), just a different template (0x1B7AA8); a failed allocation and a successful one with FFF175 set both return locally and recover; a successful allocation with FFF175 clear reaches the same 1B2650 VDP tile upload and declines, matching 1B6C0E's own recipe exactly | spawn_upper_tile_two_caller (no new game.* semantics) | tests/test_spawn_partial_arms.py (+11 for this entry: recovered skip arm x6 free/incoming_x combinations, VDP-arm decline x2, isolated-plan decline, mutants x3) | 82,161 frames; segment_verify PASS on both child and both parent fixtures against artifacts/spawn-typeclones8's reference; full cold comparison pending the next milestone | 1B6C2E's own recorded-history count 3 (both recorded path classes are the VDP arm); the FFF175-set and pool-exhausted arms are unobserved and caught by --vary sweeps, both MATCH exactly against spawn_upper_tile_caller's own numbers; delta to be recorded at the next milestone cold run | from the spawn dispatcher children census; 1B6C2E owned inside the spawn dispatcher (parent fixtures confirm ownership on both the recovered and declining arms); leaf_review PASS
2026-09-14 | spawn dispatcher child 1B6E86 kind 02 / FFF107 guard and unconditional upper-pool spawn, every arm owned | new standalone planner spawn_upper_guard_plain_caller: a BEQ.W-form guard (FFF107) whose clear arm declines directly with no spawn attempt at all; whose set arm runs the upper-pool allocator (template 0x1B7C24) then returns immediately with no Z-check and no suffix -- the wrapper's own RTS is the very next instruction after the BSR, so allocation success and pool exhaustion recover identically with no further writes either way. The only spawn-family entry recovered this stint with zero decline and zero writes on both its guard states | spawn_upper_guard_plain_caller (no new game.* semantics) | tests/test_spawn_oracle.py's existing parametrized suites, automatically extended via oracle_witness.CALLER_POOLS (both guard states recover, no decline; 1041 passed) | 82,161 frames; segment_verify PASS on the single child and parent fixture against artifacts/spawn-typeclones8's reference; full cold comparison pending the next milestone | 1B6E86's own recorded-history count 3 (1 recorded path class, the guard-clear decline); the guard-set spawn arm and its pool-exhausted sub-case are both unobserved and caught by a combined --vary sweep; delta to be recorded at the next milestone cold run | from the spawn dispatcher children census; 1B6E86 owned inside the spawn dispatcher; leaf_review PASS
2026-09-14 | spawn dispatcher child 1B6F82 kind 00 / FF7E21 outer guard around the 1B6C0E upper-tile shape, RAM-only arms owned | new standalone planner spawn_upper_guard_tile_caller: CMPI.B #0,FF7E21/BEQ.W gates the whole entry (guard clear declines directly, no spawn attempted); guard set continues into exactly spawn_upper_tile_caller's own shape (upper pool, template 0x1B8228, FFF175 tile-upload guard) -- allocation failure and FFF175-set both recover, FFF175-clear reaches the 1B2650 VDP upload and declines | spawn_upper_guard_tile_caller (no new game.* semantics) | tests/test_spawn_partial_arms.py (+12 for this entry: recovered skip arm x6 free/incoming_x combinations, outer-guard-clear decline arm, VDP-arm decline, isolated-plan decline, mutants x3) | 82,161 frames; segment_verify PASS on both child and both parent fixtures against artifacts/spawn-typeclones8's reference; full cold comparison pending the next milestone | 1B6F82's own recorded-history count 2 (both recorded path classes are the VDP arm); the outer-guard-clear, FFF175-set and pool-exhausted arms are all unobserved and caught by --vary sweeps, all MATCH; delta to be recorded at the next milestone cold run | from the spawn dispatcher children census; 1B6F82 owned inside the spawn dispatcher (parent fixtures confirm ownership); leaf_review PASS
